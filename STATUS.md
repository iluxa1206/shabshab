# FLOATERS DESK — статус проекта

_Обновлено: 2026-08-26. Контекст для новых сессий: прочитай этот файл первым._

**Журнал багов и фиксов** — разделы «Изменения на проде ‹дата›», новые сверху.
Каждый: таблица «коммит · баг · эффект» + чем ловить регрессию. Свежие:
[25.08 формат, люфт, сторожа](#изменения-на-проде-2026-08-25-формат-сигналов-люфт-объёма-сторожа),
[25.08 вечер: каналы и живая лента](#изменения-на-проде-2026-08-25-вечер-каналы-сигналов-и-свежесть-ленты),
[25.08 доступ и окружение](#аудит-доступа-и-окружения-2026-08-25-rate-limit-таймзона-тесты-якорь-дюрации),
[25.08 аудит устойчивости](#аудит-устойчивости-2026-08-25-что-ломается-и-где-считается-неверно),
[24.08 объём и формат](#изменения-на-проде-2026-08-24-объём-в-сигнале-формат-ревью-оферт),
[21.08 доставка сигналов](#изменения-на-проде-2026-08-21-вечер-доставка-сигналов-и-нкд),
[21.08 аудит расчётов](#изменения-на-проде-2026-08-21-аудит-расчётов-8-багов),
[20.08 Y-IDX на прошлую дату](#изменения-на-проде-2026-08-20-y-idx-на-прошлую-дату).

> ⚠️ Разделы ниже «Резервные копии» писались 2026-07-02 и местами устарели (прод переехал с deskdeskdesk.ru на assetallocator.ru/desk, слой НРД удалён, модуль «Фонды» снесён). Свежее — в верхних разделах и в git-логе.

## Что это
Дашборд аналитики **облигаций с плавающим купоном (флоатеров, RUB)**. FastAPI-бэкенд + React-фронт (Vite). Показывает весь рынок флоатеров с оценкой: discount margin (DM), спред, доходности, рейтинги, cashflow.

## Как запустить
```bash
cd /Users/ishabaev/python_projects/shabshab
.venv/bin/python -m uvicorn api.main:app --port 8000
# фронт: cd frontend-react && npm run build  (FastAPI отдаёт dist на /app)
# открыть http://127.0.0.1:8000/app/
```
Превью (Claude): `.claude/launch.json` server name `api`. React-правки требуют `npm run build` (FastAPI отдаёт статику из `frontend-react/dist`, не live src).

## Прод / деплой (assetallocator.ru/desk)
Живёт на **https://assetallocator.ru/desk/app/** (VPS 161.104.17.23, Ubuntu 24.04).
Домен `deskdeskdesk.ru` МЁРТВ — прод переехал 2026-07-21 на path-route
`/desk` в Caddy. Ссылки на старый домен в разделах ниже читать как
исторические.

**Деплой одной командой** (с этой машины): `./scripts/deploy.sh`
— rsync кода в `/root/floaters` → `docker compose -f docker-compose.prod.yml --env-file .env up -d --build` → чистка кэша сборки старше 24ч (иначе он тихо съедает диск) → cron бэкапов → healthcheck. Фронт (`npm run build`) собирается ВНУТРИ Docker (multi-stage `Dockerfile`), пересборка вручную не нужна.

**Инфра:** сервер НЕ выделенный — на нём уже стек `astra-prod` (`/root/catalog/`, сайт assetallocator.ru: Next.js+Postgres+**Caddy** на 80/443). Наш проект — отдельный compose-проект `floaters-prod`:
- один контейнер `floaters` (uvicorn :8000), подключён к внешней docker-сети `astra-prod_default`, лимит `mem_limit: 768m`.
- Caddy (чужой, `/root/catalog/Caddyfile`) проксирует наш путь: `handle_path /desk/*` в блоке assetallocator.ru → `reverse_proxy floaters:8000`, TLS Let's Encrypt авто. WS (`/api/ws/market`) проксируется как wss.

**Доступ:** `ssh root@161.104.17.23` по ключу `~/.ssh/id_ed25519` (уже установлен).
**Секреты:** `.env` (Alor/НРД/`AUTH_SECRET` креды) лежит в `/root/floaters/.env`, прокинут через `env_file` (в git/образ НЕ попадает). `deploy.sh` **исключает `.env` и `data/` из rsync** (`--exclude`) — прод-секреты и БД юзеров не затираются локальными; правятся вручную на сервере.
**Таймзона сервера — МСК, не UTC** (`date` на 161.104.17.23 отдаёт MSK, cron берёт ту же зону). Комментарий в `deploy.sh` про «сервер в UTC → 01:30» врал, и бэкап уходил в 01:30 МСК — ДО ночных прунов, мимо замысла. Исправлено 2026-08-21: в cron пишем московские часы как есть (бэкап 04:30).

**Правка Caddy** (после ручного изменения Caddyfile): `docker exec astra-prod-caddy-1 caddy reload --config /etc/caddy/Caddyfile`.
**DNS:** отдельного домена у дашборда больше нет — он живёт путём `/desk` на
assetallocator.ru (A-запись домена ведёт на 161.104.17.23).

## Авторизация (доступ по аккаунту)
Весь сайт закрыт логином — самостоятельной регистрации НЕТ, аккаунты заводит только админ (CLI). Чужие аккаунты попасть не могут.

**Как устроено:**
- Гейт: зависимость `require_user` на роутерах данных (`meta`/`bonds`/`curves`/`orderbook`) в `api/main.py`. Открыты только `/api/health` и `/api/auth/*`. WS проверяет cookie на хендшейке (`api/routes/ws.py`).
- Сессия: JWT (HS256, подпись `AUTH_SECRET`) в httpOnly-cookie `session`, срок 7 дней (`api/routes/auth.py`). Удаление юзера сразу лишает доступа (токен сверяется с хранилищем).
- Хранилище: bcrypt-хеши в `data/users.json` (атомарная запись, `services/auth_users.py`). В проде — Docker-том `./data:/app/data` (переживает редеплой). Файл gitignored.
- Фронт: `Login.jsx` + гейт в `App.jsx` (проверяет `/api/auth/me`), кнопка «Выход» в `Topbar`. 401 в любом запросе → редирект на логин.

**Env (`.env`):** `AUTH_SECRET` — обязателен (без него все запросы 401, fail-closed). `AUTH_COOKIE_SECURE` — `1` в проде (HTTPS), `0` локально (HTTP). Генерация секрета: `openssl rand -hex 32`.

**Управление доступом (CLI `scripts/useradd.py`):**
```bash
# локально
python scripts/useradd.py add user@example.com --role admin   # спросит пароль
python scripts/useradd.py list
python scripts/useradd.py remove user@example.com
# в проде — внутри контейнера (пишет в том data/):
docker compose -f docker-compose.prod.yml exec floaters python scripts/useradd.py add user@example.com
```

**Первый деплой авторизации:** (1) добавить `AUTH_SECRET` (+ убрать/выставить `AUTH_COOKIE_SECURE=1`) в `/root/floaters/.env`; (2) `./scripts/deploy.sh`; (3) завести первого юзера через `docker compose ... exec`. Без юзеров сайт закрыт для всех.

Файлы деплоя (в репо): `Dockerfile`, `.dockerignore`, `docker-compose.prod.yml`, `scripts/deploy.sh`.

## Изменения на проде 2026-08-25 (формат сигналов, люфт объёма, сторожа)

Задеплоено семью коммитами: `6188bc5` `be44614` `5ef4286` `2f8ffed` `3831952`
`5fdf5cf` `94dbbf0`. Общая нить захода — «прибор не должен молчать и не должен
заставлять читателя работать глазами».

### Формат и доставка сигналов

- **Формат сообщения доведён** (25.08, см. память telegram-bot): шапка «спред ·
  выпуск (срок)» (у сделки замыкает объём), под ней цена — у заявки с рейтингом
  и накопленным объёмом, **цена и объём жирным**; стакан сразу под ценой, объём
  в нём **в бумагах**; следом формула купона с ISIN; обстоятельства
  срабатывания (уровни, причина, время, порог) — в подписи к имени фильтра.
- **Люфт порога объёма 10 %** (`screener_core.money_floor`, 25.08): порог
  объёма — про ПОРЯДОК, а не про границу до рубля, поэтому «от 50 млн» ловит и
  сделку на 48. Коэффициент `VOL_TOL=0.9` (он же уже применялся к неполному
  набору по лестнице) протянут во ВСЕ пороги объёма: `block_matches`, режим
  «одна заявка» в `evaluate_candidates`, превью блок-фильтра, предвыборка
  алертов (`alert_floor`, `notify_blocks`), лента и таблица
  (`block_trades._where`, `read_days`, `tape._cond`). Иначе витрина и алерты по
  одной настройке означали бы разное. Набор по лестнице (`vwap_passes`) и фронт
  (`vwap.js`) жили по этому правилу и раньше — теперь оно одно на всех.
- **Сделки поштучно + курсив подписи** (25.08): одно сообщение = один НАБОР
  одинаковых сделок. `tg_notify._group` бьёт block-события по `_trade_key`
  (выпуск + цена + сторона + адресность): разные параметры — разные сообщения,
  а части одного принта склеиваются в карточку с СУММАРНЫМ объёмом,
  расшифровкой «3 сделки · 198м · 47,8м · 6,4м» под параметрами и интервалом
  времени в подписи, если набор растянут. Имя алерта в
  подписи стало курсивом: жирные теперь только цена и объём. Каветат: поток
  сделок = поток сообщений (MAX_BURST=5/тик на чат); 429 лечится ретраем с
  retry_after, но в канал серия поедет с задержкой.
- **Нить по бумаге вместо follow-up** (25.08): `services/signal_followup.py`
  удалён вместе с таблицей и воркером — проверка «что стало через 15 минут»
  отвечала на вопрос, который рынок и так отвечает следующим сигналом. Вместо
  неё повтор по выпуску (спред уехал, объём набрался) уходит РЕПЛАЕМ на прошлое
  сообщение о нём: в чате история одной заявки. Нить ведётся по ключу буфера
  (чат, фильтр, выпуск), начинается заново на «заявке» и протухает за
  `TG_THREAD_TTL_MIN` (180 мин).

### Сторожа: тихий отказ перестал быть тихим

Оба сторожа делят механику `api/main._watch_alert`: новая беда сообщается
сразу, известная — не чаще `STREAM_ALERT_REPEAT_MIN`=60 мин, восстановление
даёт разовый «отбой», а НЕдоставленное предупреждение не помечается
сообщённым (иначе одна сетевая ошибка хоронит его до конца дня). Память тревог
у сторожей раздельная. Адресат — админы с привязанным чатом
(`tg_notify.notify_admins`, роль из `auth_users`); на проде это
`admin@astra.local`, 2 чата.

- **Молчание стримов** (`5fdf5cf`): `stream_watchdog` существовал и раньше и
  честно ловил три случая (0 бумаг на сокетах, живые сокеты при пустом кэше
  глубины, нет сделок дольше `STREAM_SILENCE_MIN`), но писал ТОЛЬКО в лог —
  а в этой ситуации в лог никто не идёт, потому что снаружи всё выглядит как
  спокойный рынок. Теперь находки уходят в телеграм.
- **Место и копии** (`94dbbf0`): `disk_watchdog` раз в час. Место меряется не
  «сколько осталось вообще», а хватит ли на обслуживание — `DISK_NEED_RATIO`=1,5
  × размер базы, потому что и VACUUM, и бэкап требуют места в размер базы.
  Отдельно проверяется СВЕЖЕСТЬ последней копии (`BACKUP_STALE_HOURS`=36) —
  результат, а не механика: так ловится и упавший крон, и отказ по месту, и
  битый файл, не доживший до ротации. Пропущенный ночной VACUUM сообщается из
  самого обслуживания.

**Чем это НЕ является:** ни один сторож не лечит причину. Место по-прежнему
кончается от роста базы, ретеншен тикового архива — ручное решение.

### Прод-операция 25.08

Таблица `signal_followup` и индекс `ix_followup_due` удалены из прод-базы
(`integrity_check: ok`). 50 строк перед удалением выгружены в
`data/backups/signal_followup_dump_2026-08-25.json` на сервере.


## Изменения на проде 2026-08-25, вечер (каналы сигналов и свежесть ленты)

Заход начался с жалобы «в Р5 ничего не пишет, а ВЭБы вроде должны быть» и
кончился двумя независимыми находками: канал Р5 не получал блоки НИ РАЗУ, а
живая лента Alor теряла пятую часть крупных сделок. Обе — тихие: бот отвечал,
сокеты были подняты, ошибок в логах не было.

### Исправлено

| # | коммит | где | баг | эффект |
|---|---|---|---|---|
| 1 | `1b358ce` | `services/block_trades.py:920` | **дедуп крупных сделок ключевался по ПОЛЬЗОВАТЕЛЮ.** Из нескольких блок-фильтров одного аккаунта сделку забирал первый подошедший по id — замысел «два письма об одном принте это шум» | у кого фильтры разведены по каналам, широкий фильтр съедал сделку у узкого: «сделки Ф5» (порог 1 млн, id=7) забирал всё, а канал «Р5» (порог 50 млн, id=9) не получил **ни одного события за всё время** — 0 строк в `signal_events` против 107 у соседа. Ключ дедупа теперь `(пользователь, tg_target_id)`: дубль в один чат по-прежнему невозможен, разные каналы получают свою копию |
| 2 | `8389fca` | `services/trades_stream.py`, `services/market_data.py` | **валютный номинал ломал порог.** У замещающих и юаневых выпусков `FACEVALUE` — в долларах/юанях (`FACEUNIT`), а `CURRENCYID` при этом `SUR` (расчёты рублёвые). Объём тика считался как `qty × face × price` | занижение в 12–83 раза → порог `TRADES_STREAM_MIN_RUB` выбрасывал сделку на миллион как мелочь: **233 сделки за 2026-08-25**, в ленту они попадали только ISS-дрейном с его 15 минутами. Листинг теперь отдаёт `FACEUNIT`, стрим держит курс (`services/fx`, TTL 10 мин) рядом с номиналами |
| 3 | `8389fca` | `services/trades_stream.py:_shard_socket` | **обрыв сокета = дыра в ленте.** Переподписка шла с `depth=0` (только новые сделки), и всё, что случилось за время разрыва, приезжало потом ISS-дрейном | 57 крупных биржевых сделок за день, кучами вокруг обрывов и рестартов. В том числе ВЭБP-41 на 60,1 млн: сделка 16:32:11, запись 16:47:26, звонок 16:47:27 — с этого и началось разбирательство. Переподписка (не первая) теперь берёт `depth=10`; дубли снимает `INSERT OR IGNORE` по TRADENO |

### Новое

- **Сторож свежести ленты** (`api/main.py:feed_lag_watchdog`, раз в 30 мин в
  торговые часы). Отвечает не на вопрос «поток есть?» (это `stream_watchdog`), а
  на «доезжает ли он вовремя»: доля сделок, записанных в первую минуту после
  совершения (`block_trades.live_capture`). Ниже `FEED_LIVE_MIN` (0.5) при
  выборке от 20 сделок — тревога админам в телеграм. Свежий хвост окна из счёта
  выброшен: сделку минутной давности ISS ещё не привозил, и она посчиталась бы
  «живой» просто по отсутствию копии.
- **Видимость шардов.** У каждого из 13 сокетов свои счётчики (тики, обрывы,
  переподписки, минуты тишины) в `/api/status` и в сводке лога; обрыв пишется
  явно. Тики считаются ДО порога — это признак жизни сокета, а не ликвидности
  его бумаг.
- **Отсечка возраста в `_alert_rows`.** Добор хвоста нужен архиву, но звонить о
  сделке получасовой давности нельзя — порог `BLOCK_ALERT_MAX_AGE_MIN` (10 мин)
  теперь применяется и ко времени самой сделки, не только ко времени записи.

### Как мерить (пригодится при следующей жалобе на лаг)

Лаг ленты меряется разницей `ins_at − ts` по `block_trade`, и это единственный
надёжный признак: «сокеты живы» и «сделки доезжают» — разные вещи.

```sql
-- крупные БИРЖЕВЫЕ (РПС исключены: их Alor не отдаёт вовсе) за час
SELECT ts, ins_at, board, value FROM block_trade
WHERE ins_at >= strftime('%s','now')-3600 AND market!='ndm' AND value>=1000000;
-- лаг < 5с → живой тик Alor; ~900с → приехало ISS-дрейном
```
Быстрее — `block_trades.live_capture(60)` прямо в контейнере: `{total, live, ratio}`.

Замер до правок (2026-08-25): 1545 крупных биржевых за день, живьём 1255 (81%),
поздних 290. После деплоя в 17:2x: `13/13` шардов живы, немых нет,
`live_capture(60)` = 134/174 (77%) — окно ещё захватывало дореплойный час.
**Чистый замер брать с сессии 26.08.**

### Каветаты

- **РПС (`market=ndm`: PSAU, PTOB, PSOB) навсегда +15 минут.** Подписки на
  адресные сделки у брокера нет в принципе, закрыть нечем. 132 сделки за день —
  это норма, а не отказ. Учитывать при чтении `live_capture`: там они
  отфильтрованы, руками — не забывать `market!='ndm'`.
- **Курс есть не для всех валют.** `services/fx` знает USD/CNY/EUR (TOM MOEX +
  ЦБ). Бумага с иным номиналом (сегодня — 2 выпуска в CHF) даёт `fx_ok=False`:
  тик пишется в архив, но в звонок не идёт. Так задумано — недостоверный
  рублёвый объём хуже опоздавшего сигнала; счётчик таких — `no_fx` в
  `/api/status`.
- **Вебхук Telegram на этом VPS не работает** (проверено ещё раз 25.08:
  `getWebhookInfo` пуст, `TG_POLLING=1`, оба канала admin). Ставить вебхук —
  значит сломать поллинг: `getUpdates` начнёт отдавать 409. См. предупреждение
  в `scripts/tg_set_webhook.py`.
- **Диагностика на проде** — только `docker compose … cp script.py floaters:/app/`
  + `exec`. Отдельный процесс НЕ видит состояние воркеров: `trades_stream.stats()`
  и `live_isins()` там пустые (это состояние в памяти работающего процесса) —
  на этом легко построить ложный вывод «бумага не на сокете».

Регрессия ловится `tests/test_block_alerts.py` (один принт в два канала, дубль в
один канал, курс в пороге, старый добор не звонит) и `tests/test_stream_watchdog.py`
(текст тревоги, `live_capture` по разрешившемуся окну).


## Аудит доступа и окружения 2026-08-25 (rate-limit, таймзона, тесты, якорь дюрации)

Второй заход того же дня, независимо от «аудита устойчивости» ниже — пересечений
по файлам нет. Часть находок пришла из внешнего разбора; **проверял каждую по
коду, треть не подтвердилась** (см. «Что НЕ подтвердилось»). Коммит `42a8388`
собрал эти правки вместе с чужой работой под сообщением про спред по
средневзвесу — искать их по git log бесполезно, ориентир здесь.

### Исправлено

| # | где | баг | эффект |
|---|---|---|---|
| 1 | `api/routes/auth.py` | **rate-limit логина самоотключался через час.** Второй элемент `_LOGIN_FAILS` хранил начало окна и не двигался, а блокировка капнута 3600с → при `now > start+3600` остаток уходил в минус НАВСЕГДА: счётчик рос, блок больше не наступал | 5 промахов → час ожидания → дальше **неограниченный перебор**. Теперь отсчёт от ПОСЛЕДНЕЙ неудачи (`_login_block_sec`), счётчик остывает, каждый промах сверх порога начинает блок заново |
| 2 | `Dockerfile`, `docker-compose.prod.yml` | **контейнер жил в UTC.** Хост МСК (см. выше), но база `python:3.12-slim` — UTC, а сервисный слой опирается на наивный `date.today()` (144 вхождения; наивных `datetime.now()` — ноль, воркеры уже на `_MSK`) | в окне 00:00–03:00 МСК «сегодня» съезжало на вчера. Конкретно: часовой налив баров на :07 в 01:07/02:07 относил сегодняшний бар к прошлому дню. `TZ=Etc/GMT-3` (знак по POSIX инвертирован) + **явная установка `tzdata`** — без пакета переменная игнорируется молча и фикс выглядит применённым, оставаясь нерабочим |
| 3 | `services/metrics.py` | **якорь дюрации разъезжался с доходностью.** `macaulay_years`/`duration_metrics` считали τ от `calc_date`, а `solve_flat_y`, выдавшая `y`, — от `settle_date` (T+1 раб) | сдвиг τ выносится за скобки, поэтому Macaulay завышалась РОВНО на зазор: 1 день в будни, 3 в пятницу (+0,0082 года, +0,35 % на дюрации 2,3). Правка **внутри функции**, не на месте вызова — и это спасло: `0c28bdd` завёл новый путь витрины (`zspread.py:301`), который зовёт ту же `macaulay_years` и получил фикс даром |
| 4 | `tests/test_signals.py`, `test_tg_bot.py`, `test_audit_fixes.py` | **три теста писали в боевую `data/portfolio.db`** — единственные без подмены пути. Источник фиктивных ISIN `RU000A0000A1/B2/C3` и 45 событий под `u@x.ru` в `signal_events` | у `test_signals` зачистка `signal_state` шла БЕЗ фильтра по `user_email` — удаляла осиротевшие строки всех пользователей. Теперь `monkeypatch.setattr(pdb, "DB_PATH", tmp_path/...)`; reload не нужен, `_connect()` читает глобал в момент вызова |
| 5 | `api/routes/tg.py` | имя бумаги вставлялось в HTML-сообщение без экранирования (рядом `_targets_text` экранирует title) | `&` или `<` в названии → Bot API «can't parse entities» → **сигнал не доходит совсем**. Лента сделок тянет имена по всему рынку (3145 бумаг), не по юниверсу. Заодно `hmac.compare_digest` на секрете вебхука |
| 6 | `services/coupon_calib.py` | защитная правка: 7 точек `projected_ks_pct` возвращали `0.0`, когда ставку восстановить нечем → купон читался как чистая маржа выпуска | `None` + warning через `_no_rate`. Контракт `Optional[float]` был сквозным и до этого: `period_index_pct` пробрасывает наверх, `core/valuation.py:817` откатывается на форвард-проекцию. **Измеренный эффект сегодня — НОЛЬ** (см. ниже) |

Регрессия ловится `tests/test_audit_2026_08_25.py` (13 тестов). Проверено, что они
не пустые: на дофиксовом файле падают.

### Кому на самом деле принадлежит BAD 34 → 20

Мерил отдачу правки 6 через `scripts/verify_fixing_specs`: вердикты пошли
BAD 34 → 19, и это едва не записали ей в заслугу. **Неверно.** Три проверки:

- счётчик на ветке `_no_rate` — **ноль срабатываний** на всём универсе;
- старая и новая версии функции в одном процессе на одних входах — значения
  совпадают до последнего знака;
- решающий опыт: оставить правку 6, откатить **только** `valueprc` → **BAD снова 34**.

Весь выигрыш даёт `3bbdf2d` (наблюдённая ставка купона из `valueprc`, а не
пересчёт из рублей и восстановленного номинала). 14 бумаг перестают быть ложными
BAD: СНХТ БО-02 3,103 → 0,000 пп, АРЕНЗА1Р05 2,202 → 0,000, Саммит 1P3 2,131 → 0,000
и далее. Правка 6 — страховка на случай, которого сегодня не бывает.

**Как я на это купился:** бэкап делался `cp` перед правкой, но дерево параллельно
жило другой сессией, и в «чистой базе» не хватало ещё и `valueprc`. Базу брать
`git show HEAD:file`, не `cp`.

### Свойства прибора `verify_fixing_specs` (учитывать при замерах)

- **NO_DATA шумит ±5** между прогонами идентичного кода — флуктуация загрузки
  расписаний MOEX. BAD/OK стабильны бит-в-бит (по два прогона каждой версии).
  Сравнивать версии только по BAD.
- **cbr.ru отдаёт 403** с рабочей машины, в т.ч. с браузерным User-Agent и на
  `XML_daily.asp`. Слой честно падает в кэш с warning'ом; кэш КС был свежий
  (до текущей даты), на замер не повлияло. Похоже на блокировку канала
  разработчика, не на прод — прод на российском VPS.

### Что НЕ подтвердилось (не переоткрывать)

| заявка | проверка |
|---|---|
| «rate-limit блокирует жертву за NAT навсегда, верный пароль не пускает» | **полярность обратная.** Успех зовёт `_login_reset` (`auth.py:181`), заблокированная попытка отдаёт 429 ДО записи фейла — счётчик под блоком не растёт. Реальный дефект был противоположный: обход, а не блокировка |
| «`block_trades.py:100,158` режут дневные агрегаты по UTC» | `:100` — ключ суточного кэша `secid_map`; `:158` — фолбэк на отсутствующий `TRADEDATE`. Настоящий `backfill()` (`:539`) уже на `datetime.now(_MSK).date()` |
| «`signals.py:500 preview_block` показывает пустой день» | направление обратное: UTC-дата ОТСТАЁТ, фильтр захватит вчерашний день. Окно 00:00–03:00 МСК, рынок закрыт |
| «`spread_history.py:138,152` — границы окон истории» | это `cutoff = today − 400 дней`. Сдвиг 3 часа на 400-дневном окне |
| «`coupon_calib` завышает ставку при дырах: делитель уменьшается, числитель нет» | `factor *= …` и `n += 1` стоят внутри одного `if k is not None` — синхронны. Формула корректно годовит по известным дням |
| «легаси `calculate_floater_metrics` тихо отдаёт старую арифметику в проде» | зовётся только из `core/last_prices.py:300`; модуль не импортируют ни `api/`, ни `services/`. Мёртвый код, не прод-путь |
| «`telegram.py:86` — `r.json()` вне try» | внутри try (`:81`). Но `except` ловит только `httpx.ConnectError`/`HTTPError`, а `JSONDecodeError` — это `ValueError` и улетит наружу. Вывод верный, обоснование нет |

### Не закрыто

- **TZ не проверен на живом.** Docker-демон на рабочей машине не поднят. После
  деплоя (обязательна пересборка — в образе новый слой `apt-get install tzdata`):
  `docker compose -f docker-compose.prod.yml exec floaters python -c "import datetime; print(datetime.datetime.now(), datetime.date.today())"` — ждём МСК.
- **Мусор в локальной `data/portfolio.db`**: 45 событий по фиктивным
  `RU000A0000A1/B2/C3`. Чистится
  `DELETE FROM signal_events WHERE isin IN ('RU000A0000A1','RU000A0000B2','RU000A0000C3');`
- **Не разбирались** (из внешнего аудита, правдоподобны, дёшево проверяются):
  `bars.py:1016` — `resample` берёт `face` последнего бара ведра, у амортизируемых
  в день транша цена бара едет; `metrics.py:122` — `breakeven_base_pct` на
  скалярном `spread_issue_bps` мимо лесенки маржи.


## Аудит устойчивости 2026-08-25 (что ломается и где считается неверно)

### Критическое — исправлено

| # | коммит | что | эффект |
|---|---|---|---|
| 1 | `e9276f0` | **кэш сборки Docker рос с каждым деплоем**: 9,2 ГБ при 5 ГБ свободного диска | ещё несколько деплоев — и SQLite перестала бы писать. Разовая очистка вернула 7,6 ГБ, `deploy.sh` теперь чистит хвост старше суток |
| 2 | — | **отказ Telegram стирал сигналы**: буфер чистился ДО результата отправки | упавший прокси-сайдкар = сигналы пропали навсегда (в вебе есть, в чат не придут). Теперь неудачная отправка возвращает запись в очередь на 10 минут |
| 3 | `48f267e` `dabde10` | нулевой НКД без расписания проходил насквозь; НКД теперь считается сам по параметрам выпуска | РостелP21R 250 → 122 bps (норма 121) |
| 4 | — | приоритет источников спеки: проспект выше bondresearch | 7 бумаг РЖД/РСХБ/Росагро: 0,386 → 0,008 пп |

### Проверено сценариями — держит

| сценарий | поведение |
|---|---|
| кривая KEYRATE/RUONIA недоступна | контекст не собирается → бумага молчит (fail-closed), кривая пересобирается из кэша котировок |
| расписание купонов пусто | периоды достраиваются из bondization; НКД считается по параметрам выпуска |
| стакан пуст / метрик нет / цена стейл | скринер отдаёт пустой набор, сигналов нет |
| погашение через день | `MATURED`, спред-метрики скрыты (проверено на Магнит5P03) |
| ex-coupon (купон падает НА дату поставки) | НКД=0, платёж исключён из потока покупателя — 7 бумаг 25.08 посчитаны верно |
| горизонт put/call | верхнеуровневое поле остаётся «к погашению», но ВСЕ потребители (фронт, витрина, скринер, стакан, история, лента сделок) читают выбранный горизонт |
| лесенка маржи (108 бумаг) | границы диапазонов применяются верно, бэктест OK |
| амортизация, оферты, кэпы | мягкая деградация, ложных чисел нет |

### Остаточные риски

- **13 бумаг со спекой фиксинга** (макс 0,75 пп ≈ 75 bps в R-spread) + 30 бумаг на дефолтном режиме, из них 3 BAD.
- **ПионЛиз** — 0,44 пп: в проспекте «срочная версия RUONIA на 6 месяцев», это отдельный ряд ЦБ; точно закрыть можно только подключив term-RUONIA с cbr.ru.
- ~~**Тихий отказ стримов**~~ — ЗАКРЫТ 25.08: сторож `stream_watchdog` был и
  раньше, но писал только в лог, куда в этой ситуации никто не идёт (сигналов
  нет — значит «сегодня тихо»). Теперь находки уходят В ТЕЛЕГРАМ админам
  (`tg_notify.notify_admins`): новая поломка сразу, известная не чаще
  `STREAM_ALERT_REPEAT_MIN`=60 мин, восстановление — разовый «отбой».
  Недоставленное предупреждение не считается сообщённым и повторится
  следующим тактом.
- **`status=SUCCESS` при пустом Y-IDX**, когда недоступна RUONIA-кривая, — косметическая несогласованность.
- **Диск**: `portfolio.db` 2,8 ГБ + копии 1,6 ГБ, свободно 4,9 ГБ из 30 (83 %);
  растёт на десятки МБ в день. Внезапного падения не будет — и VACUUM
  (`trades_archive.vacuum`), и `scripts/backup_db.py` заранее проверяют место и
  ОТКАЗЫВАЮТСЯ. Опасен был тихий отказ: skip в лог, база дальше растёт, копии
  не снимаются. 25.08 добавлен `disk_watchdog` (раз в час): тревога админам,
  когда свободного меньше `DISK_NEED_RATIO`×размер базы (нужно на VACUUM и
  бэкап) или когда свежей копии нет дольше `BACKUP_STALE_HOURS`=36; пропущенный
  ночной VACUUM сообщается отдельно. САМ РОСТ не лечится — ретеншен архива и
  объём базы остаются на ручном контроле.

## Изменения на проде 2026-08-24 (объём в сигнале, формат, ревью оферт)

### Найденное и исправленное

| # | коммит | баг | эффект |
|---|---|---|---|
| 0a | `4b84567` `bd4137b` | **ISS изредка отдаёт ACCRUEDINT=0 посреди купонного периода** — цена считалась «чистой», без накопленного купона | РостелP21R: сигнал 233 bps против 120 верных; Газпн3P14R: 259 против 61. Прод-замер: ложный ноль у 5 из 250 бумаг (2 %), у одной НКД 91 ₽ |
| 0b | `926ddd8` | у ипотечных агентов MOEX публикует НЕПОЛНЫЙ график амортизаций (дальние транши нулями), а «остаток = Σ будущих» превращал это в копейки | sИАДОМ1P19: номинал 8,78 ₽ вместо 577,64. Воспроизведение: номинал 900 → 1594 bps, 500 → 14 532 при верных 95 |
| 0c | `413ab3b` | санити-пороги гасили метрики ПООДИНОЧКЕ: Y-IDX скрыт как безумный, а DM из того же расчёта остаётся | 9 флоатеров в снапшоте с пустым Y-IDX и dm до 14 824 bps — уезжало в spread_daily |
| 1 | `58ccbc5` | объём в шапке заявки брался из `money_ok_rub`, а он БЕЗ границ спреда считает всю сторону книги | Газпн3P13R: 20,7м вместо 3,8м |
| 2 | — | вторая попытка мерила накопление по СРЕДНЕВЗВЕСУ набора, а он лучше худшего взятого уровня | Газпн3P14R: 250,5к при наборе 1 млн — меньше собственного набора |
| 3 | — | цена в сообщении округлена до 2 знаков (99,7495 → «99,75»), из-за чего расхождение выглядело необъяснимым | сбивало при разборе |
| — | — | `verify_fixing_specs` терял `compounded` (см. раздел 21.08) | 11 ложных WARN |

**Итог:** объём считается до ГРАНИЦЫ набора — цены последнего взятого уровня
(`vwap_for.last_px`). Для верха стакана и режима «одна крупная заявка» граница
совпадает с ценой сигнала.

### Новое

- **Повтор по объёму отключается**: параметр фильтра `repeat_on_money`, чекбокс
  «и при изменении объёма». У ликвидной бумаги стакан дышит объёмом постоянно —
  тому, кто следит за спредом, это шум. Спред и первое попадание приходят всегда.
- **Очередь «пересмотр на оферте»** (`offer_reset`): ночная валидация сравнивает
  фактическую маржу по купонам до и после прошедшей оферты; сдвиг ≥25 bps значит,
  что эмитент выпуск пересматривает. РАСЧЁТ НЕ МЕНЯЕТСЯ — только ревью, потому
  что `var_type` из Cbonds (по нему решается `cut_at_offer`) пуст у 115 из 134
  бумаг с будущей офертой. На проде: детект сработал у 17, в очереди 7.
### Аудит 24.08 (что проверено и оказалось в норме)

Прогон инвариантами по проду: целостность БД `ok`, 2,96 ГБ, диск 9 ГБ свободно;
монотонность Y-IDX по цене — 1 нарушение на 150 бумаг (оно же баг 0b);
`spread_daily` за сегодня 1315 строк, у флоатеров без Y-IDX только 9 (они же
0c), без z-спреда 27 из 1315; расхождение
«событие сигнала vs свежий пересчёт» — 3 из 40 (все три = баги 0a).

**Наблюдение, не баг:** 91 запись «event loop лаг» за сутки, максимум 3,9 с,
30 раз дольше 2 с; стеки указывают на солвер DM (`pv_cashflows_with_dm`) и
`coupon_calib`. Сайт в эти секунды подтормаживает. Отдельная тема про heavy-пул.

## Изменения на проде 2026-08-21, вечер (доставка сигналов и НКД)

Началось с «почему алерт в телеграм приходит с задержкой», разошлось на
доставку, формат сообщений, маршрутизацию по каналам и — попутно — на корневой
баг оценки. Всё задеплоено, история коротких бумаг пересчитана.

### Найденное и исправленное

| # | коммит | баг | эффект |
|---|---|---|---|
| 1 | `1be17ea` | буфер телеграма ждал таймер 30 с даже по молчавшему чату | заявки ехали 3–33 с |
| 2 | — | воркер спал ДО флаша: событие сразу после такта ждало полное окно | +30 с к любому сигналу |
| 3 | — | `httpx.AsyncClient` на каждый вызов: TLS-рукопожатие через прокси-сайдкар | 0,3–1 с на сообщение |
| 4 | — | чаты обходились последовательно | хвост пачки приезжал после головы |
| 5 | `0e3da9e` | безадресные сделки звонили с ISS-ленты | **~15 минут** задержки |
| 6 | `8464da4` | кривая замораживалась в контексте расчёта: при стейл-котировках `market_data` пересобирает её раз в 15 мин, скринер считал на своей | расхождение с текущей моделью |
| 7 | `31ba1b5` | контекст reprice кэшировался НА ДЕНЬ — один неудачный сбор травил число до полуночи | до 78 bps на короткой бумаге |
| 8 | — | лестница стакана в сообщении считалась наклоном, а шапка — `exact_y_idx` | 181 в шапке против 103 в стакане |
| 9 | `85a6d77` | **корень:** НКД брался на дату биржи (`SETTLEDATE`), а срок считался до НАШЕЙ поставки; в пятницу это 3 дня | 181 вместо 103 bps (2,5-мес бумага) |
| 10 | `1032666` | вкладка СИГНАЛЫ падала: `targets` не объявлен в `BookFilterRow` | белый экран вкладки |
| — | `5d2abcd` | ISIN печатался дважды у бумаги без имени (сделки идут по всему рынку, справочник — по юниверсу) | шум в сообщении |
| — | `15ada92` | `tg_set_webhook.py` мог убить поллинг (getUpdates → 409) | бот молчит |

**Общий корень у 6–9:** снимок мира, замороженный в кэше, и величина, посчитанная
на ОДНУ дату, но использованная вместе с другой. Проверять надо не «правильно ли
число», а «на одну ли дату посчитаны его части».

### Что стало быстрее

| событие | было | стало |
|---|---|---|
| заявка стакана | 3–33 с | ~1–4 с |
| безадресная крупная сделка | ~15 мин | ~3–5 с |
| РПС / адресная | ~15 мин | без изменений (подписки у брокера нет) |

### Новое в боте и на сайте

- **`/custom`** — свои эмодзи маркеров (5 слотов: оффер, бид, покупка, продажа,
  адресная), НА ЧАТ. Хранится в `tg_users.emoji` (JSON), кладётся в буфер там же,
  где известен адресат. Значением принимается только эмодзи/значок: буквы ломают
  вёрстку строки, `<` — разбор HTML, и Telegram отбивает сообщение целиком.
- **`/chats` + каналы доставки на фильтр** (`tg_targets`, `signal_filters.
  tg_target_id`): «Р5» уходит в канал «Р5», «Ф5» — в свой, без адресата — в личку.
  Канал регистрируется пересылкой поста боту (Telegram сам отдаёт id и название);
  при привязке сразу пишем в канал — право писать есть только у администратора.
  Снятый канал не ломает фильтр: доставка возвращается в личку, а не молчит.
- **Формат сообщения** (итоговый):
  ```
  🔴  103 бп · 586к ₽ · 2 мес · РусГид2Р01     ← имя моноширинным, без ссылки
  100,23% · 4 ур · заявка · 16:34:40 · >1м     ← время с секундами, порог фильтра
  RU000A10A349                                  ← ISIN своей строкой (tap-to-copy)
  ▸ стакан (сворачиваемая цитата)
  Тест формата · оффер                          ← подпись фильтра, без значка
  ```
  Объём в шапке ЗАЯВКИ — `money_ok_rub` (деньги уровней в диапазоне спреда), а не
  `money_rub`: тот в режиме порога ≈ сам порог, то есть сообщение повторяло
  настройку вместо рынка.

### Чем ловить регрессии этого класса

| инвариант | что ловит | норма |
|---|---|---|
| НКД и срок посчитаны на одну дату | рассинхрон `SETTLEDATE` ↔ `settle_date()` | warning в `warnings`, не молчание |
| шапка события = уровень стакана в нём | разъехавшиеся пути расчёта в одном сообщении | равно |
| событие ленты = свежий `exact_y_idx` по его цене | протухший контекст/кривая | Δ ≤ 5 bps; один выбивающийся ISIN = данные, все = модель |
| ACCRUEDINT ISS = НКД из графика на SETTLEDATE | несогласованность источника | 107/116 бумаг (расходятся валютные BYM) |
| `no-undef` по `frontend-react/src` | ReferenceError, который `vite build` не ловит | 0 |

Линт фронта разово: `npx eslint@8 --no-eslintrc --env browser,es2022
--parser-options=ecmaVersion:2022,sourceType:module,ecmaFeatures:{jsx:true}
--rule '{"no-undef":"error"}' src --ext .js,.jsx`

### Бэктест спек: прибор врал (2026-08-21, поздний вечер)

`scripts/verify_fixing_specs.py` собирал спеку бэктеста БЕЗ флага `compounded`,
поэтому индексные RUONIA-купоны (ВЭБ.РФ, Роснефть, ОФЗ 29026+ — ставка это
отношение накопленных индексов) мерил простым средним и рисовал WARN 0.28–0.39
пп там, где прод считает верно. Отчёт звал чинить работающее — ровно так чуть
не был «починен» несуществующий баг daily-comp из TODO.

Фикс `f7a708c`: **OK 552→563, WARN 16→5**. Медиана ошибки по 99 RUONIA-бумагам
0.013 пп — тема daily-comp закрыта. Осталось 12 настоящих расхождений:
ПионЛизБР5/БО6 (RUONIA, 1.6–1.9 пп — спека не подобрана вовсе) и 10 старых
KEYRATE-выпусков 0.15–0.75 пп.

**Правило:** прежде чем чинить по отчёту инструмента — проверь, что инструмент
считает тем же путём, что прод. Сравнение спеки прода (`ref_data.coupon_formula`)
с той, что строит проверяльщик, стоит одну команду.

### Пересчёт истории (сделан точечно)

Прошлые дни считает as-of движок (`backdate`, `accrued_basis="calc"`) — баг №9
его НЕ трогал. Загрязнены были только бары/снимки, записанные ЖИВОЙ моделью в
свой день. Замер: короткая бумага (2,5 мес) — 47 из 190 баров расходятся ≥20 bps
с честным as-of, макс 173; средняя — 0, макс 9. Поэтому бамп
`HONEST_ENGINE_VERSION` не нужен (580 из 600 бумаг дали бы те же числа), хватило
`scripts/recalc_bonds.py` по 69 флоатерам с погашением < 6 мес: снесено 20 606
honest-строк, снят штамп с 216 363 баров, пересчитано 20 913 строк, сбоев 2
(бумаги, которых уже нет на MOEX). Память в пике 494 МиБ из 1,17 ГиБ.

### Каветаты

- `docker compose exec` — ОТДЕЛЬНЫЙ процесс со своими пустыми кэшами: он
  показывает «как надо», а не то, что видит живой воркер. Расхождение между ними
  и есть улика.
- Прод на **поллинге** (`TG_POLLING=1`), вебхук до VPS не достучится. `setMyCommands`
  после добавления команды звать отдельно — `tg_set_webhook.py` при поллинге
  выходит раньше.
- Тики бумаг ВНЕ флоатер-юниверса поток режет порогом `TRADES_STREAM_MIN_RUB`
  = 1 млн: блок-фильтр с порогом ниже по такой бумаге всё равно ждёт ISS.

## Изменения на проде 2026-08-21 (аудит расчётов: 8 багов)

Разбор начался с «фейкового спреда» в ленте сигналов и развернулся в системный
поиск инвариантами. Все правки задеплоены, история пересчитана.

### Найденное и исправленное

| # | коммит | баг | эффект |
|---|---|---|---|
| 1 | `f633d11` | база Y-IDX замерзала на ставке первого дня: путь роллирования читал границы ступеней из `curve.nodes`, а у гибрида as-of узлов два при дневной ступени факта | до 253 bps, вся история |
| 2 | `bce64a6` | прошедший отрезок базы реконструировался по ставкам вместо накопленного индекса ЦБ | 0–0.5 bps, но убран класс ошибки |
| 3 | `765b6eb` | скринер экстраполировал Y-IDX наклоном от протухшего якоря (котировки и снимок глубины из разных моментов) | 378 вместо 5 bps |
| 4 | — | тот же фейк с чёрного хода: при наборе в один уровень подставлялся `top_val` | 166 вместо 28 bps |
| 5 | `290683e` | база считалась до заявленного погашения, хотя поток обрезан офертой (`cut_at_offer`) | 19 бумаг, спред занижен вдвое |
| 6 | — | горизонт ЗА концом потока перехватывал витрину по правилу цены | разворот монотонности |
| 7 | — | горизонт выбирался по ЦЕНЕ выкупа, а не по доходности | 11 бумаг, до 75 bps |
| 8 | — | спека фиксинга ОФЗ 29008/29009/29010 врала на 2.4–2.9 пп в ставке купона | ликвидные бумаги |
| — | `d6a1390` | крон бэкапов уходил в 01:30 МСК вместо 04:30 (сервер в МСК, не UTC) | до ночных прунов |
| — | — | чек Σ PV дисконтировал от даты расчёта, а XIRR решается от даты поставки | 514 ложных тревог |
| — | — | бэкфилл часов не пересобирал дни за собой | день с 2 часами из 11 |

**Общий корень у 5–7:** «заявленная дата» вместо фактического конца потока и
прокси (цена) вместо прямого критерия (доходность).

### Инварианты — чем ловить регрессии

Проверки, не требующие знать «правильный ответ». Гонять по универсу после любой
правки расчёта:

| инвариант | что ловит | норма |
|---|---|---|
| Y-IDX(цена) убывает гладко | сорванный солвер, скачок горизонта, кэп | разворотов 0 |
| витрина = карточка = стакан при одной цене | разъехавшиеся копии логики | ±5 bps |
| горизонт по доходности | неверный выбор put/call | 0 неверных |
| Σ PV = dirty на решённом XIRR | display-поток ≠ pricing-поток | ±0.1% |
| полнота графика купонов до погашения | обрезанное расписание | 601/601 |
| история (as-of) = живой движок на одной дате | расхождение движков | медиана 0–2 bps |
| день = агрегат своих часов | несобранная свёртка | только текущий день |
| бэктест спеки по прошлым купонам | враньё в проекции купонов | < 0.15 пп |

Состояние на 21.08.2026 после правок: **528 бумаг, разворотов 0, неверных
горизонтов 0, расхождений путей 0** (плавающие ±5 bps — артефакт длинного
прогона, не логика).

### Известное ограничение методики

На границе переключения горизонта Y-IDX даёт ступеньку ~20 bps: правило
выбирает горизонт по равенству ДОХОДНОСТЕЙ, а Y-IDX = YTM − база, и базы у
горизонтов считаются на разные сроки. Непрерывность гарантирована для YTM, но
не для спреда. Против прежних тысяч bps (когда решала цена) — приемлемо.

### Где живёт расчёт спреда (единственные точки)

```
core/valuation.py            математика: поток, XIRR, солверы, роллирование базы
services/valuation.py        calculate_valuation_metrics — ЕДИНСТВЕННОЕ ядро
  ├ pick_horizon             метрики выбранного горизонта
  ├ horizon_pair             (выбранный, второй, ключ) — для витрины/графика
  └ horizon_value            одно поле с откатом на верхнеуровневое
```

Потребители зовут ядро и извлекают через эти три функции: `services/universe.py`
(витрина, WS), `services/bond_details.py` (карточка, стакан, скринер),
`services/backdate.py` (история), `services/bars.py` (бары),
`services/screener_core.py` (сигналы). Своей формулы спреда нет ни у кого —
21.08.2026 устранены пять копий извлечения горизонта, из них одна (витрина)
обходила `pick_horizon` вручную.

### Автоматика: петля «нашли → починили → пересчитали»

Ежедневный синк (`services/instruments_sync.py`):

* шаг 5 — бэк-аут маржи из последнего купона против факта индекса (порог 1.5 пп);
* шаг 8 — бэктест спеки, 24 бумаги за проход, **вперёд идут бумаги с офертой за
  последние 45 дней** (там ставка меняется предсказуемо);
* шаг 8b — `spec_autofit.autofit(apply=True)`: подбор лага и окна с
  подтверждением на отложенных купонах, 12 бумаг за ночь. Что не лечится лагом —
  остаётся помеченным, а систематический сдвиг во всех купонах логируется как
  «похоже смещена МАРЖА, столько-то bps»;
* сразу за автофитом — пересчёт истории применённых бумаг (`drop_honest` +
  `ensure_honest_backfill`): версия движка та же, а числа другие, поэтому
  `drop_stale_honest` такие строки не заметит.

**Скорость доезда правки параметров:** витрина/WS — 5 секунд (событийный такт),
карточка/стакан — следующий запрос, скринер — следующий тик. История НЕ
пересчитывается сама: `scripts/recalc_bonds.py --isins ...` или `--with-offers`.

### Миграции данных, выполненные 21.08

* полный пересчёт честной истории: 601 бумага, 276 002 строки, 78 мин;
* бары за год окнами по 20 бумаг (см. грабли ниже), закончено 07:22;
* пересчёт 161 бумаги с офертами после фиксов базы и горизонта: 37 438 строк;
* ОФЗ 29008/29009/29010 — спека и история;
* догон второго года архива баров — крон `/etc/cron.d/floaters-bars730` на 22:00.

## Версии механик спреда (кто что штампует)

Спред одной бумаги хранится в ТРЁХ местах, и каждое пересчитывается по-своему.
Правка, меняющая цифру, обязана поднять версию — иначе в базе смешиваются числа
двух движков, и медианы бакетов/эмитентов считаются по несопоставимым строкам.

| слой | таблица | версия | кто пишет | как чинится после бампа |
|---|---|---|---|---|
| точка на день | `spread_daily`, `src='honest'` | `HONEST_ENGINE_VERSION` (`services/backdate.py`) | as-of движок на кривую того дня | `drop_stale_honest` сносит старое при открытии графика; массово — `scripts/recalc_honest_history.py` |
| точка на день | `spread_daily`, `src='snap'` | — | вечерний снимок ЖИВЫМ движком в свой день | не версионируется: правки as-of его не касаются |
| точка на день | `spread_daily`, `src IS NULL` | — | легаси candle-est (`backfill_yidx_history.py`) | недоверенный, пересчитывается на своей цене |
| внутри дня | `bar_hourly` / `bar_daily` | `BARS_METRICS_VERSION` (`services/bars.py`) | as-of на прошлые дни, живая модель на сегодня | зануляет спред и считает фоном при первом запросе; массово — `scripts/backfill_hourly_bars.py` |

### HONEST_ENGINE_VERSION — история

| v | дата | что было сломано | эффект |
|---|---|---|---|
| 1 | до 2026-08-03 | базовая версия | — |
| 2 | 2026-08-03 | НКД не доначислялся до нерабочей даты; ОФЗ не резолвились в SECID/TQOB (шли без фактических купонов); тихий фолбэк на сегодняшний НКД; день без сделок терял цену | ОФЗ считались мимо, дни без сделок выпадали |
| 3 | 2026-08-04 | КС-бумаги сравнивались с роллированием КС, RUONIA-бумаги — с RUONIA: два базиса, Y-IDX двух бумаг несопоставим | база стала единой (роллирование RUONIA для всех) |
| 4 | 2026-08-11 | якорь realized-гибрида — сегодняшняя кривая вместо первой архивной | скачок на границе архива котировок, ТрансмхПБ8 −50 bps за день при флэт цене |
| 5 | 2026-08-12 | НКД в день выплаты купона: на нерабочей дате оставался почти полный купон старого периода, на рабочей не доначислялся на поставку | dirty завышен на купон → спред улетал в минус на сотни bps |
| 6 | 2026-08-13 | спред считался всегда к погашению, а шапка/стакан — к горизонту по правилу цены | у бумаг с офертой две разные метрики на одном экране |
| 7 | 2026-08-14 | эхо неопределённых купонов MOEX: хвост заполнялся последней объявленной ставкой (29010: 16.59% до 2034) | Y-IDX −5…−212 bps, SM −5…−176 bps |
| **8** | **2026-08-20** | **путь роллирования RUONIA читал границы ступеней из `curve.nodes`, а у гибрида as-of узлов два (eff, splice) при ДНЕВНОЙ ступени факта — уровень первого дня замерзал до splice** | **база = спот-индекс, скомпаундированный до погашения. МБЭС 2P-02 @2025-08-20: 18.01% вместо 16.10%, Y-IDX 48 вместо 239 при марже 250** |

### BARS_METRICS_VERSION — история

| v | дата | что было сломано | эффект |
|---|---|---|---|
| NULL | до 2026-08-11 | candle-est: цена дня × модель дня записи | оценка, не as-of |
| 1 | 2026-08-11 | — | прошлые дни переведены на честный as-of |
| 2 | 2026-08-11 | см. HONEST=4 (якорь гибрида) | скачок на границе архива убран |
| 3 | 2026-08-12 | см. HONEST=5 (НКД в день выплаты), бары выходной сессии | 495 бумаг / 3152 бара в окне 95 дней |
| 4 | 2026-08-13 | цена сегодняшнего бара через FACEVALUE, которого у ISS за сегодня нет | RU000A108C58: 89.44 при сделках 100.46–101.14 |
| 5 | 2026-08-13 | спред баров всегда к погашению | РЖД 1Р-52R: put 2029 против погашения 2036 |
| 6 | 2026-08-13 | — | добавлен спред ко ВТОРОМУ горизонту (свитчер без пересчёта) |
| 7 | 2026-08-14 | см. HONEST=7 (эхо купонов) | сдвиг до 200 bps |
| **8** | **2026-08-20** | **см. HONEST=8** | **МБЭС 2P-02 год назад 48 bps вместо 239** |

### Грабли долгих миграций на проде (стоили заходов 20–21.08)

* `pgrep` в образе НЕТ — ждать процесс контейнера нужно ХОСТОВЫМ pgrep;
* `docker compose exec -T` умирает вместе с каналом и возвращает 0 за убитым
  процессом — для фона только `exec -d`;
* деплой пересоздаёт контейнер и убивает всё, что внутри: долгий пересчёт и
  редеплой не совмещать;
* память скрипта растёт с числом бумаг В ПРОЦЕССЕ (год часовых баров + as-of
  контекст на каждый день): весь юниверс = OOM на ~145-й бумаге при mem_limit
  1200m. Дробить `--limit` БЕСПОЛЕЗНО (режет хвост, тянет предыдущие) — только
  `--offset`, окно 20 бумаг, concurrency 1.

### Как понять, что цифра поехала из-за версии
`spread_daily.engine_ver` и `bar_hourly.metrics_ver` хранят версию строки. Ступенька
на графике ровно на дате выката = смешаны версии; строки старой должны быть снесены,
а не досчитаны поверх.

## Изменения на проде 2026-08-20 (Y-IDX на прошлую дату)

**Симптом.** МБЭС 2Р2 на историческом графике: при цене около номинала спред < 100 bps,
хотя маржа выпуска +250 bps над КС.

**Разбор.** Сегодняшняя цифра оказалась верной: бумага короткая (115 дней до погашения),
и маржа съедается механикой — премия цены 100.43 даёт −136 bps, НКД 30.71 ₽ платится
авансом и процентов не приносит (−43 bps), маржа начисляется на 1000 при вложенных 1035
(−8 bps). Итог 63 bps против расчётных 65. Цена, при которой SM = 250, — 99.84.

**Баг нашёлся в истории.** Год назад тот же расчёт давал 48 bps вместо 239 — см.
HONEST_ENGINE_VERSION=8 выше. Занижение тем больше, чем короче бумага и чем дальше дата:

```
дата         срок 1г / 3г / 5г
2025-09-01     253 /  82 /  49 bps
2025-12-01     101 /  33 /  20
2026-03-02      41 /  14 /   8
2026-06-01+      0 /   0 /   0     ← ближе к архиву котировок расхождение исчезает
```

Live-прайсинг и market-даты (архив своп-котировок с 2026-07-30) НЕ затронуты: у
bootstrap-кривой узлы настоящие, ступень с ними совпадает.

**Коммиты.**
- `f633d11` fix(y-idx) — `DiscountCurve.rate_bounds()`: кривая сама объявляет свои
  ступени, гибрид отдаёт дневной факт + ступени anchor. Поиск границы через bisect.
- `bce64a6` refactor(y-idx) — прошедший отрезок базы берётся ОТНОШЕНИЕМ УРОВНЕЙ
  официального накопленного индекса RUONIA (`cbr.ruonia_index_history`, с 2010, каждый
  календарный день) вместо реконструкции по ставкам. Контракт `realized_until()` /
  `realized_growth()` на кривой; хвост за splice по-прежнему реконструируется.
  Сходимость с прежней реконструкцией 0.0–0.5 bps — правка не про точность, а про то,
  что в делении ошибиться негде: ровно в реконструкции сидел баг v8.
- `22707f3` chore(scripts) — `scripts/recalc_honest_history.py`, массовый пересчёт.

**Миграция данных (три шага, порядок обязателен) — ВЫПОЛНЕНА в ночь на 2026-08-21.**
1. `scripts/recalc_honest_history.py --days 400` — 601 флоатер, 276002 строки, 78 мин,
   3 сбоя (свежие выпуски без истории НКД: движок не достраивает НКД догадкой).
2. `scripts/backfill_hourly_bars.py --days 365 --no-ticks` — только ПОСЛЕ шага 1:
   вдвоём они дерутся за MOEX и за `portfolio.db`. Прошёл окнами по 20 бумаг
   (`/root/floaters/bars_chunks.sh`), закончил 07:22, все окна код 0.
3. Ночной прогрев 03:00 МСК (`nightly_spread_warm`, топ-200 × 150 дн) — отработал сам.

**Покрытие после миграции** (в окне года — том, что рисуют графики):

```
spread_daily  v8: 143512 строк · 512 бумаг    остаток старых: 4 бумаги
bar_hourly    v8: 1919465 баров · 1505 бумаг  остаток старых: 13 бумаг
bar_daily     v8: 62953 дня · 1336 бумаг
```

Остаток — бумаги, где спред не считается вовсе (DM не брекетируется, тонкая история):
движок такие строки штатно не штампует, подберёт при следующем заходе.

**ХВОСТ: второй год архива.** `ChartPage.jsx` LAYER_MAX_DAYS=730, а пересчитан год —
на окне «ВСЁ» линия даёт ступеньку на 2025-08-21 (слева старая механика). Догон
запланирован кроном на 21.08 22:00 МСК: `/etc/cron.d/floaters-bars730` →
`/root/floaters/bars730.sh` (`--days 730`, окна по 20, flock, снимает свой cron
после прохода). Логи `data/logs/bars730.log` / `bars730_run.log`.

**Грабли долгих миграций на проде** (все трое стоили заходов, записаны в bars_chunks.sh):
* `pgrep` в образе НЕТ — ждать процесс контейнера нужно хостовым pgrep;
* `docker compose exec -T` умирает вместе с каналом и возвращает 0 за убитым
  процессом — для фона только `exec -d`;
* память растёт с числом бумаг В ПРОЦЕССЕ (год часовых баров + as-of контекст на
  каждый день, ликвидные бумаги дороже на порядок): весь юниверс = OOM на ~145-й
  бумаге при mem_limit 1200m. Дробить `--limit` БЕСПОЛЕЗНО — он режет хвост, и
  пачка «до 200» тянет предыдущие 150. Дробить только `--offset` (добавлен
  2026-08-21), окно 20 бумаг, concurrency 1 → пик ~300M поверх ~260M uvicorn.

## Резервные копии (2026-08-20)

Тиковый архив и история спредов живут ТОЛЬКО в `data/portfolio.db` (2,5 ГБ).
У Alor глубина сделок 30 дней — всё, что глубже, восстановить неоткуда.

**Как снимается:** `scripts/backup_db.py` — `sqlite3.backup()` порциями
(консистентно на живой базе, не запирает писателей), проверка копии чтением
контрольных таблиц, gzip, ротация «2 свежих + 2 воскресных». Копируются
`portfolio.db`, `instruments.db` и `users.json`. Замер на проде: 2,5 ГБ → 537 МБ
за 91 с. Лежит в `data/backups/` (том переживает редеплой), лог —
`data/logs/backup.log`.

**Расписание:** `/etc/cron.d/floaters-backup`, 04:30 МСК (после ночного
прун/VACUUM/ANALYZE в 03:30). Крон-файл ставится КАЖДЫМ деплоем — правится в
`scripts/deploy.sh`, не руками на сервере.

**Копия к себе:** `./scripts/pull_backup.sh [каталог]` (по умолчанию `./backups`,
gitignored). Раз в неделю — обязательно: копия на сервере спасает от кривой
миграции, но не от смерти VPS.

**Восстановление** (проверено 2026-08-20 на полной копии — `quick_check: ok`,
5,9 млн тиков, 3,9 млн баров):
```bash
gunzip -c backups/portfolio-YYYYMMDD-HHMM.db.gz > portfolio.db
sqlite3 portfolio.db "PRAGMA quick_check(3)"      # ~48с на 2,4 ГБ
# на прод: остановить контейнер, положить файл в data/, поднять
```

## Архитектура
```
api/           FastAPI: main.py, schemas.py, routes/{health,meta,bonds,curves,orderbook,ws}
services/      bonds, cashflow, valuation, market_data, nrd, exceptions
root .py       auth(Alor token), cashflow, forwards(кривые), valuation(DM/XIRR), rates(Cbonds), last_prices(Alor WS)
frontend-react/ Vite+React+framer-motion: src/{App,api,format,main}.jsx + components/ + styles.css
tests/         только test_nrd.py (pytest)
```

## Источники данных (контракты)
- **НРД Ценовой центр** (NSDDATA API v2, base `https://nsddata.ru`). Auth: POST `/api/auth/login` {login, password=apikey} → {access_token, refresh_token}; refresh на 401; Bearer. Данные: GET `/api/get/valuationnewadd?filter=<json>&limit&skip` (доп.параметры). **Подписка даёт ТОЛЬКО valuationnewadd** — `valuationnew` (fair value) = 403. `filter` = JSON Mongo-стиль `{"isin":{"$in":[...]}}`. Спека: `https://nsddata.ru/ru/products/api/scheme`. Креды в `.env`: `NRD_LOGIN`, `NRD_APIKEY`.
  - **Масштабы полей (доли/проценты!)**: yields ×100→%; z_spread/g_spread/nominal_margin ×10000 (доли→bps); discount_margin/simple_margin ×100 (проценты→bps). `base_coupon_index`: CBRATED→KEYRATE, RUONIARATED→RUONIA. `nominal_margin`=спред выпуска (совпал 16/16).
  - Юниверс: пагинация valuationnewadd (1000/стр), фильтр `coupon_type=float` + база, **дедуп по isin (max val_date — НРД отдаёт дубли!)**, кэш `nrd_universe_cache.json` на день. ~453 рублёвых флоатера.
- **MOEX ISS**: цены prev/accrued (`fetch_moex_snapshot`, борд TQCB), справочник (`fetch_moex_securities`), расписание купонов с реальными суммами (`fetch_bond_schedule_full` — bondization value/valueprc), короткие имена выпусков (`fetch_moex_shortnames`, один запрос всех бондов), поиск (`/api/bonds/search`).
- **Alor**: live-цены по WebSocket (`last_prices.py`), refresh-токен в `.env` `ALOR_REFRESH_TOKEN`.
- **Cbonds**: кривые ставок RUONIA OIS / KEYRATE IRS (`rates.py`, парсинг HTML).

## Расчёты
- Наш `dm_bps` = `solve_dm_bps` (valuation.py) — правильный discount margin (PV cashflow при forward+DM = dirty price), бисекция. Та же метрика что НРД discount_margin.
- **DM-cashflow починен (2026-07-08)**: `build_cashflows_to_maturity` принимает тройки (start,end,value) — зафиксированный купон берётся ФАКТОМ MOEX (не перепрогнозируется форвардом, как в z-движке), + параметр `amorts` — принципал по графику амортизаций, прогнозные купоны от остаточного номинала. `services/valuation.calculate_valuation_metrics` больше НЕ срезает value; `amorts=` прокинут из bonds.py (universe/detail, из уже загруженного schedule_full). Эффект: KEYRATE +2…+48bps (факт купона), амортизируемые до ±56bps. Гипотеза «рассинхрон day-count сетки начисления/дисконта» ПРОВЕРЕНА И ОТВЕРГНУТА (купон платится за полный период, покупатель компенсирует НКД; par-смоук 152≈150bps ✓).
- **Ex-coupon T+1 (2026-07-09)**: `valuation.settle_date` — купон с pay_date <= T+1 (скип выходных) покупателю НЕ достаётся (MOEX НКД уже 0 под эту конвенцию); исключён из cashflow во всех трёх движках (DM `build_cashflows_to_maturity`, z `project_cfs`, фикс `build_fixed_cashflows`). До фикса накануне каждой купонной выплаты PV завышался на целый купон → DM/z +сотни bps мусора (пойман на RU000A106JX4: 731 → 237 vs НРД 200).
- Cashflow (`services/cashflow.build_cashflow_from_moex`): прошлые купоны = факт MOEX (value/valueprc), будущие = прогноз (forward+spread), погашение из амортизаций/номинал. dirty = clean + live accrued (MOEX, не стейл-кэш).
- Кривые: bootstrap из Cbonds (`forwards.py`), KEYRATE quarterly comp, RUONIA daily, ACT/365.
- **Расхождение наш DM vs НРД — РАЗОБРАНО ПО МЕТОДИКАМ (2026-07-01).** Документы НРД скачаны в `docs/nrd/`: `met_float_10112025.pdf` (флоатеры) + `met_rub_21_05_2026.pdf` (базовая). Изоляц. стенды: `scripts/reconcile_dm.py`, `scripts/ks_spline_probe.py` (подают цену НРД → расхождение = чистая математика).
  - **Конвенция дисконта НРД (базовая, п.4.9): `P=Σ CF_i·exp(−(r(τ_i)+z)·τ_i)−AI` — НЕПРЕРЫВНОЕ компаундирование.** Наш daily `(1+r/365)^days` ≈ непрерывному → конвенция уже почти верна (годовое `(1+r)^−t` — НЕВЕРНО, переоценивает DM +120bps, проверено).
  - **`r(τ)` — с кривой КБД ОФЗ (G-curve) МосБиржи**, `z` — z-спред эмитента по кривой **Нельсона-Сигеля-Свенсона**, калиброванной **фильтром Калмана** (Прил.6). То есть НРД дисконтирует по БЕЗРИСКОВОЙ ОФЗ + КРЕДИТНЫЙ z-спред, а своп-кривые КС(кубич.сплайн Прил.3)/RUONIA(Smith-Wilson UFR 7.14% Прил.2) — только для ПРОГНОЗА купонов.
  - **Эмпирика:** near-par качественные флоатеры сходятся ±10bps уже сейчас; бумаги с большим НРД DM (дисконт/ниже рейтинг) недобираем на 100–250bps — ровно там, где играет кредитный z-спред над ОФЗ. `discount_margin` НРД — **производный аналитик, точная формула НЕ опубликована** (методики задают справедливую цену и z-спред).
  - **РЕШЕНИЕ (пивот):** НРД `discount_margin` — авторитетный (есть из API, колонка «NRD DM»). Наш DM — intraday model-оценка для live-цены watchlist, помечен «DM MODEL». Полный матч <10bps = воспроизвести весь NSS/Kalman-движок НРД (G-curve+z-спред) — отклонён (недели, новые данные, аналитик проприетарен). BUG-1 (форвард прошлого стаба) исправлен клэмпом анкера к calc_date.

## 📐 ДВЕ МЕТРИКИ СПРЕДА: SM + DM (2026-07-09)
НРД публикует 5 спред-метрик флоатера: `nominal_margin` (спред выпуска), `simple_margin`, `discount_margin`, `z_spread`, `g_spread`. Мы считаем **две** и привязываем каждую к своему полю НРД:
- **`sm_bps` (simple margin)** = наш старый DM-солвер (`solve_dm_bps`, дисконт по форвард-кривой+спред). Воспроизводит НРД **`simple_margin`** (ликвид near-par med 0-2, off-par med +19, m\|Δ\|49). Поле `dm_bps` СОХРАНЕНО = `sm_bps` (обратная совместимость).
- **`disc_margin_bps` (discount margin)** = НОВЫЙ настоящий FRN DM (`solve_discount_margin_bps` + `FlatForwardCurve`): индекс держим ПЛОСКИМ на текущем уровне (`current_index_pct` из зафикс. купона), money-market дисконт `Π 1/(1+(L+DM)·τ)`. Правильно дисконтирует pull-to-par → DM выше simple на дисконте, ниже на премии (как НРД). Воспроизводит НРД **`discount_margin`** (off-par ликвид med −20, m\|Δ\|46). Остаток — их проприетарная fair-value машина (NSS/Kalman), несводимо.
- Стенд калибровки: `scripts/dm_calibrate.py` (перебор конвенций: flat/forward проекция × simple/comp/cont дисконт; FRN-simple-flat выиграл).
- **Прокинуто**: `sm_bps`/`disc_margin_bps` в `calculate_valuation_metrics`, `BondValuation` (detail), `BondListItem` + `_uni_item` + watchlist enrich (список). НРД `simple_margin_bps` уже в юниверсе (v4). UI: колонка «наш SM» ↔ НРД simple, «наш DM» ↔ НРД discount.

## 🎯 КОРНЕВАЯ ПРИЧИНА DM-РАСХОЖДЕНИЯ — НАЙДЕНА (2026-07-09)
**Мы сверялись НЕ с тем полем НРД.** Наш `dm_bps` (простой спред над проекцией при PV=цена) = НРД **`simple_margin`**, а НЕ `discount_margin`. Стенд `scripts/calc_isolation.py` (одна цена+дата+реальный НКД, независимый фильтр ликвидности по `trade_volume_rub`):

| наш DM против | KEYRATE | RUONIA |
|---|---|---|
| НРД `discount_margin` | med −14, m\|Δ\|62, std 96 | med −28, m\|Δ\|148, std 277 |
| НРД **`simple_margin`** | **med +2, m\|Δ\|36** | **med 0, m\|Δ\|12, std 20** |

RUONIA против simple_margin — **почти идеально (med 0, std 20bps)**. `discount_margin` НРД — их проприетарная fair-value метрика (NSS+Kalman кредит-слой, corr остатка −0.81 с уровнем dm, без закрытой формулы) И **само поле шумное/битое** на части бумаг: RU000A107456 dm=1175 но их же simple_margin=215, ytm=17.5% (наш DM 160); RU000A0JXK99 dm=1389 vs simple_margin=262. **Внедрено**: `simple_margin_bps` прокинут в юниверс (nrd.py CACHE_VERSION→4) + BondListItem + get_bonds; detail уже отдавал (BondNrd). UI-якорь сравнения = simple_margin, discount_margin оставить отдельной НРД-колонкой.

**Остаток после правильного таргета:** хвосты KEYRATE — неликвид (бо́льшая часть флоатеров <1млн₽/день оборота → wa_price ненадёжна, НРД метки модельные) + оферты. Ликвидное ядро сходится. НКД-баг стенда (RUONIA current-купон value=None → accrued=0) исправлен: реальный MOEX НКД → «RUONIA +39» исчез. **Прод НКД корректен** (accrued_override из MOEX snapshot во всех путях).

## 🔬 ГЛУБИННАЯ ПРИЧИНА РАСХОЖДЕНИЯ DM — ДОКОПАНО (2026-07-08, дополнено выше 2026-07-09)
Стенды: `scripts/hypothesis_matrix.py` (матрица гипотез, 37 бумаг, свежие сырые строки valuationnewadd), `scripts/sensitivity.py` (что двигает DM), `scripts/reconcile_after.py` (before/after фиксов). Итог — 4 слоя по величине:
1. **Базис данных (главный, излечим).** (а) Дневной юниверс-кэш держал вчерашние wa_price/dm — на свежих данных систематика KEYRATE −25 → **−8 mean / −14 median** → TTL кэша 4ч (внедрено). (б) У неликвида НРД dm посчитан от их fair value (`valuationnew`, у нас 403), не от wa_price — выбросы до ±1000bps несводимы без подписки.
2. **Оферты.** MOEX: `OFFERDATE` пуст, но **`BUYBACKDATE` заполнен** и **bondization отдаёт блок `offers`** (offerdate/offertype/price — бесплатно в том же запросе, внедрено в fetch_bond_schedule_full v2). ~17% юниверса с офертой до погашения. **Клэмп горизонта к оферте ПРОВЕРЕН И ОТВЕРГНУТ**: to-maturity ближе к НРД dm на ВСЕХ горизонтах (4дн…5.7лет) — НРД считает dm к погашению. Оферта = информационный флаг: `offer_date/offer_type` в detail reference + warning при оферте ≤180дн (цена может прайситься к оферте).
3. **Ядро остатка — CF-путь НРД ≠ наш (несводимо).** Разложение per-bond: наш flat-yield при ТОЙ ЖЕ цене на **−110bps mean** ниже их ytm → их купонный прогноз (NSS+Kalman имплайд-путь) выше наших IRS-форвардов на 1-2%. В dm почти сокращается (curve-relativity), остаток corr −0.81 с уровнем dm = кредитная кросс-секция их машины. **Закрытой формулы dm НЕТ** (b=ytm−dm не ложится ни на одну кривую ±110bps std — в отличие от z, где ytm−G(0.25) ±4-13bps).
4. **Конвенции — исключены измерением**: непрерывный/годовой дисконт ±6-10bps, simple-купоны KEYRATE +4bps, cd=val_date — шум, кривая parallel ±100bp → ΔDM≈0 (DM curve-relative).
- **Sensitivity (что реально двигает DM)**: цена — доминанта (ΔDM≈100/spread_dur bps на пункт; короткие/амортизируемые — до 230bps/пт), НКД сопоставим (короткая бумага: +5₽ → −379bps!), спред выпуска ~1:1, уровень кривой ~0. Для watchlist критичны свежие цена+НКД одного источника; гнаться за <10bps к НРД бессмысленно.
- **RUONIA median −70bps** — подозрение на конвенцию котировки базы (effective vs nominal daily-comp), потолок ~50-70bps, не дожато (низкий приоритет).

## Текущее состояние UI (готово)
- **Дизайн**: Realtime/swiss-минимализм (Juri Zaech vibe), монохром, Martian Mono шрифт. Темы light (бумага) / dark (LED-табло), persist localStorage. WCAG AA контраст пройден.
- **Список = весь рынок** (юниверс НРД ~453). Live-цена Alor + наш DM только для watchlist (WS-подписка на них). Остальным — НРД-аналитика (VWAP цена, discount_margin, z-spread, duration, спред, база, рейтинг, погашение).
- **Watchlist**: ★-колонка (sticky) toggle, persist localStorage `watch`, обогащается live+наш DM.
- **Фильтры**: 3 независимые группы флажков (watchlist ★ / база КС·RUONIA / рейтинг AAA·AA·A·BBB·BB↓·NR) — AND между группами, OR внутри. Рейтинг нормализован в бакет (`rating_bucket`).
- **Поиск**: клиентский по юниверсу, результаты строками в таблице.
- **KPI**: пересчитываются по отфильтрованному набору (AVG DM = наш ?? НРД discount_margin).
- **Drawer**: карточка любой бумаги (не только кэш) — референс (MOEX+НРД), cashflow-график (факт+прогноз, линия «сегодня»), НРД-блок, наш DM.
- **Колонка RATING**, footer с источниками-статусами (● ALOR/CBONDS/NRD).

## 💼 МОДУЛЬ «ФОНДЫ» (2026-07-02, Ф1 готов и закоммичен)
Продукт стал мульти-модульным: переключатель «Флоатеры | Фонды» в Topbar (persist `module`). 3 хедж-фонда: **R5** (RUB: корп флоатеры 0.9× + длинные ОФЗ 2.2× капитала), **D5** (USD: + замещающие), **Y5** (CNY: + юаневые). Плечо = явные РЕПО-сделки; NAV = MV − РЕПО; net carry = купоны − funding.

**Бэкенд** (SQLite `data/portfolio.db`, том в проде): `services/portfolio_db.py` (funds/positions/repo_deals/nav_daily), `services/instruments.py` (классификатор 7 классов; ISIN→SECID через q-поиск ISS — board-less endpoint ОФЗ не резолвит; борд-строка по CURRENCYID==FACEUNIT — иначе у замещающих рублёвый НКД с TQCB), `services/fixed_income.py` (YTM/dur/DV01/G-spread к КБД; yield-to-put), `services/fx.py` (**TOM с MOEX** LAST→WAPRICE→PREV TTL 60с + 15-мин stale-буфер + ЦБ-фолбэк), `services/portfolio.py` (агрегация; месячный кэшфлоу-календарь из bondization, флоатеры оценкой по current coupon; `snapshot_all_navs`). Фоновый `fund_nav_snapshotter` в main.py (раз в час, идемпотентно за день). API `api/routes/funds.py`: CRUD/snapshot CSV/repo/summary/cashflow/nav_history.

**Фронт** `frontend-react/src/components/funds/`: карточки фондов, деталка (KPI-строка, классы с фильтром, таблица позиций WGT/warn-бейджи, РЕПО-секция, инлайн-редактор капитала — ввод в ₽), **тумблер валюты ₽|$|¥** (пересчёт всех метрик по TOM, подпись источника), 12-мес кэшфлоу-чарт (купоны/прогноз/принципал), экспорт позиций CSV (Excel-RU: BOM+`;`+запятая), форма «В фонд» в Drawer скринера (заменяет qty, не суммирует).

**Валидировано**: ОФЗ 26238 G-spread −2бп; ГазКЗ-37Д YTM 8.0% USD, НКД в USD; РЖД 1Р-26R yield-to-put; микс-РЕПО WA по ₽-эквиваленту; cross-fund изоляция удаления.

**Ф2–Ф4 (2026-07-02, тот же день):**
- **Фикс-движок добит**: дюрация Маколея, выпуклость (numeric bump ±10бп). Yield-to-worst: ISS OFFERDATE пуст даже у бумаг с офертой → наш детект «первый купон без value = оферта» и есть YTW-консистентная оценка (пост-офертный купон не определён).
- **Risk-split** в summary: рублёвый DV01 фиксов vs MV флоатеров (rate dur≈0, риск в carry) vs валютный DV01; **fx_split**: MV/РЕПО/нетто по валютам. UI: RiskStrip-строка в деталке.
- **Сценарии** (`fund_scenarios`, endpoint `/{code}/scenarios`, UI-таблица): КС ±100/±200 (фиксы −DV01·Δ+½conv·Δy²·MV; флоатеры Δcarry=MV·Δ перефиксинг; РЕПО RUB ходит с КС), steepener/flattener (Δy от дюрации: −mag@≤1г…+mag@≥7л), FX ±10% (чистая валютная экспозиция; carry валютных ∝Δ). Проверено: R5 КС−200 → +1.44 млн MtM (convexity-асимметрия) + carry +0.33 млн (funding-выигрыш > потери купона флоатеров); D5 FX±10% = ±7.7 млн.
- **Календарь** `/_meta/calendar`: купоны/амортизации/погашения/оферты 90дн по всем фондам (поймал реальную оферту ГТЛК 30.07). **Алерты** `/{code}/alerts`: Δz/Δdm D/D из history.py ≥20бп. **Сравнение фондов** таблицей на главном экране. **Бенчмарки** `services/benchmarks.py` + `/_meta/benchmarks`: RGBITR/RUCBTRNS/RUCEU история с MOEX ISS (кэш день), perf 1д/30д/YTD.
- **Фундамент атрибуции**: nav_daily + fx_usd/fx_cny (ALTER-миграции в init_db), pos_daily (дневной позиционный срез с ценами) — полная P&L-атрибуция (carry/rolldown/spread/rate/FX) в Ф5 вместе со сделками.
- Ф5 план: сделки+P&L+атрибуция, паи (цены юзера), графики NAV vs бенчмарк (nav_daily копится), рейтинги НРД, двухвалютный NAV.

## 📊 ФЛОАТЕР-МЕТРИКИ + КРОСС-СЕКЦИЯ (2026-07-02)
Реализован полный пакет метрик, специфичных для флоатеров (адаптация bond-аналитики под плавающий купон: rate duration≈до рефиксинга мала, spread duration≈до погашения — весь кредитный риск).
- **Бэкенд**: `services/metrics.py` (чистые функции: `macaulay_years` spread duration, `days_to_refix`, `current_coupon_pct`, `carry_bps`, `breakeven_base_pct`, `base_level_pct` с короткого конца кривой, `rank_pct` перцентиль в бакете, `years_to`). `services/history.py` (ежедневный снапшот юниверса → `nrd_history.json`, `dod_map("z"/"dm"/"px")` день-к-дню, cap 40 дат — записывается на каждой загрузке юниверса; Δ появляется со 2-го дня).
- **Схемы**: `BondListItem` += spread_dur_yrs, z_pctile, delta_z_dod (по всему рынку) + carry_bps, days_to_refix, current_coupon_pct (watch). Новая модель `FloaterRisk` в detail. `nrd.py` universe items += current_yield_pct/nrd_calc_date, CACHE_VERSION→3 (regen).
- **Кросс-секция** (`_universe_bonds`): перцентиль z внутри рейтинг-бакета, spread duration = срок до погашения (по всем 456, без сети). Watch: carry/refix/current_coupon от MOEX-расписания + кривой.
- **Фронт**: 3 новые колонки (CARRY, z%ile с мини-баром, Δz D/D); Kpis += MEDIAN DM + REPRICED D/D; Drawer += секция «Флоатер-риск» (spread/rate duration, carry, breakeven, mod_dur/convexity/pvbp) + staleness-чипы per-source (fresh/stale по датам); `AnalyticsPanel.jsx` (тумблер 📊 АНАЛИТИКА в Toolbar) — 3 inline-SVG графика: scatter z vs spread duration (цвет=рейтинг), распределение z по бакетам (p25-медиана-p75), профиль рефиксинга watchlist. Валидировано превью: медиана z монотонна по бакетам AAA 325→B 1914 (sanity ✓), 456 строк, консоль чистая, light+dark.

## ✅ ИСПРАВЛЕНО (quick wins, 2026-07-01)
- Блокировка event loop: `get_curves` теперь async (`asyncio.to_thread`), `get_access_token` (Alor) уводится в поток из всех async-хендлеров (bonds/meta/curves/orderbook). Cbonds-fetch и bootstrap больше не вешают сервис.
- Гонки токен-кэша: `auth.py` — `threading.Lock` + double-check + атомарная запись (`os.replace`); `nrd.py` — `asyncio.Lock` (`_ensure_token`) + атомарный `_save_json`.
- НРД масштаб спредов — `_sane_bps` guard (±20000 bps): нетипичные единицы → None + warning.
- Семафор MOEX: `_MOEX_SEM = Semaphore(8)` + `_moex_get` (ретрай на 429/5xx) для snapshot/securities/schedules.
- TTL кривых: `_load_curves_sync` инвалидирует кэш при смене даты.
- Честный `is_stale` в detail: `rates_date < today`.
- Дедуп `fetch_nrd_metrics` в detail (был 2×, стал 1×).
- Фронт: WS reconnect-таймер чистится в `close()` — нет зомби-сокетов.

## 🎯 NRD-STYLE ПАЙПЛАЙН z-СПРЕДА (2026-07-01, стенд `scripts/nrd_pipeline_probe.py`)
Построен пайплайн под методику (met_rub п.4.9): прогноз купонов на кривой ОЖИДАНИЙ индекса (кубич.сплайн из свопов) → дисконт по **КБД ОФЗ непрерывно** `exp(−(r_g(τ)+z)τ)` → solve **z-спред**. Цель: наш z ≈ НРД `z_spread`.
- **КБД ОФЗ (G-curve)**: `iss/engines/stock/zcyc.json?iss.only=yearyields` — готовые OFZ zero-yields (0.25y→13.6%, 3y→14.6%, 10y→16.4%). Восходящая; short-end ≈ текущий КС ~14%.
- **Результат сверки**: RUONIA **±7–70bps (медиана ~40)** — методика воспроизведена ✅. KEYRATE **РЕШЁН 2026-07-02** (см. ниже), было систематически −150…−208.
- **KEYRATE-РАЗГАДКА (2026-07-02, реверс-инжиниринг)**: публикуемый НРД `z_spread` = **`ytm_eff − G(горизонт рефиксинга)`** — производная от их YTM, а НЕ независимый solve п.4.9 по всей кривой. Подтверждено на 68 бумагах nrd_cache: медиана |ytm−G(dur)−z| = 4–13bps. `duration` НРД у флоатера = ДЛИНА КУПОННОГО ПЕРИОДА (месячный → 0.08), G клэмпится к короткому концу КБД (~13.6%). Наш старый −150/−208 = разница между средней G по потокам до погашения (~15%+) и G на коротке. Их ytm — из проприетарной fair-value машины (NSS+Kalman, имплайд-купоны шумные), но наш y из projected-CF воспроизводит его достаточно.
- **Наш прод-аналог (V7)**: y = плоская непрерывная доходность наших projected CF (`solve_flat_y`), z = (e^y−1) − G(τ_reset), τ_reset = длина текущего купонного периода. **Сверка: watch-выборка (12 KEYRATE) mean|Δ|=43, медиана −17; live API mean|Δ|=27; юниверс (93) медиана −13; AAA..A mean|Δ|=86, остаток растёт к низким рейтингам (BB+138/647) — кросс-секция NSS+Kalman эмитента, из публичных данных несводимо. Устойчив во времени (12 дат июня: bias≈0, std 17–32bps = сглаживание Калмана + рыночный шум).** RUONIA-путь не тронут (старый solve по кривой, бит-в-бит).
- **История НРД доступна**: `valuationnewadd` с `filter={"isin": X}` отдаёт дневные строки с ~мая 2026 (фильтр `val_date $gte/$lte` НЕ работает — резать локально). Пригодно для day-over-day аналитики/бэктестов.
- **СПФИ КС-кривая подключена**: `rates.get_spfi_curve()` — 13 GET `cbonds.ru/indexes/{ID}` (98488 3M … 98510 10Y), regex `"actual_value.numeric"`, кэш `spfi_cache.json` на день, фолбэк hardcode 30.06 с warning. Для фикса оказалась НЕ нужна (кривая ниже IRS-effective → купоны ниже), оставлена как источник данных. Метод.СПФИ: `fs.moex.com/f/21666/20250324-ext-spfi-swaprates.pdf`.
- **Проверенные и ОТБРОШЕННЫЕ гипотезы** (матрица в `scripts/nrd_pipeline_probe.py`): spot-на-дату-фиксинга vs форвард (±2bps — кривая плоская), СПФИ-кривая (−322), сплайн с exp-экстраполяцией к ω=7.5% (эффект ~0 — короткие duration), лаг фиксинга 5дн (~0), купонное усреднение (~0), без effective-conv (−245, конверсия (1+r/4)^4−1 ПОДТВЕРЖДЕНА).
- **ПРОМОУТНУТО В ПРОД (2026-07-02)**: `services/zspread.py` — `compute_z_bps` ветвится по базе (KEYRATE → `solve_flat_y` + `current_period_len`; RUONIA → старый `solve_z_bps`). **Value-фикс**: `fetch_coupon_schedules` теперь отдаёт тройки (start, end, value) с TTL-кэшем на день (`schedule_cache.json` v2 `{"date","items"}`, старый формат отбрасывается) — зафиксированный купон больше не перепрогнозируется в проде. `bonds._universe_bonds` передаёт value в `compute_z_bps`; `calculate_valuation_metrics` нормализует тройки в пары. Стенд: `scripts/nrd_pipeline_probe.py` (матрица вариантов + `--universe` + паритет-проверка прод-модуля 17/17 ✓). UI-колонка **OUR Z** без изменений.

## ⚡ ПРОИЗВОДИТЕЛЬНОСТЬ (2026-07-01)
Диагноз: `iss.moex.com` медленный (~3.5с/запрос) и флаки под нагрузкой (ConnectTimeout при burst); каждый тогл watchlist/открытие карточки re-дёргали MOEX **последовательно**. Alor `get_last_prices_dict` ждал полные 10с (неликвид не шлёт котировку). Было: watch(8) 27–39с, карточка 10–17с. (NB: наивный `gather` без кэша сделал ХУЖЕ — 60–70с из-за шторма коннектов + ретрая таймаута.)
Фиксы: (1) `_universe_bonds`/`get_bond_details` — независимые вызовы через `asyncio.gather`; (2) `_moex_get` **fail-fast** на таймаут (ретрай только 429/5xx), семафор 8→5; (3) **кэш `fetch_moex_securities`** (память+диск `securities_cache.json`) + **TTL-кэш `fetch_moex_snapshot`** (120с) — повторные действия не бомбят ISS; (4) Alor WS-таймаут 10с→4с; (5) `api.js` — `extra` (watchlist) теперь идёт в universe-запрос (без него dirty/DM/coupon = None); (6) `next_coupon` из реального расписания MOEX + fallback цены (live→prev→НРД). **Итог: watch 4.3с cold / 1.3с warm; карточка 1.1с.** NB бэкенд `/api/ws/market` — пустой pass-through (нет фонового Alor-фида; цены только из блокирующего fetch при загрузке) — кандидат на постоянный фид.

## ⚠️ ИЗВЕСТНЫЕ ПРОБЛЕМЫ (аудит 2026-07-01, НЕ исправлены)
### CRITICAL — секреты (2026-07-01: РАЗРЕШЕНО)
- **Alor JWT + rates_cache в git-истории** — исправлено. Файлы были только в tip-коммите `4c7ee02`; переписан через `commit --amend` (added-then-removed → исчезли из дерева), force-push (`--force-with-lease`), `origin/main` теперь `1612955`, в удалённой истории секретов нет.
  - Факт-чек утечки: `.env` **никогда не коммитился** → НРД apikey и Alor **refresh** в git не попадали. В историю утёк только Alor **access-JWT** (уже **протух**, `exp` в прошлом) — торгового риска нет, но светил PII (client id / agreements / portfolios / scope OrdersCreate·Trades). `rates_cache.json` — не секрет.
  - **Остаточный риск**: старый коммит `4c7ee02` может ещё открываться на GitHub по прямому хешу (кэш GitHub GC) и остаётся в форках/старых клонах — данные считать засвеченными. Опц.: сделать репо private + попросить GitHub Support почистить кэш. Ротация Alor refresh/НРД apikey — перестраховка (не были в git), не блокер.

### HIGH
1. Блокировка event loop: `auth.py:39` (requests.post refresh, без timeout) + `rates.py` (Cbonds) — sync в async, вешают весь сервис. → httpx async / asyncio.to_thread.
2. Гонки токен-кэша (нет lock, не атомарная запись) → дубль-логины/rate-limit.
3. ~~Нет auth на API~~ — **исправлено** (см. «Авторизация»): гейт `require_user` на всех роутерах данных + WS, JWT-cookie, аккаунты через CLI. CORS всё ещё `*` (не критично — same-origin, credentials не отдаём).
4. ~~DM смещён: сетка начисления ≠ сетка дисконта~~ — **проверено 2026-07-08, НЕ баг** (купон начисляется за полный период — покупатель платит НКД за истёкший стаб; «фикс» ломал par-инвариант DM=spread). Реальные баги DM-cashflow (перепрогноз зафикс. купона, игнор амортизации) исправлены (см. «Расчёты»).
5. НРД масштаб спредов — эвристика ×10000/×100 без sanity-guard (nrd.py:116); нетипичные единицы → CF/DM ×100 врут.
6. Фронт: WS reconnect-таймер не чистится (api.js:56,70) — зомби-сокеты.

### MEDIUM
- TQCB борд захардкожен (market_data.py:220) — часть флоатеров без prev/accrued.
- N+1 MOEX без семафора; нет ретраев/429.
- Стейл: кривые кэшируются в процессе без TTL (после полуночи — вчерашние); `is_stale=False` захардкожен (bonds.py:360); WS-цены не сбрасываются по дате.
- `fetch_nrd_metrics` дважды в detail (bonds.py:301,411).
- ~~Амортизируемые: DM-модель от полного номинала~~ — **исправлено 2026-07-08**: обе модели (z и DM) учитывают амортизацию (принципал по графику bondization, купоны от остаточного номинала). NB: вторичные пути `get_bonds` (список по явным ISIN) и `/{isin}/valuation` amorts не получают (schedule_full там не грузится — N лишних ISS-запросов); фронт их не использует.
- `price_vs_nrd` от разных баз (список=wa_price, detail=fair||wa).
- Фронт: 453× useAnimationControls без React.memo + нет debounce поиска.

### LOW
- Молчаливые except без логов; print вместо logger; нет валидации ISIN; тесты только test_nrd; double-load на маунте (StrictMode); WS без unsubscribe; CWD-зависимые пути кэшей.

### Ложные срабатывания (проверены, НЕ баги)
- Фильтр рейтинга модификаторы — бэкенд нормализует в бакет, ок.
- Дубли key={isin} — в universe extra не добавляет строки отдельно, дублей нет (но watch-бумага вне юниверса не покажется).

## СЛЕДУЮЩИЕ ШАГИ (по приоритету)
1. **Security**: ротация Alor/НРД токенов + чистка git-истории (нужно решение по force-push).
2. **Quick wins** (безопасно): async-обёртка блокирующих вызовов; TTL кривых + честный is_stale; дедуп fetch_nrd_metrics в detail; семафор MOEX; WS-timer cleanup; guard масштаба НРД; guard base=UNKNOWN.
3. **Крупное**: переписать дисконтирование DM под реальные периоды (свести с НРД); auth на API; амортизация в DM-модели (в z-модели сделана 2026-07-02); кривая под методику НРД для <10bps совпадения.
4. Опции: watchlist на бэкенд (шаринг); внутридневной НРД (valuationnewaddintraday); графики кривых/стакан в drawer.

## Файлы кэшей (gitignored, локальные)
`nrd_cache.json`, `nrd_universe_cache.json`, `shortnames_cache.json`, `schedule_cache.json`, `*_token_cache.json`, `rates_cache.json`, `isins_cache.json`, `.env`, `frontend-react/dist/`. `isins.txt` (16 старых ISIN) + `bondsearch_20_11_2025.xlsx` — устаревший снапшот, юниверс теперь из НРД (не привилегированы).
