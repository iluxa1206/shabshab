# TODO — shabshab (desk)

Ветка: `floaters-desk-deploy`. Прод: assetallocator.ru/desk.

---

## ✅ Аудит конвейера 03.09 — сделано и задеплоено

Повторный прогон `docs/audit_pipeline_prompt.md` в чистом чате: 31 находка → 28
после дедупа → **25 подтверждено** адверсариальной проверкой (непроверенных не
осталось). Плюс при разборе результата вскрылась **регрессия коммита `5b45d33`**:
версия контекста стала тройкой, отпечаток кривых уехал на `[2]`, а три вызова
`metrics_persist` остались на `[1]` — снимок витрины писался и читался под датой
торгового дня, то есть сверка кривой была выключена целиком.

Починены все 22 остававшихся находки + регрессия. Затем **самопроверка тем же
методом нашла 15 проблем уже в самих правках** (шесть — настоящие регрессы:
sanity гасил весь батч цен, `_ctx_no_sched.clear()` самоотключал ретрай,
снятие кэшей сносило только что записанную строку `_level_memo`, `row["wap"]`
перезаписывался после расчёта, сверка средневзвеса ничего не гасила, у `yoi` не
было цены расчёта) — все закрыты до деплоя.

Коммит `f170bb0`, **прод 03.09 13:34**. Полный отчёт с таблицей статусов и
разбором регрессов: `docs/audit_pipeline_2026-09-03.md`.

Замеры с прода после деплоя против цифр в промпте: Y-IDX 87 % (было 86,9 %),
bid 94 %, ask 81 %, wap 72 % (было 75,6 — новая цена средневзвеса теперь гасит
спред и ждёт пересчёта; **проверить завтра, вернётся ли к прежней доле**),
«цена есть, спреда нет» 13 строк вместо 19. Стороны **550/мин по 8-9 мс** против
340/мин по 28 мс. Сетки 593 при 17-29 построений/мин против 25-59 вхолостую.

**Правило на будущее: после правок по аудиту прогонять самопроверку тем же
адверсариальным методом.** Первый проход правок дал 6 регрессов из 22 находок —
без второго круга они уехали бы на прод.

---

## 🔍 Аудит конвейера 02.09 — что осталось

Прогон: Workflow, пять углов (последовательность расчёта, доставка на экран, кэш
потоков, кэши уровней, методическая согласованность) + адверсариальная проверка.
39 находок, из них 14 успели пройти проверку — прогон был остановлен.

Статусы проставлены по итогам 03.09: **закрыто 25** (20 правками `f170bb0`, 5 —
коммитом `5b45d33`), **2 сняты** как неверные, **12 остаются открытыми** — они
перечислены ниже как есть. Их не переоткрывал прогон 03.09: часть просто не
попала под его углы (поллер витрины, гонки записи, as-of), часть смежна
починенному, но конкретно не проверена.

Открытые хвосты одним списком:
- ~~10-минутный поллер витрины подменяет строки целиком~~ — ПОЧИНЕНО 03.09
  (мимо кэша потоков он по-прежнему идёт, см. Minor);
- ~~при фильтре по объёму приглушённым показывается спред верха стакана~~ — ПОЧИНЕНО 03.09;
- ~~смена цены по ISS не заказывает пересчёта~~ — ПОЧИНЕНО 03.09;
- очередь ПОЛНОГО пересчёта без приоритета (у очереди сторон он есть);
- потерянное обновление строки: читают витрину до `await`, пишут после;
- цена набора по объёму без своего спреда, старый спред на фронте не стирается;
- as-of пересобирает поток на каждую цену одного дня;
- кэш-хит `_yoi_cache` выбрасывает построенные узлы вместе с бюджетом такта;
- лента сделок считает цены поштучно вместо батча;
- неудачная сборка контекста стакана в `alor_ws` не запоминается (негативного
  кэша нет; отпечаток кривых и версии Справочника там появился 03.09);
- потолок кэша потоков ≈118 МБ, подрезка спрятана в минутную сводку.

### 🔴 Critical

- [x] ~~ПОЧИНЕНО 03.09 (f170bb0)~~ — цены расчёта в патче + сверка streamMetricPatch. **WS-буфер склеивает патч метрик с более поздней котировкой: старый спред садится на новую цену и НЕ помечается приглушением**
      `frontend-react/src/App.jsx:414`
      Коалесцирующий буфер патчей `buf[isin] = { ...prev, ...q, metrics: prev.metrics ||
      q.metrics }` склеивает патч движка (_metrics_patch) и пришедшую ПОСЛЕ него котировку
      (_broadcast_quote) в один объект. Цена в нём — из котировки (она позже), а спреды — из
      патча метрик (они посчитаны к прежней цене), и флаг metrics:true переносится с прежнего
      патча. В флаше (строки 458-486) applySideQuote честно гасит спред под новую цену, но
      следующая же строка `if (q.metrics) { for (const k of METRIC_KEYS) if (k in q) n[k] =
      q[k]; }` возвращает старое число обратно, а `if (!q.metrics) n._mstale = true` не
      срабатывает — строка не dim.
      СЦЕНАРИЙ: Бумага в стриме, такт движка 5 с. t=0.00: движок пушит _metrics_patch
      {metrics:true, bid:99.50, y_idx_bid_bps:220, yield_over_index_bps:214} (посчитано к last
      99.65 / bid 99.50). t=0.12: приходит котировка Alor {bid:99.62, last_price_pct:99.80,
      src:'ws'}. Оба попадают в один 400-мс флаш → merged q = {bid:99.62, last_price_pct:99.80,
      y_idx_bid_bps:220, yield_over_index_bps:214, metrics:true}. Результат на экране: BID 99,62
      со спредом 220 бп (посчитан к 99,50) и PRICE 99,80 с R-spread 214 бп (посчитан к 99,65),
      обе пары выглядят согласованными и обе врут; _mstale=false, приглушения нет. Это ровно
      рассинхрон 27.08.2026, вернувшийся на главный (WS) путь.
      ЛЕЧЕНИЕ: Патч метрик обязан везти ЦЕНУ, к которой посчитан (как /api/bonds/quotes везёт
      yoi_bid_px/yoi_ask_px): добавить в services/universe_stream._METRIC_FIELDS
      'last'→'metrics_px' и bid/ask уже там, а на фронте перед присвоением METRIC_KEYS сверять
      metrics_px с ценой, которая ложится в строку (тот же приём, что sideMetricPatch).
      Альтернатива-минимум: не наследовать metrics:true при склейке — если после патча метрик
      пришла котировка с ДРУГОЙ ценой, метрические поля из буфера выбросить.

- [x] ~~ПОЧИНЕНО 03.09 (f170bb0)~~ — yoi_px / yoi_wap_px в ответе, сверка на клиенте. **Y-IDX по цене сделки и по средневзвесу едут в котировках БЕЗ цены расчёта — пара «цена+спред» не сверяется**
      `api/routes/bonds.py:460`
      В /api/bonds/quotes `it["last"]` берётся из борд-снапшота ISS (`v.get("last")`, строка
      454), а `it["yoi"]` — из строки движка (`m["yoi"]`), посчитанной по ЕГО цене `m["last"]`
      (Alor-стрим). Цена расчёта наружу не едет: для сторон её специально добавили
      (`yoi_bid_px`/`yoi_ask_px`, строки 500-511) и фронт её сверяет
      (`quotesMerge.sideMetricPatch`), а для главной колонки и для средневзвеса — нет. На фронте
      (App.jsx:596) метка устаревания ставится условием `q.last !== b.last_price_pct && q.yoi ==
      null`, то есть ровно НЕ в том случае, ради которого нужна: когда число у движка есть, но
      от прошлой цены, строка получает `_yoi_stale=false` (App.jsx:619) и рисуется как свежая.
      СЦЕНАРИЙ: Бумага торгуется, движок отстал (очередь _dirty режется по времени,
      недосчитанные возвращаются в очередь; плюс задокументированный даунтайм Alor). Снапшот ISS
      уже несёт last=100.20, движок держит last=99.80 с yoi=250. Ответ котировок: {last: 100.20,
      yoi: 250} без указания, что 250 посчитано к 99.80. Фронт кладёт 100.20 в цену и 250 в
      спред, снимает метку устаревания — в мониторе стоит свежая цена рядом со спредом от
      прошлой, и выглядит это согласованно. Это ровно инцидент 27.08.2026, вылеченный для сторон
      и оставшийся для первичной колонки; то же для пары wap (строка 456: wap из live_quotes по
      тикам) и yoi_wap (строка 466: из движка).
      ЛЕЧЕНИЕ: Отдавать цену расчёта рядом с числом — `it["yoi_px"] = m.get("last")` и
      `it["yoi_wap_px"] = m.get("wap")` (обе уже лежат в строке движка), а на фронте применять
      их через ту же сверку, что `sideMetricPatch`: спред ставится только при совпадении цены,
      иначе прежнее число остаётся приглушённым (`_yoi_stale = true`).

- [x] ~~ПОЧИНЕНО 03.09 (f170bb0)~~ — сверка yoi_px отклоняет число к чужой цене. **Цена сделки из снапшота ISS едет со спредом движка, посчитанным к другой цене**
      `api/routes/bonds.py:454`
      В /api/bonds/quotes строка собирается из ДВУХ источников: it["last"] берётся из борд-
      снапшота ISS (v.get("last")), а it["yoi"] — из строки движка m, где он посчитан к цене
      m["last"]. Цена расчёта наружу НЕ едет (в отличие от сторон, где рядом с yoi_bid идёт
      yoi_bid_px), поэтому фронт сверить пару не может. Больше того, единственная защита на
      фронте выключена условием: App.jsx:596 ставит метку `_yoi_stale` только если `q.yoi ==
      null`, а App.jsx:619 снимает её при любом непустом q.yoi. У бумаги вне универса движка yoi
      нет вовсе, у бумаги внутри — он есть почти всегда, то есть метка не ставится практически
      никогда.
      СЦЕНАРИЙ: Бумага не пушилась Alor'ом >15 с (шард пула отвалился либо неликвид), фронт
      считает её не-streamed. Движок держит строку last=99.20 / yoi=350. В 12:03 на MOEX
      проходит сделка по 99.80, quotes_poller обновляет борд-снапшот за 5 с. Ответ
      /api/bonds/quotes: last=99.80, yoi=350 (посчитан к 99.20), yoi_px не передан. Фронт кладёт
      99.80 в last_price_pct, 350 в yield_over_index_bps и СНИМАЕТ _yoi_stale — в мониторе стоит
      свежая цена с чужим спредом, неприглушённая, и по ней сортируют и фильтруют. Держится до
      ближайшего пересчёта бумаги (до 10 мин, а без пуша Alor — до прохода поллера универса).
      ЛЕЧЕНИЕ: Отдавать цену расчёта рядом с числом — `it["yoi_px"] = m["last"]` — и на фронте
      применять yoi ровно тем же приёмом, что sideMetricPatch: ставить число только при
      совпадении yoi_px с ценой, которая окажется в строке, иначе поднимать _yoi_stale
      (последнее известное приглушённым). Условие App.jsx:596 заменить с `q.yoi == null` на
      `q.yoi_px !== q.last`.

- [x] ~~ПОЧИНЕНО 03.09 (f170bb0)~~ — seed_begin чистит _flow_cache (5b45d33). **seeded-исключение в _check_version не сносит потоки, накопленные ДО засева**
      `services/universe_stream.py:1994`
      `if not seeded: _flow_cache.clear()` исходит из того, что весь кэш потоков — это то, что
      положил засев. Но seed_begin (api/main.py:609, 690) ставит отметку версии, которую сам же
      и породил, вызвав get_curves строкой выше; к этому моменту в _flow_cache могут лежать
      потоки ПРЕДЫДУЩЕГО отпечатка кривых, и такт их не тронет — при том что _level_memo (строка
      1992) чистится всегда, то есть каждая строка будет пересчитана заново по старым потокам.
      СЦЕНАРИЙ: Ночью драйны реестра (рейтинги/дискавери работают 24/7) зовут
      invalidate_params(isin) → _dirty (universe_stream.py:1008), движок считает бумаги и кладёт
      потоки на кривой fp1. В 09:00 daily_prewarm вызывает MarketDataService.get_curves()
      (api/main.py:689) — истёк 15-минутный ретрай стейл-пина (market_data.py:258), кривые
      пересобираются на свежих котировках → fp2; следующей строкой seed_begin помечает засев (D,
      fp2). Ближайший такт: version=(D,fp2) ≠ _memo_version=(D,fp1) → seeded=True → _flow_cache
      НЕ чистится. Потоки на кривой fp1 обслуживают весь торговый день, а витрина считает их
      посчитанными на fp2.
      ЛЕЧЕНИЕ: Ту же версию, что и в фиксе №1, положить рядом с кэшем: чистить _flow_cache
      всегда, когда версия потоков ≠ версии такта, независимо от засева (засев тогда пишет уже
      под новую версию и ничего не теряет). Минимальная заплатка: не применять исключение
      seeded, если _memo_version непустая и отличается от version.

- [x] ~~ПОЧИНЕНО 03.09 (f170bb0)~~ — альт-цены получили свой префикс и строчный sanity не взводят. **Одна мусорная альт-цена (бид/оффер из книги) гасит Y-IDX ВСЕЙ строки**
      `services/valuation.py:697`
      `_sanity = any(w.startswith("sanity:") for w in warnings)` не различает, чьё число вышло
      за границы: строки расчёта или АЛЬТ-ЦЕНЫ. `_sane_bps`/`_sane_pct` для alt-цен (строки 491,
      495, 589, 592 — имена "yield_over_index_alt", "yield_alt", "*_horizon") пишут в тот же
      список warnings, поэтому одна дикая цена из стакана обнуляет
      sm_bps/disc_margin_bps/yield_over_index_bps во ВСЕХ горизонтах и ставит
      pricing_status=SANITY_FLAG. `universe.enrich_bond` кладёт в alt_prices bid, ask и wap
      прямо из книги — то есть числа, которыми движок не управляет.
      СЦЕНАРИЙ: Прогнал: бумага с погашением через 30 дней, цена сделки 100.0, в книге бид 95.0
      (обычный широкий стакан неликвида). alt_prices=[95.0, 100.0, 100.5] → YTM бида 11743% →
      'sanity: yield_alt=11743.049% вне [-5.0,150.0]' → row_yoi=None,
      pricing_status=SANITY_FLAG, при том что y_idx_by_price[100.0]=3048 посчитан нормально. Тот
      же расчёт без бида даёт yoi=3048. То есть у короткой бумаги с бидом на 4-5 пп ниже
      номинала главная колонка Y-IDX в мониторе пустеет, бумага выпадает из сортировки по
      спреду, из фильтров скринера по диапазону спреда и из выборки сторожа yidx_sentinel (он
      пропускает строки с yoi=None).
      ЛЕЧЕНИЕ: Развести уровни: пометить альт-ценовые нарушения отдельным префиксом (напр.
      `_sane_bps(..., name="...", row_level=False)` → warning 'sanity_alt:'), а `_sanity`
      считать только по row-level предупреждениям. Плохая альт-цена уже гасит себя сама — её
      значение в y_idx_by_price становится None, этого достаточно.

- [x] ~~ПОЧИНЕНО 03.09 (f170bb0)~~ — день расписаний в версии (5b45d33) + контексты сносятся на смене дня. **Перекат дня 09:00 не сносит вчерашние контексты и потоки — весь рынок считается по вчерашнему графику платежей**
      ✅ Половина закрыта: `seed_begin` теперь чистит `_flow_cache`, поэтому утренний
      проход больше не считает витрину дня на вчерашних потоках, а день расписаний
      вошёл в версию (сносит `_level_memo`/`_yoi_cache`/`_yoi_grid` в 09:00).
      ОСТАЛОСЬ: `same_day` по-прежнему считается по календарному дню, и `_eval_ctx`
      с вчерашними `accrued/periods/amorts/offers` переживает перекат через ветку
      `seeded`. Правка рискованная (без прогрева даёт холодный догрев рынка) —
      делать отдельно и с тестом на `_seeded_version`.
      `services/universe_stream.py:1981`
      В `_check_version` две страховки засева перекрывают друг друга и превращают смену ДНЯ в
      «ту же кривую внутри дня». Строка 1981 (`if not same_day and _seeded_version and
      _seeded_version[0] == version[0]: same_day = True`) писалась под случай «_memo_version
      пуста после старта», но в живом процессе на перекате `_memo_version` НЕ пуста — в ней
      вчерашняя дата. `daily_prewarm` (api/main.py:690) объявляет `seed_begin(сегодня)` ДО
      прохода, поэтому к первому такту дня `_seeded_version[0] == version[0] == сегодня` →
      `same_day` принудительно True → вместо `_eval_ctx.clear()` идёт `_rebind_curves`, который
      меняет только `curve`/`ruonia_curve`/`calc_date` и оставляет вчерашние `accrued_live`,
      `accrued_date`, `periods`, `amorts`, `offers`. Одновременно `seeded` тоже True, поэтому
      строка 1994-1995 не чистит `_flow_cache` — а его ключи (`("main", spread)`, `("cut",
      cut)`, `("flat", L)`) даты и кривой НЕ содержат: версию держит вызывающий, и он её здесь
      как раз не сбросил. Хуже того, сам утренний проход получает этот же словарь
      (`compute_universe_metrics(flows_by=_us._flow_cache)`, api/main.py:692), то есть считает
      витрину дня на вчерашних потоках ещё до всякого такта. Воспроизведено запуском: после
      `_check_version(("2026-09-02","fp1"), ctx)` при `_memo_version=("2026-09-01","fp0")` и
      `_seeded_version=("2026-09-02","fp1")` в `_eval_ctx` остались `accrued_live=12.34`,
      `accrued_date='2026-09-01'`, `periods=['вчера']`, а `_flow_cache` не тронут. Тест
      tests/test_universe_stream.py:668 это не ловит: он гоняет `_check_version` при
      `_seeded_version=None`.
      СЦЕНАРИЙ: Бумага с купоном, выплаченным сегодня утром. 09:00, `daily_prewarm`:
      `seed_begin(2026-09-02)` → `compute_universe_metrics(flows_by=_flow_cache)` берёт
      вчерашний `("main", spread)` поток, в котором СЕГОДНЯШНИЙ купон ещё будущий → PV завышен
      на целый купон → Y-IDX в главной колонке занижен на десятки б.п. Первый такт движка видит
      version=(2026-09-02, fp) ≠ (2026-09-01, fp) и по строке 1981 решает «тот же день»:
      `_eval_ctx` бумаги не сносится, `y_idx_many` продолжает считать стороны и всю сетку по
      вчерашним `periods`/`amorts`/`offers` и вчерашнему НКД. Держится это до первой СДЕЛКИ по
      бумаге (только тогда `_store_eval_ctx` пересоберёт ctx) и до следующей смены отпечатка
      кривых (только тогда очистится `_flow_cache`); у неликвида — весь торговый день. Прочерка
      нет, число выглядит нормальным.
      ЛЕЧЕНИЕ: Разделить два понятия. `same_day` считать ТОЛЬКО по `_memo_version` (как было), а
      «нет прошлой версии» обрабатывать явно: `same_day = _memo_version is not None and
      _memo_version[0] == version[0]`, и добавку по `_seeded_version` разрешать лишь когда
      `_memo_version is None`. Отдельно: `_flow_cache.clear()` делать безусловно при смене ДНЯ
      (`_memo_version` есть и день другой), а `seeded` пускать в ход только внутри одного дня. И
      перед `compute_universe_metrics` в api/main.py:692/609 передавать пустой словарь потоков,
      если день прохода не совпадает с днём, на котором наполнялся `_flow_cache` (проще всего —
      звать `invalidate_params()` или чистить `_us._flow_cache` в `seed_begin`, раз именно он
      объявляет новый день). Тест на перекат дополнить установкой `_seeded_version` — сейчас он
      проходит только потому, что она None.

- [x] ~~ПОЧИНЕНО 03.09 (f170bb0)~~ — seed_flows льёт только при совпадении версии, seed_ctx не берёт контекст на уехавшей кривой. **Прогрев переживает сброс версии и заново отравляет кэш потоками старой кривой**
      `services/universe.py:347`
      compute_universe_metrics пишет в ОБЩИЙ universe_stream._flow_cache
      (flows_by.setdefault(isin, {})), держа кривые и calc_date, захваченные в начале прохода
      (universe.py:310-313), а ключ потока версии не содержит (services/valuation.py:323 —
      ('main', маржа)). Проход идёт слайсами с await между ними (universe.py:410-415), поэтому
      такт движка может посреди прохода сменить версию и очистить кэш (universe_stream.py:1995).
      Всё, что прогрев запишет ПОСЛЕ очистки, — это потоки старой кривой, лежащие в кэше как
      свежие до следующей смены отпечатка.
      СЦЕНАРИЙ: 09:00, daily_prewarm (api/main.py:691) идёт по 1148 бумагам минутами (холодный
      кэш bondization). На середине прохода Cbonds отдаёт котировки за сегодня →
      curves_fingerprint меняется → такт движка чистит _flow_cache и _level_memo. Оставшиеся
      ~800 бумаг прохода кладут в очищенный кэш потоки, посчитанные по ВЧЕРАШНЕЙ кривой. Дальше
      движок на каждой новой цене сделки берёт их за свежие: цена сегодняшняя, форварды купонов
      вчерашние — та же пара «свежая цена + чужой расчёт», что ловили 27.08.2026, только
      спрятанная на уровень глубже.
      ЛЕЧЕНИЕ: Версию хранить В САМОМ кэше, а не у одного из писателей: _flow_cache_version +
      аксессор flows_for(isin, version), который на несовпадении возвращает пустой словарь и
      переставляет версию. Прогрев и поллер получают не голый dict, а аксессор со своей версией
      — после смены отпечатка их записи просто не принимаются (или проход прерывается и
      перезапускается).

### 🟠 Major

- [x] ~~ПОЧИНЕНО 03.09~~ **[проверено]** **10-минутный поллер подменяет витрину целиком: числа движка откатываются, и никто не переставляется в очередь**
      ✅ Строки движка штампуются `_calc_ts` (`_store_rows`), проход сливается через `universe_stream.merge_universe_metrics(market_cache, metrics, since)`: строка, посчитанная движком после старта прохода, побеждает целиком, более старая сливается по полям (vol_px/yoi_vol переживают проход). Тот же приём в прогреве старта и в 09:00. Тесты: `tests/test_showcase_merge.py`.
      `api/main.py:228`
      universe_price_poller делает `market_cache["universe_metrics"] = metrics` — замену ВСЕГО
      словаря результатом compute_universe_metrics, который считался с начала прохода (проход по
      ~1300 бумагам режется по времени и длится десятки секунд) и не содержит полей, которые
      кладёт только движок: vol_px и yoi_vol. Строки, посчитанные движком ЗА ВРЕМЯ прохода,
      теряются, а бумага после отката ни в _dirty, ни в _sides_dirty не попадает — событий на
      неё больше не будет. Тот же приём стоит в warmup_caches (main.py:614) и daily_prewarm
      (main.py:695).
      СЦЕНАРИЙ: Проход поллера стартует в 14:00:00 и снимает борд-снапшот. В 14:00:05 по бумаге
      проходит сделка, движок пересчитывает строку по новой цене и рассылает патч. В 14:00:35
      поллер кладёт свой словарь: строка бумаги откатывается к цене и спреду 14:00:00, колонки
      цены набора на объём (vol_px/yoi_vol) обнуляются по ВСЕМУ рынку. Пересчёт никто не
      заказывает — до следующей сделки в строке живут числа до сделки, и /api/bonds отдаёт их
      как есть.
      ЛЕЧЕНИЕ: Не заменять словарь, а сливать построчно (как _store_rows): `um.update({i:
      {**um.get(i, {}), **row} for i, row in metrics.items()})` — чтобы поля движка
      (vol_px/yoi_vol/точные yoi_bid/yoi_ask) переживали проход, — либо пропускать бумаги, чья
      строка движка новее начала прохода, отмечая строки временем расчёта.

- [x] ~~ПОЧИНЕНО 03.09~~ **[проверено]** **METRIC_KEYS не содержит preferred_horizon и spread_dur_yrs — горизонт прайсинга на экране не обновляется, хотя движок его прислал**
      ✅ `preferred_horizon` и `spread_dur_yrs` добавлены в METRIC_KEYS (App.jsx).
      `frontend-react/src/App.jsx:421`
      services/universe_stream._METRIC_FIELDS специально кладёт в патч
      'horizon'→preferred_horizon и 'spread_dur'→spread_dur_yrs (в комментарии прямо сказано,
      зачем: «иначе маркер оферты в таблице остался бы от прошлой цены и врал»). Но белый список
      METRIC_KEYS во флаше WS-патчей этих двух полей не содержит, а /api/bonds/quotes их не
      везёт вовсе (в `it` их нет). Единственный путь доставки — полная перезагрузка /api/bonds
      (маунт, смена watchlist, смена размера тикета).
      СЦЕНАРИЙ: Бумага с пут-офертой, цена уходит ниже цены пут-выкупа. Движок переключает
      горизонт maturity→put и присылает патч {metrics:true, yield_over_index_bps: спред К
      ОФЕРТЕ, preferred_horizon:'put', spread_dur_yrs:1.8}. Фронт применяет только METRIC_KEYS:
      спред в колонке R-spread меняется на «к оферте», а b.preferred_horizon остаётся
      'maturity'. Следствия одновременно: в колонке MATURITY синим подсвечены годы до ПОГАШЕНИЯ
      (BondTable.jsx:186 и OfferMarks:104 — жирный маркер p стоит не там), сортировка колонки и
      окно фильтра «срок» меряют горизонт по horizonDate() = дата погашения (App.jsx:673-678,
      horizon.js:32), ось X графика аналитики/сравнения (CalcModule AXES.x = horizonYears) тоже.
      Бумага со спредом к оферте через 1,8 года стоит в списке среди одиннадцатилеток — до F5.
      ЛЕЧЕНИЕ: Добавить 'preferred_horizon' и 'spread_dur_yrs' в METRIC_KEYS (App.jsx:421).
      Лучше — держать один список полей на бэке и фронте (экспортировать состав _METRIC_FIELDS в
      схему), иначе следующее поле, добавленное в патч, потеряется так же молча.

- [x] ~~ПОЧИНЕНО 03.09 (f170bb0)~~ — цены расчёта едут рядом со всеми четырьмя числами. **yoi/yoi_wap едут без цены расчёта, а last в том же ответе — из другого источника (снапшот ISS против цены движка)**
      `api/routes/bonds.py:460`
      Для сторон стакана пара «цена→спред» починена (строки 491-505: цена берётся оттуда же,
      откуда спред, и едет ещё и явным yoi_bid_px/yoi_ask_px для сверки на фронте). Для ЦЕНЫ
      СДЕЛКИ этого не сделано: it['last'] = v.get('last') — борд-снапшот ISS (bonds.py:454), а
      it['yoi'] = m['yoi'] — число движка, посчитанное к m['last'] (цена из пуша Alor,
      services/universe.py:265 «last»: px_display). Поля yoi_px в ответе нет, и фронт
      App.jsx:619 гасит признак устаревания по одному лишь наличию yoi: `if (q.yoi != null)
      n._yoi_stale = false`. То же у пары wap / yoi_wap (bonds.py:463-466): wap читается сейчас
      (live_quotes/waprice), yoi_wap посчитан к средневзвесу на момент такта движка.
      СЦЕНАРИЙ: Бумага вне множества streamed на фронте (последний WS-пуш старше
      LIVE_FRESH_MS=15 с). Строка движка посчитана к цене Alor 100,00. Поллер котировок обновил
      борд-снапшот ISS (force=True, такт 5 с), там уже сделка 100,35. Ответ: last=100,35,
      yoi=спред к 100,00. Фронт (App.jsx:596) НЕ ставит _yoi_stale, потому что q.yoi != null,
      затем строка 619 его ещё и снимает, если он стоял с прошлого такта. На экране: PRICE
      100,35 и рядом R-spread, посчитанный к 100,00, без приглушения — пара выглядит
      согласованной. По методике сторон (комментарий bonds.py:478-490) это признано ошибкой ещё
      02.09.2026, но только для bid/ask.
      ЛЕЧЕНИЕ: Симметрично сторонам: когда строка движка есть, брать `it['last'] = m['last']`
      (цена и спред из одного расчёта), а снапшот оставлять фолбэком для бумаг вне универса
      движка. Либо везти `it['yoi_px'] = m['last']` (и yoi_wap_px = цена средневзвеса расчёта) и
      на фронте ставить yoi только при совпадении цены — как это делает sideMetricPatch.

- [x] ~~ПОЧИНЕНО 03.09~~ **[проверено]** **При фильтре по объёму в ячейке приглушённым показывается спред ВЕРХА СТАКАНА, а не прошлое число этой ячейки**
      ✅ `applyVolume` гасит память ячейки (`y_idx_bid_stale`/`y_idx_ask_stale`) вместе с подменой цены — прочерк честнее чужой метрики. Тесты: `frontend-react/src/vwap.test.jsx`.
      `frontend-react/src/vwap.js:105`
      applyVolume подменяет цену стороны на VWAP набора и спред на y_idx_vol_*_bps, но поле
      «последнее известное число» (y_idx_bid_stale / y_idx_ask_stale) не трогает — оно
      продолжает хранить спред верха стакана, набитый applySideQuote по сырой строке.
      BondTable.Quote (строки 55-56) при spread==null && stale!=null рисует stale приглушённым с
      подписью «спред к прежней цене — пересчитывается».
      СЦЕНАРИЙ: Включён тикет 5 млн. Верх стакана бид 99,62 со спредом 214 бп — он уже лежит в
      y_idx_bid_stale. applyVolume ставит bid_price_pct = 99,10 (VWAP набора) и y_idx_bid_bps =
      null (движок ещё не досчитал спред набора: очередь сторон / сетка). Ячейка BID показывает
      99,10 и под ней 214 бп приглушённо с подсказкой «пересчитывается». 214 — не прошлое
      значение этой ячейки, а другая метрика (спред к лучшей цене, а не к цене набора); разница
      между верхом книги и глубиной тикета читается как нулевая, то есть именно то, ради чего
      фильтр включали, исчезает.
      ЛЕЧЕНИЕ: В applyVolume подменять и stale-поле вместе с ценой/спредом: добавить в
      FLOATER_FIELDS/FIXED_VOL_FIELDS ключи bidStale/askStale и при wantBid ставить туда
      собственную память по цене набора (или null, если её нет). Прочерк здесь честнее чужого
      числа.

- [x] ~~ПОЧИНЕНО 03.09~~ **[проверено]** **Снятие стороны стакана глотается коротким замыканием `same` в мердже /api/bonds/quotes**
      ✅ Проверка «строка не изменилась» переведена на наличие ключа (`"bid" in q`) с eqPx, чтобы null-против-null не гонял ререндер (App.jsx).
      `frontend-react/src/App.jsx:580`
      Условие «строка не изменилась» написано по ЗНАЧЕНИЯМ, а не по ключам: `(q.bid == null ||
      q.bid === b.bid_price_pct)`. У /api/bonds/quotes ключи bid/ask присутствуют ВСЕГДА
      (bonds.py:454), и null означает «заявку сняли» — это новость. Такая строка проходит
      проверку как «без изменений», делается `return b`, и applySideQuote (строки 608-609), у
      которой ровно для этого случая есть флаг hasKey, никогда не вызывается.
      СЦЕНАРИЙ: У бумаги вне стрима уходит бид: движок кладёт в строку bid=None,
      /api/bonds/quotes отдаёт {isin, last:99.80(без изменений), bid:null, ask:100.10(без
      изменений), wap:…, vol:…} — дельта строку ДОСТАВЛЯЕТ (тело изменилось, _quote_delta:392
      даёт новый mt). Фронт: q.bid == null → «same», q.ask совпал, quoteChanges игнорирует
      null'ы, sideMetricChanges вернёт false (yoi_bid в ответе нет) → same=true → строка не
      тронута. На экране остаётся цена бида 99,62 и её спред у стороны, которой в книге больше
      нет, — до следующего движения цены (у неликвида часами). Ровно тот баг, ради которого в
      WS-ветку добавляли hasKey (комментарий App.jsx:604-607).
      ЛЕЧЕНИЕ: Сравнивать по наличию ключа: `(!('bid' in q) || q.bid === b.bid_price_pct)` и так
      же для ask/last (для wap/vol текущее поведение верно — они аддитивные). Проще и надёжнее —
      убрать ручной `same` и решать по результату мерджа: собрать n и сравнить с b по изменённым
      полям.

- [x] ~~ПОЧИНЕНО 03.09~~ **[проверено]** **У _sides_from нет проверки возраста котировки: мёртвый шард морозит верх стакана в строке движка (live_sides такую проверку имеет)**
      ✅ `_sides_from` стареет пуш по `_ts` c порогом `_LIVE_SIDE_MAX_AGE_SEC` и падает на борд-снапшот. Синтетический пуш без метки не отбрасывается (её ставит только _on_quote).
      `services/universe_stream.py:1467`
      _last_quote не чистится и не стареет никогда (записи только в _on_quote:668). Порог
      живости _LIVE_SIDE_MAX_AGE_SEC=120 применяется ТОЛЬКО в live_sides (строка 1125) — там он
      и появился с комментарием «мёртвый сокет не обновляет _last_quote вовсе». Но _sides_from
      (1467), которым пользуются и recrunch_sides (1501), и _crunch (2320), и
      _blank_side_targets (1531), безусловно предпочитает значение из пуша значению из свежего
      борд-снапшота. Строка движка получает замороженные bid/ask и посчитанный к ним спред, а
      /api/bonds/quotes (строки 501-505) предпочитает m[bid]/m[ask] снапшоту.
      СЦЕНАРИЙ: Шард пула (150 бумаг) падает или Alor даёт даунтайм; _last_quote по этим бумагам
      заморожен на значениях 10-минутной давности. live_sides по ним корректно молчит (пуш
      старше 120 с), но движок при очередном заходе (волна размера тикета, добор прочерков
      _blank_side_targets, invalidate_params) пересчитывает стороны по замороженным ценам и
      кладёт их в universe_metrics. Дальше /api/bonds/quotes отдаёт эти bid/ask ВМЕСТО живого
      снапшота ISS, потому что «ключ есть в m». На экране 150 бумаг с замороженным верхом
      стакана и идеально согласованным с ним спредом — распознать нечем.
      ЛЕЧЕНИЕ: Ту же проверку возраста внести в _sides_from: сторону из пуша брать только если
      time.time() - q.get('_ts', 0) <= _LIVE_SIDE_MAX_AGE_SEC, иначе падать на snap. Заодно это
      чинит и _crunch/warm_grids, которые сейчас строят сетку по мёртвой книге.

- [x] ~~ПОЧИНЕНО 03.09~~ **[проверено]** **У фикса частичный пуш стирает цену стороны и все её метрики, и это уезжает явным null**
      ✅ `_crunch_fixed`: `_sides_of(q)` → `_sides_from(q, snap)`.
      `services/universe_stream.py:1579`
      _crunch_fixed берёт стороны как `_sides_of(q)` — только из пуша, — тогда как ветка
      флоатера уже переведена на `_sides_from(q, snap)` с фолбэком на борд-снапшот (докстринг
      _sides_from прямо описывает, зачем: «Пуш Alor приходит и без стороны — например, тик
      сделки… На проде 02.09 таких строк было 75»). Дальше row["bid"], row["ask"] пишутся
      безусловно, а _fixed_patch (universe_stream.py:593) кладёт bid/ask в патч всегда, когда
      ключ в строке есть, и явный null на фронте стирает число. Компенсирует это _on_quote: у
      фикса смена сторон уходит в _dirty (дешёвой ветки у него нет), то есть частичный пуш сам
      же и заказывает пересчёт, который сторону обнулит.
      СЦЕНАРИЙ: По ОФЗ проходит тик сделки; Alor присылает пуш с last_price и без bid/ask.
      _on_quote видит расхождение сторон (значение → None) и кладёт ISIN в _dirty. На такте
      _crunch_fixed получает sides={"bid": None, "ask": None}, пишет их в строку,
      compute_fixed_row возвращает ytm_bid/ytm_ask/g_spread_bid_bps пустыми; _store_rows кладёт
      это в fixed_metrics, _push_metrics рассылает патч с bid=null/ask=null. В витрине ФИКСОВ у
      бумаги гаснут обе стороны и все их метрики, хотя в борд-снапшоте цены есть — до следующего
      полного пуша книги.
      ЛЕЧЕНИЕ: В _crunch_fixed заменить `sides = _sides_of(q)` на `sides = _sides_from(q, snap)`
      (snap там уже собран строкой выше) — ровно как в _crunch:1885.

- [x] ~~СНЯТА 03.09~~ — не баг, а структурный потолок: бумага попадает в _dirty только на смену цены сделки, 1,13 смены на уровень — это скорость возврата цены на рынке. **_level_memo не спасает по построению ключа: hit 1 / miss 578 — это норма, а не аномалия**
      `services/universe_stream.py:1839`
      Ключ кэша — `(isin, round(px, 3))`, а единственный поставщик очереди `_dirty` — условие
      `prev.get("last_price") != px` в `_on_quote` (строка 672). То есть в `_crunch` бумага
      попадает ТОЛЬКО тогда, когда её цена отличается от прошлой, и промах гарантирован
      конструкцией: попасть можно лишь возвратом на уже посчитанный уровень с той же версией
      кривых. Возвраты ещё и прорежены: за такт из `_dirty` берётся ПОСЛЕДНЯЯ цена
      (`_last_quote.get(i)`), промежуточные тики в кэш не попадают вовсе, так что запомненные
      уровни — редкая выборка, а не окрестность рынка. Отсюда «hit 1 / miss 578»: это не сбой
      кэша, это его устройство. Число вдобавок нечитаемо: в минутной сводке (строка 2265)
      `_memo_hits`/`_memo_misses` — единственные счётчики, которые НЕ обнуляются вместе с
      `done_since_log`/`_grid_builds`, то есть печатаются накопительно с момента старта в
      строке, размеченной как «в минуту». Цена этого кэша реальна: до 20 000 полных строк метрик
      в памяти (`_LEVEL_MEMO_MAX`) — тот самый рост 599 → 1004 МБ 13.08, — а выигрыш на
      попадании невелик, потому что самую дорогую часть (сборку потока) уже держит
      `_flow_cache`.
      СЦЕНАРИЙ: Бумага торгуется 100,01 → 100,02 → 100,03 → 100,02. Первые три тика — три
      промаха и три полных `enrich_bond`. Четвёртый мог бы попасть, но за это время такт мог
      схлопнуть 100,03 и 100,02 в одну запись (в `_dirty` бумага одна, берётся последняя цена),
      а на смене отпечатка кривых `_check_version` чистит весь словарь. По итогам сессии в проде
      остаётся один хит на шестую сотню промахов, а в памяти висят тысячи строк, которых больше
      никто не спросит.
      ЛЕЧЕНИЕ: Либо убрать `_level_memo` вовсе (сборку потока и так кэширует `_flow_cache`, а
      `_eval_ctx` держит контекст) и снять с процесса десятки мегабайт, либо перестать
      притворяться, что он кэш: обнулять `_memo_hits`/`_memo_misses` в минутной сводке рядом с
      остальными счётчиками, снизить `_LEVEL_MEMO_MAX` до сотен и мерить хит-рейт честно неделю,
      прежде чем платить за него памятью. Если кэш оставлять — единственный сценарий, где он
      может попадать, это возврат цены; тогда стоит писать в него и промежуточные тики из
      `_last_quote`, иначе выборка уровней слишком редкая.

- [x] ~~ПОЧИНЕНО 03.09 (f170bb0)~~ — снимок книги один на заход, протаскивается в _vol_prices/_grid_nodes. **get_depth() зовётся на каждую бумагу внутри цикла по очереди сторон — та самая пересборка словаря всего рынка, от которой уходили**
      `services/universe_stream.py:1447`
      `_vol_prices` (строка 1447) при `ladders=None` и `_grid_nodes` (строка 1078) каждый зовут
      `depth_svc.get_depth()` для ОДНОЙ бумаги. `get_depth()` дешёвым геттером не является — при
      каждом вызове он строит множество `dead` обходом всех ISIN всех шардов, а при протухшем
      шарде ещё и пересобирает словарь рынка целиком (services/depth.py:50-68). Комментарии в
      этом же файле (`_has_book`, `apply_vol_sizes`, api/routes/bonds.py:540) явно запрещают
      такой вызов в цикле и передают снимок снаружи — но `_fill_side_metrics` зовёт
      `_vol_prices(isin)` без `ladders`, а `warm_grids`, взяв снимок для `_has_book`, тут же
      теряет его в `_grid_nodes`.
      СЦЕНАРИЙ: Такт с полной очередью сторон: `recrunch_sides` идёт по 400 бумагам, на каждой
      `_fill_side_metrics` → `_vol_prices(isin)` (один `get_depth`) и при отсутствующей сетке
      `_grid_nodes` (второй `get_depth`). До 800 обходов по ~1300 ISIN шардов за такт, плюс
      полная пересборка словаря на каждом вызове, если хоть один depth-шард протух. Всё это
      внутри `run_heavy`, то есть с удержанным GIL: к 13-35 мс полезного счёта на бумагу
      добавляются миллисекунды чистой пересборки книги, а сторож лага показывает функцию, а не
      причину.
      ЛЕЧЕНИЕ: Пробросить снимок глубины через ветку сторон: брать `book = _depth_snapshot()`
      один раз в `recrunch_sides` и в `_crunch`, передавать в `_fill_side_metrics`, оттуда — в
      `_vol_prices(..., ladders=book.get(isin) or {})` и в `_grid_nodes(..., lad=book.get(isin)
      or {})`. Сигнатура `_vol_prices` для этого уже готова, `_grid_nodes` надо дополнить
      параметром. Заодно передать `sizes=active_vol_sizes()` снаружи — сейчас он тоже
      пересчитывается на каждой бумаге.

- [x] ~~ПОЧИНЕНО 03.09 (f170bb0)~~ — гасится цена расчёта во всех картах; чужие цены батча не трогаются. **Гарды sanity/implausible чистят yield_over_index_bps горизонтов, но не y_idx_by_price — лестница и лента отдают числа, которые расчёт признал мусором**
      `services/valuation.py:702`
      В обоих гардах (`if price_implausible:` ~строка 683 и `if _sanity:` строка 698) цикл `for
      _h in horizons.values()` зануляет только sm_bps/disc_margin_bps/yield_over_index_bps и
      дюрации. Поценовые карты `_h["y_idx_by_price"]`, `_h["ytm_by_price"]`, `_h["dm_by_price"]`
      остаются заполненными. Верхнеуровневый `y_idx_by_price` при этом ПЕРЕПРИВЯЗЫВАЕТСЯ на
      пустой dict (строка 701), а не очищается, поэтому horizons-копия продолжает жить со старым
      содержимым. Потребители делятся на два лагера: `screener_core.exact_y_idx`, витрина и
      карточка читают `yield_over_index_bps` (получают None), а `yidx_exact.y_idx_many` (стороны
      движка, сетка _yoi_grid), `orderbook_svc.build_levels_fn` (лестница стакана) и
      `trade_yidx` (лента сделок) читают `y_idx_by_price` (получают число).
      СЦЕНАРИЙ: Прогнал: bond 30 дн до погашения, price=100.0, alt=[95.0,100.0,100.5] → sanity
      сработал, `horizons['maturity']['yield_over_index_bps']=None`, а
      `horizons['maturity']['y_idx_by_price'] = {95.0: None, 100.0: 3048, 100.5: 2165}`. Итог: в
      мониторе по цене 100.0 прочерк, в стакане/ленте по той же цене 3048 бп, и это одно и то же
      событие одного расчёта. Сторож согласия молчит: он сравнивает пару только когда оба числа
      не None.
      ЛЕЧЕНИЕ: В обоих гардах чистить и поценовые карты того же горизонта (`_h["y_idx_by_price"]
      = _h["ytm_by_price"] = _h["dm_by_price"] = {}`) — вместе с решением из первой находки
      (row-level причины гасят всё, альт-ценовые гасят только свою цену). Заодно заменить
      `y_idx_by_price = {}` на очистку того же объекта, чтобы верх и горизонты не расходились.

- [x] ~~ПОЧИНЕНО 03.09~~ *[не проверялась]* **Движок узнаёт цену только из _last_quote: смена цены по ISS не заказывает пересчёта вовсе**
      ✅ Разведены источник цены и источник события: `_crunch` (и `_crunch_fixed`) берут цену пуш → борд-снапшот; `quotes_poller` при расхождении снапшотной цены с прошлой зовёт `queue_price_change` (бумаги вне стрима, только при ИЗВЕСТНОЙ прошлой цене — первый проход очередь не забивает); в `invalidate_params` снято условие `if isin in _last_quote`, и правка Справочника доезжает до бумаги без котировки.
      `services/universe_stream.py:1836`
      Единственный вход цены в полный пересчёт — словарь пушей: очередь наполняет только
      _on_quote (px из пуша Alor), а _crunch берёт цену исключительно из _last_quote (`px =
      q.get("last_price")`, иначе `continue`). Ни борд-снапшот ISS, ни
      market_cache['last_prices'] (их наполняет quotes_poller в api/main.py:555 именно для бумаг
      ВНЕ стрима) в очередь ничего не ставят. Отсюда три дыры одной природы: (1) сделка, которую
      увидел только ISS, не заказывает ничего; (2) invalidate_params (правка Справочника) пинает
      бумагу только `if isin in _last_quote` — у бумаги без пуша правка вообще не доезжает; (3)
      заказанный пересчёт теряется, если к моменту такта _last_quote перезаписан пушем БЕЗ
      last_price (что такие пуши бывают, зафиксировано в докстринге _sides_from: «Пуш Alor
      приходит и без стороны — например, тик сделки»): ISIN уже снят с очереди через
      _dirty.difference_update(take), а в _crunch он отваливается на `if px is None: continue`.
      СЦЕНАРИЙ: Шард пула на 150 бумаг отвалился (сторож молчит — соседние шарды живы). По
      бумаге проходит сделка 99.20 → 99.80: quotes_poller кладёт цену в last_prices, борд-
      снапшот отдаёт её витрине котировок, но _dirty пуст, _eval_ctx не трогается. Строка движка
      (yoi, dm, dirty, horizon, spread_dur) остаётся посчитанной к 99.20 до ближайшего
      10-минутного прохода universe_price_poller, а вне торговых часов — навсегда. Вместе с
      находкой №1 это даёт свежую цену со старым спредом без всякой пометки.
      ЛЕЧЕНИЕ: Разделить «источник цены» и «источник события»: в _crunch добавить фолбэк `px =
      q.get("last_price")` → `ctx["board"].get(isin,{}).get("last")` → session_prices, а в
      quotes_poller при расхождении снапшотной цены с прошлой класть ISIN в _dirty (для бумаг
      вне live_isins()). Тогда же снимается ограничение `if isin in _last_quote` в
      invalidate_params.

- [x] ~~ПОЧИНЕНО 03.09~~ *[не проверялась]* **День кэша потоков (полночь) не совпадает с днём расписаний купонов (09:00)**
      ✅ Версия кэшей стала тройкой `(calc_date, _trading_day(), curves_fp)` — в `_day_ctx` и в `seed_begin`; вдобавок `seed_begin` чистит `_flow_cache` (он же объявляет новый день, а прогрев получает этот словарь на вход). ОСТАЛОСЬ: `_eval_ctx` на перекате 09:00 всё ещё переживает смену расписаний через ветку `seeded` — это часть отдельной Critical-находки ниже.
      `services/universe_stream.py:1995`
      Версия = (calc_date, отпечаток кривых), где calc_date = date.today() и перекатывается в
      ПОЛНОЧЬ (market_data.py:249), а купоны/амортизации/оферты приходят из day-кэша с перекатом
      в 09:00 МСК (_trading_day, market_data.py:38-45). Ключ потока (services/valuation.py:323,
      537) расписание не включает вовсе. Поток, построенный между 00:00 и 09:00 на вчерашнем
      bondization, переживает перекат расписаний и обслуживает весь торговый день.
      СЦЕНАРИЙ: В 03:00 ночной драйн реестра дёргает бумагу (invalidate_params → _dirty), движок
      строит поток по расписанию за D-1: купон начавшегося периода там ещё не определён (эхо
      снято) и проецируется форвардом. В 09:00 MOEX отдаёт фактическую ставку, прогрев тянет
      свежее расписание — но flows_by.setdefault отдаёт ночной словарь, ключ ('main', маржа)
      совпадает, и факт купона в поток не попадает. При этом НКД и current_index_pct (уровень
      индекса для DM) считаются уже по НОВОМУ расписанию — строка смешивает два дня в одном
      числе.
      ЛЕЧЕНИЕ: Включить день расписаний в версию: version = (str(calc_date), _trading_day(),
      curves_fp) в universe_stream._day_ctx (строка 1935) — тогда 09:00 честно сносит и
      _flow_cache, и _level_memo, и _eval_ctx (в них лежат те же periods/amorts/offers), а не
      только полночь.

- [x] ~~СНЯТА 03.09~~ — прогон 03.09 разобрал и снял: y_idx_many кладёт None по всем ценам, пустой ответ не кэшируется только на исключении. **Добор прочерков в сторонах вечно перебирает голову _eval_ctx — хвост рынка не добирается никогда**
      `services/universe_stream.py:1509`
      _blank_side_targets каждый такт обходит _eval_ctx С НАЧАЛА и выходит по `len(out) >=
      limit` (_BLANK_SIDES_BATCH=150). Паузы после неудачной попытки, в отличие от сеток
      (_grid_cold + _GRID_RETRY_SEC, заведённые ровно от этой болезни), здесь нет. Бумага,
      которой спред стороны не посчитать в принципе (yidx_exact.y_idx_many возвращает {} при
      pricing_status NO_ACCRUED или при исключении расчёта), остаётся с yoi_bid=None после
      пересчёта, снова проходит фильтр и снова занимает слот — и так весь день.
      СЦЕНАРИЙ: После утреннего засева в голове _eval_ctx (порядок вставки = порядок универса)
      оказывается ≥150 бумаг без биржевого НКД и с неберущимся расчётом. Каждый такт
      _blank_side_targets набирает лимит на них одних, _queue_sides ставит в очередь те же 150
      ISIN, recrunch_sides честно платит по 13–35 мс за штуку и ничего не заполняет, а ликвидная
      бумага из хвоста _eval_ctx, у которой цена стороны есть из снапшота, а спреда нет и
      движения книги за день не случилось, в очередь не попадает ни разу. В минутной сводке это
      видно как «прочерков N» на постоянном уровне при полностью выбранной пачке сторон.
      ЛЕЧЕНИЕ: Тот же приём, что для сеток: словарь _blank_cold[isin] = monotonic неудачной
      попытки и пропуск в _blank_side_targets, пока не вышел таймер (плюс сброс на смене
      версии/эпохи, как _grid_cold.clear() в _check_version). Дополнительно — начинать обход не
      с начала dict, а с курсора-ротации, чтобы лимит не был привязан к голове.

- [x] ~~ПОЧИНЕНО 03.09 (f170bb0)~~ — снос только при реальном изменении _grid_ctx_key. **Каждая сделка уничтожает сетку цен своей бумаги, и её же перестройка съедает потолок такта**
      `services/universe_stream.py:1648`
      `_store_eval_ctx` заканчивается `_yoi_grid.pop(isin, None)` (строка 1648) и вызывается
      БЕЗУСЛОВНО на каждом промахе кэша уровней (строка 1873). Промах — это фактически каждая
      сделка (см. отдельную находку про `_level_memo`), поэтому у самых торгуемых бумаг сетка не
      доживает до второго использования. Обоснование сноса («узлы считались по старому графику и
      старому НКД») в этом месте не работает: `_store_eval_ctx` пересобирает ctx из
      `ctx["full_by"]`, который берётся из дневного кэша расписаний, и из того же борд-снапшота
      — содержимое то же самое, меняется только идентичность объекта. Дальше в том же вызове
      `_fill_side_metrics` → `_grid_nodes_if_needed` видит отсутствие сетки и строит её заново,
      списывая слот из `_grid_budget` (потолок 12 на такт, строка 2303 сбрасывает его перед
      ПОЛНЫМ батчем на 60 бумаг). То есть ликвидные бумаги перестраивают сетку, которую сами
      только что снесли, и выбирают квоту раньше очереди сторон.
      СЦЕНАРИЙ: Включён фильтр по объёму, в очереди сторон стоит 300 бумаг после волны нового
      размера тикета. В том же такте приходят 60 сделок по ликвидным флоатерам → 60 промахов
      memo → 60 раз `_store_eval_ctx` роняет сетку → первые 12 из них тут же её отстраивают
      (+~96 мс к батчу каждая, 260 узлов × 0,37 мс) и обнуляют `_grid_budget`. Очередь сторон в
      этом такте не получает ни одной сетки: `apply_vol_sizes` для них возвращает `yoi_at →
      None`, ставит бумаги обратно в `_SIDES_PRIO_WAVE`, и цена набора расползается по рынку
      минутами — ровно та жалоба, под которую вводили `_GRID_BUILD_PER_TICK`. У ликвидной бумаги
      цикл «снёс — построил» повторяется на каждой сделке весь день.
      ЛЕЧЕНИЕ: Ронять сетку только когда контекст ДЕЙСТВИТЕЛЬНО изменился. В `_store_eval_ctx`
      посчитать отпечаток входов (`accrued_live`, `accrued_date`, `len(periods)` и последний
      период, `amorts`, `offers`), сравнить с отпечатком прежнего `_eval_ctx[isin]` и звать
      `_yoi_grid.pop`/`_grid_cold.pop` только при расхождении. Отдельно — `_grid_budget` перед
      полным батчем и перед очередью сторон делить (например, половина слотов резервируется под
      `_sides_dirty`), чтобы волна размера тикета не голодала за счёт ликвида.

- [x] ~~ПОЧИНЕНО 03.09 (f170bb0)~~ — bonds.normalize_ref_face — одна копия на проект. **Контекст движка из warm_ctx собирается без сверки номинала — амортизируемая бумага прайсится от стейл-номинала**
      `services/universe_stream.py:1711`
      `warm_ctx` строит ref через `build_universe_ref` и сразу отдаёт его в `_store_eval_ctx`
      (строка 1715), пропуская две правки номинала, которые есть в обоих других сборщиках того
      же ref: `reconcile_face(...)` и `amort_remaining_face(...)` в `universe.enrich_bond`
      (строки 100-108) и ровно они же в `bond_details.load_reprice_ctx` (строки 350-354).
      `calculate_valuation_metrics` номинал сам не восстанавливает: `face_for_pricing` вычитает
      только транши в окне (calc, settle], остальное берётся из `bond.face_value` как есть. Этот
      контекст кормит `y_idx_many` — то есть стороны стакана в строке, сетку `_yoi_grid` и через
      неё пары «цена+спред» в /api/bonds/quotes (live_sides).
      СЦЕНАРИЙ: Амортизируемый флоатер погасил часть номинала (реальный остаток 900 ₽,
      isins_cache держит 1000 ₽ — задокументированный случай БалтЛизП10). Бумага сегодня не
      торговалась, а её контекст собрал не утренний проход (seed_ctx после enrich_bond, где
      номинал уже сверен), а `warm_ctx` — так бывает после правки строки в Справочнике
      (`invalidate_params` выбрасывает _eval_ctx, а _dirty пинок ставится только при наличии
      котировки) и у бумаг, пропущенных прогревом (_seed_skips). Тогда dirty стороны считается
      как 1000×цена% + НКД вместо 900×цена% + НКД — завышение на ~11 % → спред стороны уезжает
      на сотни бп, при том что главный Y-IDX той же строки (из enrich, номинал сверен) и
      лестница стакана (load_reprice_ctx, номинал сверен) показывают верное число.
      ЛЕЧЕНИЕ: Перенести сверку номинала в `_store_eval_ctx` (там уже есть `full["coupons"]` и
      `full["amorts"]`) либо внутрь `build_universe_ref`, чтобы номинал правился один раз для
      всех трёх сборщиков контекста.

- [x] ~~ПОЧИНЕНО 03.09 (f170bb0)~~ — y_idx_many получает кэш витрины через _with_flows. **Кэш потоков не подключён к горячей ветке сторон: y_idx_many пересобирает график платежей на каждый пересчёт**
      `services/yidx_exact.py:71`
      `y_idx_many` зовёт `calculate_valuation_metrics` БЕЗ `flows_cache`, хотя в том же модуле
      `universe_stream` уже лежит `_flow_cache[isin]` ровно с этими потоками, собранный
      `enrich_bond` в `_crunch` (строка 1848). В `_eval_ctx` словаря потоков тоже нет. Значит
      вся дешёвая ветка — `_fill_side_metrics` (стороны + средневзвес + цены наборов),
      `warm_grids` и каждая перестройка сетки — платит полную сборку
      `build_cashflows_with_spread` заново, и не один раз: `_flows` вызывается по ключам
      `("main", spread)`, `("cut", cut)` для второго горизонта и `("flat", L)` под DM. По
      собственному замеру в коде (services/valuation.py:81) это 35 мс на тридцатилетней бумаге,
      дважды за расчёт, — при том что очередь сторон прогоняет до 400 бумаг за такт и в сводке
      движка это самая частая работа. Кэш существует, наполнен, версионируется вместе с уровнями
      — и не используется тем, кто зовёт расчёт чаще всех.
      СЦЕНАРИЙ: Ипотечный агент с 370 платежами стоит в `_sides_dirty`. Верх книги дёргается 20
      раз в минуту → 20 вызовов `recrunch_sides` → `_fill_side_metrics` → `y_idx_many` → 20 ×
      (сборка потока к погашению + сборка потока к оферте) ≈ 20 × 70 мс = 1,4 с ядра в минуту на
      ОДНОЙ бумаге, при том что `_flow_cache['RU000A…'][('main', spread)]` в этот момент лежит
      готовым в соседнем словаре. Тот же счёт повторяется при каждой перестройке сетки (≈150 мс
      на бумагу, из них большая часть — этот же поток).
      ЛЕЧЕНИЕ: Положить словарь потоков в контекст: в `_store_eval_ctx` добавить `"flows":
      _flow_cache.setdefault(isin, {})`, в `y_idx_many` пробросить
      `flows_cache=ctx.get("flows")` в `calculate_valuation_metrics`. Ключи потоков у обоих
      вызывающих одинаковы (тот же `ref_obj` из `_eval_ctx["ref_obj"]`, та же кривая, те же
      `periods/amorts/offers`), а `("flat", round(L,6))` различает плоские кривые сам, так что
      смешивания не будет. Версионирование уже общее — `_check_version` и `invalidate_params`
      чистят `_flow_cache` вместе с остальными. Заодно тот же приём в
      `services/orderbook_svc.build_levels_fn`: там свой локальный `flows` на контекст, то есть
      потоки одной и той же бумаги строятся дважды — для монитора и для карточки.

- [x] ~~ПОЧИНЕНО 03.09 (f170bb0)~~ — horizon_at_price на каждую цену, как в движке и стакане. **Спред bid/ask/средневзвеса в enrich_bond берётся с горизонта ЦЕНЫ СДЕЛКИ, а не с горизонта своей цены**
      `services/universe.py:193`
      `_alt = _hzm.get("y_idx_by_price")` — `_hzm` это горизонт, выбранный правилом цены для
      price_calc; из него достаются yoi_bid/yoi_ask/yoi_wap. Все остальные потребители той же
      цены решают горизонт ПОКАЖДОЙ цене: `yidx_exact.y_idx_many` зовёт `horizon_at_price(p,
      m)`, `orderbook_svc.build_levels_fn` — тоже (строка 126). Расхождение живёт в проде,
      потому что enrich-версия сторон не всегда перетирается движком: `_fill_side_metrics`
      выходит раньше зануления, когда контекста нет (universe_stream.py:1347), а
      `_blank_side_targets` добирает только строки, где yoi_side уже None, — заполненную
      enrich'ем сторону движок больше не трогает.
      СЦЕНАРИЙ: Прогнал: KEYRATE+150, погашение 2034, пут через 110 дней по 100.0, цена сделки
      99.5, оффер 100.5. Правило цены для 99.5 даёт горизонт 'put', для 100.5 — 'maturity'.
      Y-IDX оффера: путь витрины (горизонт строки) = -23 бп, путь лестницы/движка (горизонт
      своей цены) = 243 бп. Δ = 266 бп на одной и той же цене 100.5. При цене сделки 99.4/оффере
      100.4 — 16 против 246 (Δ=230). Пользователь видит в колонке Y-IDX ASK монитора одно число,
      а в панели стакана на верхнем уровне — другое, на сотни бп.
      ЛЕЧЕНИЕ: В enrich_bond выбирать горизонт по каждой альт-цене тем же
      `services.valuation.horizon_at_price(p, m)`, что и yidx_exact/orderbook_svc (или вовсе не
      считать здесь стороны, оставив единственным писателем yoi_bid/yoi_ask/yoi_wap
      `universe_stream._fill_side_metrics`, чтобы у поля был один автор).

- [x] ~~ПОЧИНЕНО 03.09 (f170bb0)~~ — with_margins=False + общий кэш потоков; на проде 28 → 8-9 мс. **Точный Y-IDX сторон и сеток (y_idx_many) строит поток заново на каждый вызов и считает маржи, которые выбрасывает**
      `services/yidx_exact.py:71`
      y_idx_many — самый частый потребитель расчёта (стороны стакана universe_stream.py:1395,
      догрев сеток 1275, скринер screener_core.py:842) — зовёт calculate_valuation_metrics без
      flows_cache и без with_margins=False, хотя читает только y_idx_by_price. Значит на каждый
      вызов пересобирается поток к погашению, поток к оферте, и ради выбрасываемого DM ещё два
      потока на плоской кривой.
      СЦЕНАРИЙ: Замер (синтетика, 3 цены — bid/ask/средневзвес): бумага с 38 будущими купонами —
      8,3 мс сейчас, 6,8 мс с кэшем потоков, 4,25 мс с кэшем и без марж; с 366 купонами — 145 мс
      / 66 мс / 39 мс. При потоке стаканов 600–900 сообщений в минуту это единицы секунд CPU в
      heavy-потоке каждую минуту, а heavy держит GIL: ровно те секунды, на которые сайт не
      отвечает.
      ЛЕЧЕНИЕ: Передавать flows_cache (у движка — тот же версионный аксессор из фикса №1, ctx
      лежит в _eval_ctx на ту же бумагу) и with_margins=False. Порядок важен: включать кэш здесь
      можно только после фиксов №1–№2, иначе перепривязка кривой в _eval_ctx
      (universe_stream.py:1939) начнёт встречаться с непочищенными потоками.

### 🟡 Minor

- [ ] **[проверено]** **Десятиминутный поллер витрины идёт мимо кэша потоков**
      `api/main.py:226`
      Фоновый цикл в торговые часы считает ВЕСЬ универс через compute_universe_metrics без
      flows_by, то есть строит ~1150 потоков заново каждые 10 минут — при том что рядом лежит
      уже наполненный _flow_cache той же версии.
      СЦЕНАРИЙ: По реестру (data/instruments.db): 1148 активных бумаг, в среднем 38 будущих
      купонов, 94 бумаги со 100+. Измеренная экономия попадания на пути витрины
      (with_margins=False): 0,7–2 мс на короткой бумаге, 5,3 мс при 120 купонах, 38,7 мс при 366
      → ≈3 с CPU heavy-потока на каждый проход, каждые 10 минут, всю сессию.
      ЛЕЧЕНИЕ: Передать тот же версионный аксессор кэша, что и прогреву (после фикса №1); без
      версии подключать этот проход к общему кэшу нельзя — он даст третьего писателя без
      синхронизации.

- [ ] **[проверено]** **Очередь полного пересчёта — без порядка и без приоритета, в отличие от всех остальных**
      `services/universe_stream.py:2305`
      _dirty — обычный set, и такт берёт `list(_dirty)[:_MAX_BATCH]`, то есть порядок задаётся
      хешами строк. У очереди сторон приоритет есть (_SIDES_PRIO_LIVE/_WAVE), догрев контекстов
      и сеток идёт «видимые вперёд», а у ОСНОВНОЙ очереди — той, что несёт первичную метрику
      Y-IDX по цене сделки, — ни FIFO, ни приоритета видимых. Недосчитанный хвост возвращается
      через _dirty.update(pend) в тот же неупорядоченный набор.
      СЦЕНАРИЙ: Массовая инвалидация (импорт xlsx в Справочнике → invalidate_params(None) →
      _dirty.update(_last_quote.keys()), ~1300 бумаг) при потолке 60 за такт и бюджете 2.5 с:
      очередь разбирается ~2 минуты в произвольном порядке. Строка, на которую человек смотрит и
      которая только что торговалась, может оказаться в хвосте хеш-порядка и ждать все две
      минуты, пока считается неликвид, которого никто не открывал.
      ЛЕЧЕНИЕ: Брать пачку с сортировкой, как у сторон: сначала is_visible(isin), затем всё
      остальное — `take = sorted(_dirty, key=lambda i: (not is_visible(i),))[:_MAX_BATCH]`; или
      держать _dirty упорядоченным словарём isin→приоритет, тем же приёмом, что _sides_dirty.

- [ ] **[проверено]** **Потерянное обновление строки: волна размера тикета и пересчёт сторон читают витрину до, а пишут после await**
      `services/universe_stream.py:410`
      И flush_vol_sizes → apply_vol_sizes, и такт → recrunch_sides работают по схеме
      «скопировать строку из universe_metrics в heavy-потоке → вернуться в петлю → _store_rows
      заменить строку целиком». Копия и запись разделены await'ом, а run_heavy отпускает петлю,
      поэтому две такие последовательности могут переплестись, и та, что вернулась позже,
      положит строку, прочитанную РАНЬШЕ. Бумага после этого ни в одну очередь не встаёт —
      событий на неё больше нет.
      СЦЕНАРИЙ: Клиент прислал по WS новый размер тикета, ws.py:214 запускает задачу
      flush_vol_sizes. Её apply_vol_sizes читает строку бумаги в момент T0 (heavy-поток
      работает, петля свободна). В T1 такт движка завершает свой recrunch_sides и кладёт в
      строку свежие yoi_bid/yoi_ask по новому верху книги. В T2 задача волны просыпается и
      делает _store_rows со своей копией от T0 — свежие спреды сторон исчезают, в витрине снова
      числа до движения книги, и до следующего движения их никто не пересчитает.
      ЛЕЧЕНИЕ: Сливать поля, а не строки: в _store_rows для флоатеров писать `um[isin] =
      {**um.get(isin, {}), **row}`, а в apply_vol_sizes возвращать только свои ключи
      (vol_px/yoi_vol) вместо копии всей строки.

- [ ] **[проверено]** **Цена набора по объёму едет без своего спреда, а на фронте старый спред не стирается — пара расходится**
      `api/routes/bonds.py:549`
      Ветка `if key in px_map:` отдаёт цену набора всегда, а спред — только если он есть (`if y
      is not None`). На фронте метрики котировок присваиваются по правилу `if (q[k] != null)
      n[field] = q[k]` (App.jsx:617-618, quotesMerge.js:66) — отсутствующее поле НЕ стирает
      прежнее. Соседняя ветка (`elif y is not None`, строки 553-558) написана правильно и даже
      объясняет почему («живую цену — только в паре со спредом»), но на числа самого движка это
      правило не распространили.
      СЦЕНАРИЙ: Тикет 5 млн. Движок положил vol_px['bid:5000000']=99.10, а
      yoi_vol['bid:5000000']=None (батч не сошёлся / цена вне сетки / перелом горизонта в
      yoi_at). Строка 545 пробует добрать из сетки — тоже None. Ответ: vol_bid_px=99.10 без
      vol_bid_y. Фронт обновляет vol_bid_price_pct=99.10, а y_idx_vol_bid_bps остаётся числом от
      ПРЕДЫДУЩЕЙ цены набора; applyVolume (vwap.js:103,105) рисует их одной парой в ячейке BID.
      ЛЕЧЕНИЕ: Привести ветку к правилу соседней: либо не отдавать vol_*_px без vol_*_y, либо
      отдавать vol_*_y: null явно и на фронте разрешить null стирать число (как это делает
      applyVolPatch на WS-пути, App.jsx:429-431).

- [ ] *[не проверялась]* **As-of расчёт истории пересобирает поток на каждую цену одного и того же дня**
      `services/backdate.py:774`
      fn(day_iso, price) из asof_bar_metrics мемоизирует по дню кривую (curve_memo, строка 767),
      но не поток: calculate_valuation_metrics зовётся без flows_cache, хотя в пределах дня
      поток зависит только от дня (кривая, оферты, номинал, НКД дня), а не от цены.
      СЦЕНАРИЙ: /history/{isin}/trades?days=150 с 500 сделками (api/routes/history.py:243-262):
      в дне с 30 разными ценами 29 пересборок потока лишние — по 1–40 мс на бумагу в heavy-
      потоке, то есть секунды блокировки ядра на один запрос ленты. То же на часовых барах (8
      баров в дне = 7 лишних сборок на каждый день окна).
      ЛЕЧЕНИЕ: day_flows: dict = {} в фабрике и flows_cache=day_flows.setdefault(day_iso, {}) в
      fn. Ключ ОБЯЗАН быть по дню: кривая, оферты и ref.face_value (мутируется на строке 754)
      меняются от дня ко дню — общий словарь на окно дал бы поток чужого дня. Плюс потолок на
      число дней в словаре, окно бывает 400 дней.

- [x] ~~ПОЧИНЕНО 03.09 (f170bb0)~~ — снимок книги один на заход. **get_depth() зовётся на КАЖДУЮ бумагу в самой горячей ветке такта**
      `services/universe_stream.py:1369`
      _fill_side_metrics зовёт `_vol_prices(isin)` без ladders, а _grid_nodes (строка 1078) —
      `depth_svc.get_depth()` без снимка. get_depth() на каждый вызов строит множество протухших
      ISIN обходом всех шардов (services/depth.py:51), а при живом протухшем шарде ещё и
      пересобирает словарь всего рынка. Это ровно тот случай, от которого в коде заведены
      параметры ladders/book и комментарии «вызов на бумагу превращает обход в квадрат»
      (_has_book, apply_vol_sizes, _vol_prices, роут котировок) — но самая частая ветка их не
      использует.
      СЦЕНАРИЙ: Такт берёт до _SIDES_BATCH_MAX=400 бумаг в recrunch_sides; при активном размере
      тикета каждая делает минимум один обход всех ISIN всех шардов (~3000) в _vol_prices, а
      бумага с построением сетки — второй в _grid_nodes. Один умерший шард превращает это в 400
      пересборок словаря глубины за такт, и цена стороны, по которой считается адаптивная пачка,
      растёт на ровном месте.
      ЛЕЧЕНИЕ: Пробросить снимок книги сверху: recrunch_sides/_crunch берут `book =
      _depth_snapshot()` один раз и передают его в _fill_side_metrics →
      _vol_prices(ladders=book.get(isin) or {}) и _grid_nodes(..., book).

- [x] ~~ПОЧИНЕНО 03.09 (f170bb0)~~ — среднее считается по реально посчитанным + нарезка по дедлайну. **Адаптивная пачка сторон не умеет уменьшаться: пол выше расчёта по бюджету**
      `services/universe_stream.py:106`
      _sides_batch() возвращает `max(_MAX_SIDES_BATCH, min(n, _SIDES_BATCH_MAX))`, где n =
      бюджет/цена. Пустой замер уже отсечён веткой выше (`if _sides_ms_avg <= 0`), поэтому пол
      100 применяется всегда и бюджет _SIDES_BUDGET_SEC работает только вверх. Поведение
      закреплено тестом (tests/test_universe_stream.py:835 «дорогая — не ниже прежнего пола»),
      то есть расходится с обоснованием в комментарии, где бюджет вводился как раз против того,
      что очередь «съедала 4–7 секунд из минуты».
      СЦЕНАРИЙ: После пересборки кривых сетки снесены, и каждая сторона тянет за собой
      построение узлов: замеренная цена уходит на 35 мс. n = 2500/35 = 71, но пачка остаётся 100
      → 3.5 с работы при бюджете 2.5 с. Такт переезжает границу, left() в _warm_pass уходит в
      минус, догрев сеток сваливается в урезанную пачку (_GRID_WARM_BATCH // 5) — и дорогая
      сторона держится дольше именно потому, что сетки не догреваются.
      ЛЕЧЕНИЕ: Развести пол и потолок: `return max(_SIDES_BATCH_MIN, min(n, _SIDES_BATCH_MAX))`
      с отдельным маленьким полом (напр. 20), оставив _MAX_SIDES_BATCH только для ветки без
      замера; тест поправить под новое правило.

- [x] ~~ПОЧИНЕНО 03.09 (f170bb0)~~ — _schedule_fp несёт маржу и весь график. **Ключ потока к оферте не содержит маржу, хотя поток от неё зависит**
      `services/valuation.py:537`
      Ключ ('main', bond.spread_issue_bps) маржу включает, а ('cut', cut) и ('cut_flat', cut, L)
      — нет, при том что в build_cashflows_with_spread на строках 538-543 и 558-563 передаётся
      та же bond.spread_issue_bps. Разные маржи в одном словаре бумаги схлопываются в одну
      запись горизонта оферты.
      СЦЕНАРИЙ: Импорт xlsx зовёт invalidate_params(None) → _flow_cache.clear()
      (universe_stream.py:1011), но идущий в этот момент проход прогрева держит уже выданный
      подсловарь бумаги и после очистки кладёт его обратно. Для ('main', маржа) новая маржа даёт
      новый ключ, и число обновится; для ('cut', дата) ключ тот же — горизонт оферты останется
      на доимпортной марже. Строка выйдет внутренне противоречивой: Y-IDX/SM к погашению по
      новой марже, к оферте — по старой, и именно горизонт оферты чаще выигрывает по правилу
      цены.
      ЛЕЧЕНИЕ: Собрать ключ из всего, от чего зависит СБОРКА: ('cut', cut,
      bond.spread_issue_bps) и ('cut_flat', cut, spread, L). Внешние измерения (день, кривая,
      расписание) держать не в ключе, а в версии кэша (фиксы №1–№3).

- [x] ~~ПОЧИНЕНО 03.09 (f170bb0)~~ — поздний НКД сносит сетку, _yoi_cache и уровни, плюс ставит строку в _dirty. **Кэш Y-IDX по набору цен переживает приход биржевого НКД — число считается по прикидке, а сетка не восстанавливается**
      `services/universe_stream.py:1391`
      В `_fill_side_metrics` приход биржевого НКД правит контекст на месте и сносит сетку
      (строки 1354-1360), но `_yoi_cache[isin]` не трогает. Ключ кэша — `(набор цен, края и
      число узлов, _yoi_cache_epoch)` — про НКД не знает ничего, хотя НКД входит в dirty, то
      есть в КАЖДОЕ число ответа. Вторая половина той же проблемы: при попадании в кэш (строка
      1391-1393) ветка `else` пропускается целиком, и `_yoi_grid[isin]` не записывается — хотя
      ключ гарантирует, что в кэшированном `got` лежат значения ровно тех же узлов, а слот
      `_grid_budget` за построение узлов уже списан в `_grid_nodes_if_needed`.
      СЦЕНАРИЙ: Бумага получила контекст из `warm_ctx` по борд-снапшоту без НКД
      (`accrued_missing=True`), и стороны посчитались по собственному начислению лестницы. Через
      20 секунд снапшот обновился с биржевым НКД; бумагу переставил в очередь
      `_blank_side_targets` (стороны и средневзвес те же). `_fill_side_metrics` честно правит
      `ev`, сносит сетку — и тут же получает попадание в `_yoi_cache` со старым набором цен: в
      строку уезжают спреды, посчитанные по ПРИКИДКЕ НКД, при том что точный НКД уже в
      контексте. Сетка при этом не восстанавливается, слот такта потрачен впустую, `yoi_at`
      возвращает None и `apply_vol_sizes` гонит бумагу по кругу — до истечения TTL 60 с.
      ЛЕЧЕНИЕ: Добавить `_yoi_cache.pop(isin, None)` рядом с `_yoi_grid.pop(isin, None)` в ветке
      прихода НКД (строка 1360), а надёжнее — внести признак НКД в ключ: `key =
      (tuple(sorted(want)), ..., _yoi_cache_epoch, ev.get("accrued_live"))`. Отдельно, в ветке
      попадания восстанавливать сетку из готового ответа: `if nodes and isin not in _yoi_grid:
      _yoi_grid[isin] = (_yoi_cache_epoch, nodes, {n: got.get(round(n, 4)) for n in nodes})` —
      работа уже оплачена, выбрасывать её незачем.

- [ ] *[не проверялась]* **Кэш-хит _yoi_cache выбрасывает уже построенные узлы сетки вместе с бюджетом такта**
      `services/universe_stream.py:1392`
      В _fill_side_metrics узлы считаются до развилки: _grid_nodes_if_needed строит список и
      уменьшает _grid_budget (потолок _GRID_BUILD_PER_TICK=12 на такт). Но _yoi_grid[isin]
      заполняется ТОЛЬКО в ветке промаха кэша. При попадании в _yoi_cache (тот же набор цен, TTL
      60 с) узлы и потраченный бюджет пропадают, хотя значения по ним лежат в got — том самом
      кэше, из которого пришёл ответ.
      СЦЕНАРИЙ: У бумаги приезжает биржевой НКД: _fill_side_metrics обновляет контекст и делает
      _yoi_grid.pop(isin). Следующий пересчёт стороны с тем же набором цен строит узлы (бюджет
      12→11), попадает в _yoi_cache и уходит, не записав сетку. Бумага остаётся без сетки,
      live_sides по ней молчит, а один из двенадцати слотов построения сетки за такт потрачен
      впустую; при живой книге это повторяется тактами.
      ЛЕЧЕНИЕ: В ветке попадания тоже класть сетку: `if nodes: _yoi_grid[isin] =
      (_yoi_cache_epoch, nodes, {n: got.get(round(n,4)) for n in nodes})` — либо не строить узлы
      до проверки кэша (перенести _grid_nodes_if_needed за развилку).

- [ ] *[не проверялась]* **Лента сделок считает цены поштучно вместо батча — своё мемо не спасает от базовой цены вызова**
      `services/trade_yidx.py:115`
      `_metrics_fn` берёт `build_metrics_fn`, то есть обёртку `levels_fn([price])` на ОДНУ цену,
      и `enrich` (строка 112-115) обходит уникальные цены через собственное мемо `(isin,
      price)`. Каждая такая цена — отдельный `calculate_valuation_metrics`, а по замеру в шапке
      `services/orderbook_svc` база вызова ~65 мс против ~0,4-0,5 мс за дополнительную цену в
      батче. Мемо снимает повторы, но не снимает главное: цены одной бумаги уже сгруппированы
      (`fns` строится по выпускам), и их можно посчитать одним проходом. Плюс ключ мемо — сырой
      float цены, тогда как внутри `levels_fn` цена всё равно округляется до 4 знаков (`_px`):
      два представления одной цены дают два полных расчёта.
      СЦЕНАРИЙ: Крупный флоатер с 40 уникальными ценами принтов в очереди `price_new_trades`:
      сейчас 40 × ~65 мс ≈ 2,6 с на одну бумагу вместо одного вызова ~65 мс + 40 × 0,4 мс ≈ 80
      мс. Именно этим объясняется замер в шапке модуля «103 выпуска с холодными контекстами — 65
      с»: холодные контексты там не единственная причина, вторая — поштучный счёт.
      ЛЕЧЕНИЕ: Хранить в `_ctx_cache` не `metrics_fn`, а `levels_fn` (`build_levels_fn`), и в
      `_calc` считать по выпуску: собрать все уникальные `round(price, 4)` строк этого ISIN,
      один вызов `levels_fn(prices)`, дальше раскладка по строкам. Мемо тогда становится
      результатом одного вызова, а ключ — округлённой ценой, совпадающей с внутренним `_px`.

- [ ] *[не проверялась]* **Неудачная сборка контекста стакана в alor_ws не запоминается — попытка повторяется на каждом пуше книги**
      `services/alor_ws.py:74`
      `_ensure_ctx` обновляет `sub.ctx_ts` только на успехе (строка 75). Если `build_levels_fn`
      бросил — экзотика, бумаги нет в реестре, кривая не собралась, — `sub.ctx_ts` остаётся
      прежним, условие раннего выхода `now - sub.ctx_ts < _CTX_TTL` не выполняется, и полная
      пересборка контекста (`load_reprice_ctx`, gather из шести источников +
      `create_bond_ref_data` + `reconcile_face`) запускается заново на КАЖДОМ пуше стакана — то
      есть раз в 800 мс на подписанную бумагу, в event loop, с логированием на уровне debug (в
      проде выключен, то есть молча).
      СЦЕНАРИЙ: Пользователь открыл карточку бумаги, у которой `load_reprice_ctx` падает (нет в
      isins_cache и MOEX её не отдаёт). Подписка живёт, книга пушится 75 раз в минуту — и
      столько же раз запускается сборка контекста. Наружу это выглядит как «стакан без метрик»,
      а в профиле — как постоянная нагрузка в петле без единой строки в логе.
      ЛЕЧЕНИЕ: Отмечать попытку независимо от исхода: завести `sub.ctx_try = now` и выходить
      рано, если `now - sub.ctx_try < backoff` (короткий бэкофф, например 30 с, растущий до
      `_CTX_TTL`). Заодно поднять уровень лога до warning с троттлингом по ISIN — сейчас причина
      отказа не видна нигде, как это было с отказами `y_idx_many` до `_warn_once`.

- [x] ~~ПОЧИНЕНО 03.09 (f170bb0)~~ — цена расчёта пишется в строку и едет наружу; клиент сверяет. **Пара «средневзвес → yoi_wap» тоже собирается из двух моментов**
      `api/routes/bonds.py:456`
      it["wap"] = свой тиковый VWAP (live_quotes, движется на каждой сделке), it["yoi_wap"] =
      число движка, посчитанное в _fill_side_metrics к ЕГО значению wap на момент такта. Цена
      расчёта наружу не едет, метки устаревания у колонки нет, на фронте QUOTE_METRIC_FIELDS
      присваивает yoi_wap безусловно. В WS-ветке то же самое: _METRIC_FIELDS везёт yoi_wap, а
      wap в патч не входит вовсе, зато q.vwap_pct применяется сразу (App.jsx:459).
      СЦЕНАРИЙ: В неликвиде утренний VWAP 99.10, движок посчитал yoi_wap к нему. Днём проходит
      крупный принт по 100.30, тиковый VWAP скачком уезжает на 100.05; движок пересчитает бумагу
      своим тактом, но до этого /api/bonds/quotes отдаёт wap=100.05 и yoi_wap от 99.10 —
      аналитика (она считает по средневзвесу, а не по last) получает согласованную на вид и
      неверную пару.
      ЛЕЧЕНИЕ: Возить цену расчёта рядом — it["yoi_wap_px"] = цена wap, по которой считал движок
      (её достаточно положить в строку в _fill_side_metrics), — и на фронте применять по
      совпадению, как сделано для сторон.

- [ ] *[не проверялась]* **Потолок кэша потоков ≈118 МБ, а подрезка спрятана внутрь минутной сводки**
      `services/universe_stream.py:217`
      _FLOW_CACHE_MAX_ITEMS=400000 платежей: замер tracemalloc даёт 294 байта на Cashflow
      (dataclass без __slots__, три даты и три float), то есть потолок — ~118 МБ до первого
      срабатывания, при том что контейнер уже ловил OOM на 768 МиБ. Сам _trim_flow_cache
      вызывается только внутри ветки минутной сводки (строка 2286), которая выполняется лишь
      если за минуту была работа (`if (done_since_log or sides_since_log) and ... >= 60`).
      СЦЕНАРИЙ: Стационар витрины (1148 бумаг × ~38 платежей × 2 ключа ≈ 87к платежей ≈ 26 МБ)
      до потолка не доходит, но карточка/лента/стакан кладут ещё горизонт оферты и два плоских
      потока марж, а 94 длинные бумаги дают по 200–370 платежей на ключ — при таком составе кэш
      подбирается к сотне мегабайт молча, и в тот момент, когда память и так на пределе,
      срабатывает сброс ЦЕЛИКОМ: весь рынок уходит в холодный догрев.
      ЛЕЧЕНИЕ: Опустить потолок до ~120к платежей (≈35 МБ, с запасом над стационаром) и вынести
      _trim_flow_cache из ветки сводки — звать его в каждом такте безусловно (или вести счётчик
      платежей инкрементально при записи, чтобы не проходить кэш целиком).

### ✅ Проверено замерами и в порядке (02.09)

- доставка «посчитано → показано»: из 588 строк на экране **ноль** случаев, где
  число есть на сервере, а ячейка пуста;
- пары «цена + спред» на пути котировок согласованы: рассинхрон 0 из 577 строк со
  спредом стороны, битых пар набора по объёму 0;
- кэш потоков полон: 611 бумаг, 23к платежей, контексты 611;
- остаточные прочерки — 19 строк, все дефолтные выпуски (цена 1,8–29 % номинала).

**ПОЧИНЕНО 03.09:** сверка цены в `sideMetricPatch` переведена с `1e-9` на
округление до 3 знаков — канонический ключ цены (`universe_stream._px_key`).

Отдельная находка замера, не из аудита: `sideMetricPatch`
(frontend-react/src/quotesMerge.js:103) сверяет цену с точностью `1e-9` и отвергает
готовый спред при расхождении в незначащих знаках — 2 строки из 588 держали
приглушённое число при готовом сервере. Канонический ключ цены в проекте —
округление (`_px_key`, 3 знака), сверять надо по нему.

---

## ⏭️ Следующий заход — начни отсюда

**Аудит конвейера 03.09 на проде** (`f170bb0`). Смотреть глазами в первую очередь
— правки про «перестаёт» и про приглушение подтверждаются только наблюдением:

- [ ] **Доля спреда по СРЕДНЕВЗВЕСУ.** После деплоя 72 % против 75,6 % до него.
      Ожидаемо: новая цена средневзвеса теперь ГАСИТ спред и уводит его в
      `y_idx_wap_stale`, пока движок не пересчитает (раньше в ячейке оставалось
      число к прежней цене, без пометки). Если за торговый день доля не вернётся
      к прежней — очередь не успевает, и стоит поднять приоритет пересчёта
      средневзвеса или считать его чаще.
- [ ] **Приглушённые ячейки.** В день деплоя единицы (bid 3, ask 0, wap 4). Если
      их станет десятки — сверка цены расчёта отбраковывает больше, чем должна:
      искать в паре «цена в строке ↔ `yoi_*_px`», кто из двух отстаёт.
- [ ] **Холостые перестройки сеток.** 17-29/мин при неизменных 593 (было 25-59).
      Остаток — либо законные расширения под уехавшую цену набора, либо ещё одно
      поле, меняющее `_grid_ctx_key` без нужды. Мерить по сводке движка.
- [ ] **Хиты кэша уровней.** Правка переставила `_store_eval_ctx` ПЕРЕД записью
      строки в `_level_memo` (иначе снятие кэшей сносило её же). Проверить, что
      hit-rate вышел на прежние ~12 %, а не на ноль.

Что стоит посмотреть глазами (правки про «перестаёт»):

- [ ] **N4 — R-spread в мониторе.** Если число снова замрёт при живой цене,
      остаётся второй подозреваемый: событийный пересчёт бэка не будится
      движением bid/ask (`universe_stream`: «dirty только по смене цены сделки»).
      Это осознанное решение ради нагрузки, менять его — отдельный разговор.
      С 03.09 движение ГЛУБИНЫ книги пересчёт заказывает (видимые бумаги,
      активный фильтр объёма, дебаунс 20 с) — сам верх книги по-прежнему нет.
- [ ] **Фиксы на средневзвесе** — что `g_spread_wap_bps` доезжает в строку и
      графики встали на него (единственный непроверенный на живом пункт N3).
- [ ] **Пикер фильтров в ленте** — удобен ли порядок и хватает ли расшифровки.

### Замеры 26.08 (закрытые P0)

- **Свежесть ленты: 95,5 %** живьём за 8 часов (763 сделки), 4 часа — 95,2 %.
  До правок 25.08 было 81 %, цель ставили ≥90 %. Часовое окно 88 % — норма,
  свежий хвост ещё досылается. Мерить: `bt.live_capture(480)` в контейнере.
- **Канал «Р5» (фильтр id=9) получил первое событие** 26.08 в 10:50. До фикса
  `1b358ce` — ноль за всё время. Дедуп по каналам работает.
- **РПС (`market=ndm`) остаются +15 мин НАВСЕГДА** — подписки у брокера нет.
  Не баг, не переоткрывать.

## 🆕 Заход 26.08 — новые задачи (с приоритетом)

Приоритет сквозной, вместе с уже лежащими в списке пунктами: см. таблицу
«Сводный приоритет» ниже.

### N1. Цвет сделки в окне уведомлений — `P1`
- [ ] Заливка фона строки с прозрачностью: **покупка — зелёная**, **продажа —
      красная**, **РПС — голубая**. Не текст, а фон всей строки (чтобы читалось
      боковым зрением, как в терминале).
- Где: `frontend-react/src/components/SignalsBell.jsx` (рендер строки события,
  уже зовёт `sideInfo(e)` из `src/signalFormat.js`).
- Данные есть: `block_trade.side` (`'buy'/'sell'`, `services/block_trades.py:164
  _side()` из `BUYSELL` безадресной ленты) и `market` (`'ndm'` = РПС).
- **Каветат:** у адресных сделок стороны нет в принципе — поэтому у РПС свой
  цвет, а не «серый без стороны». Проверить, что `side`/`market` доезжают до
  события уведомления (`signal_events`), а не только до ленты СДЕЛКИ.
- Проверить читаемость в обеих темах и на подсвеченной (сработавшей) строке —
  красный «сработал алерт» не должен путаться с красным «продажа».

### N2. Как и когда новый выпуск попадает в монитор — `P0` — СДЕЛАНО 26.08
- [x] Разобрано. Discovery отработала **в день размещения** (РЖД 1Р-54R:
      `discovery_seen.is_floater=1` и `instruments.first_seen` = 13.08 13:04).
      Бумага не показывалась потому, что **непрайсуема**: без `base`/`margin_bps`
      её выкидывает `universe_rows(only_priceable=True)`
      (`services/instruments_registry.py`). Ни одна из трёх гипотез не
      подтвердилась: negative-кэш не при чём, голодания очереди нет
      (`enrich_pending` ставит «никогда не пробованные» первыми), попытка
      обогащения была через 24 минуты после заведения — вердикт `nodata`.
      Причина: **corpbonds не знает свежих выпусков**, а TTL перепопытки был
      14 дней. Следующая автопопытка пришлась бы на 27.08.
- [x] **Формула купона теперь берётся у самой биржи.** MOEX ISS отдаёт
      `COUPON_BENCHMARK` + `COUPON_BENCHMARK_SPREAD` в карточке
      `/iss/securities/{isin}.json` — мы эти поля просто не читали. У РЖД 1Р-54R
      там `RUONIA + 1.90`, у ДОМ 2P10 — `RREFKEYR + 1.20`. Коды: `RREFKEYR` =
      ключевая ставка, `RUONIA`, `ZR_YLD_CRV` = G-кривая (→ `set_exotic`, вне
      линейной модели). Покрытие: свежие корпоративные — да, СФО/ВДО (ТБ-10,
      Филберт, ВИС) — нет, у них поле пустое.
- [x] **TTL перепопытки для свежих выпусков** — сутки вместо 14 дней
      (`_FRESH_ISSUE_DAYS`=45, `_FRESH_ISSUE_TTL_DAYS`=1), и свежие идут впереди
      хвоста очереди (иначе квоту 30/прогон съедает ротация старого бэклога).
      Только для исходов `nodata`/`not_found` — это вердикт про ИСТОЧНИК;
      `exotic`/`filled` от возраста выпуска не зависят.
- [x] **`issue_date` при дискавери** — из самого раннего купона графика. Листинг
      MOEX даты размещения не отдаёт вовсе, поэтому `mo.get("issue")` там всегда
      был None и «свежесть» было нечем мерить: из 521 непрайсуемой дата известна
      у 30. Плюс добор в справочнике MOEX (`_MAX_SECMASTER_PER_RUN`).
- [x] **Список свежих непрайсуемых на /status** — поимённо, с датой размещения и
      возрастом, ссылкой в Справочник. Счётчик очереди на вопрос «где моя
      бумага?» не отвечает.
- [x] **Проверено на проде** (деплой `2d3b15b`, синк 26.08 11:28: `enriched 140`).
      ДОМ 2P10 → `KEYRATE + 120` автоматически, даты размещения проставились.
      РЖД 1Р-54R на проде `manual_locked`, но его ручной ввод (`RUONIA + 190`)
      совпал с карточкой биржи ровно — источник сверен.
- [ ] **Осталось руками — 16 свежих без параметров** (список на /status).
      У ВСЕХ шестнадцати `COUPON_BENCHMARK` у биржи пуст (проверено запросом),
      т.е. автоматика их не закроет: ТБ-10_А1/А2, РКСОлм×3, ВИС Ф БП15,
      РусУдоб1Р1, Филберт01, ЭконЛиз2Р3, СТМ 1Р8/1Р9, НеваФин01, Сплит ПВ-3,
      СБСекр5АР1/АС и **РостелP28R (42 дня без базы, крупный корп)**.
      Секьюритизация/ВДО биржа формулой не описывает — только проспект.
- [ ] Мелочь на будущее: `count()['incomplete']` = 4, а `queue_stats()` = 349 —
      два разных счётчика с одним именем на одном экране админа. Не баг расчёта,
      но читателя путает.
- [ ] Отдельным вопросом: **smart-lab как источник формулы — мёртв.** Страница
      выпуска не содержит ни базы, ни спреда (проверено на трёх бумагах, HTTP 200,
      совпадений по «ключевая ставка»/RUONIA/спред — ноль). Он остаётся только
      источником рейтинга и типа купона.
- [ ] Инференс по одному купону (когда биржа молчит) — **не годится**: при плоской
      КС маржа под KEYRATE выходит «круглой» автоматически, и эвристика
      округления выбрала бы KEYRATE для РЖД 1Р-54R, у которого на самом деле
      RUONIA. `scripts/infer_base_margin.py` не зря требует 3 выплаченных купона.

### N3. Аналитика строится от средневзвешенных цен — `P1` — СДЕЛАНО 26.08
- [x] **Проверено: базы разъезжались.** У флоатеров scatter и box считались по
      средневзвесу, а линия «R-spread ДИНАМИКА» читалась из `spread_daily` — там
      спред по цене ЗАКРЫТИЯ (вечерний снапшот или honest-бэкфилл по close
      свечи), — и при этом её сегодняшнюю точку фронт достраивал опять по
      средневзвесу: две базы на одном графике, три на вкладке. Разница по бумаге
      4–10 бп, по медиане бакета до 18 бп (замер 17–26.08 на проде).
- [x] Динамика переведена на `bar_daily` (свёртка баров, средневзвес). Побочно
      ушли **строки за дни без сделок**: `spread_daily` заводит строку на каждый
      календарный день, включая выходные (523 строки за субботу 16.08), и линия
      шла по стейл-цене.
- [x] Для этого починена сама свёртка: **часы несут горизонт с версии движка 6,
      а `build_daily` его терял**. Без горизонта линию по средневзвесу строить
      нельзя — спред к оферте сложился бы со спредом к погашению (обвал на
      220 бп без движения цены). Рядом лежит спред ко второму горизонту,
      взвешенный тем же оборотом. Задеплоено `d45f322` + разовый прогон
      `scripts/backfill_daily_bars.py --days 400 --force` (1340 бумаг,
      292 176 дней): горизонт есть у 98,6 % строк с 2025-09.
- [x] **Вкладка ФИКСЫ** тоже переведена на средневзвес (`6e01d70`): `WAPRICE`
      борда шёл только фолбэком для `last`, теперь отдельным полем; g-спред по
      нему считается прямым пересчётом (700 бумаг 128 → 256 мс, heavy-поток).
      Свой тиковый средневзвес впереди биржевого. **НЕ ЗАДЕПЛОЕНО** — ждём,
      пока вторая сессия закончит работу над телеграм-дайджестом: `deploy.sh`
      синкает дерево целиком, а её `api/main.py` уже импортирует код, чей
      Pillow в прод-`requirements.txt` ещё не попал.
- [ ] Не проверено на живом: фиксы после деплоя — что `g_spread_wap_bps`
      приходит в строке и графики встали на него.

### N4. Спред врал до перезагрузки страницы — `P0`
- [ ] Воспроизвести и починить: 26.08 спред показывался неверно, пока страницу
      не обновили руками. То есть **клиентское состояние расходится с сервером** —
      либо кэш react-query не инвалидируется, либо WS-апдейт приносит частичные
      поля и затирает пересчитанные.
- Что проверить в порядке дешевизны:
  1. `staleTime`/`refetchInterval` витрины и карточки — не отдаётся ли
     дореплойный/довечерний снапшот из кэша query.
  2. WS-канал рынка: мерж апдейта в строку — не перетирает ли он `dm`/`y_idx`
     старыми значениями, когда апдейт пришёл без метрик.
  3. Смена **горизонта** (put/call vs погашение) и смена **базы цены** — не
     остаётся ли метрика от прошлого выбора (см. `pricing-horizon-rule`).
  4. Стейл-цена: спред мог быть посчитан от вчерашней цены, а после F5 — от
     сегодняшней (тогда баг не в фронте, а в свежести цены).
- Нужен точный сценарий воспроизведения (какая вкладка, какая бумага, что
  показывало до F5 и после) — без него легко починить не то.

### Сводный приоритет (новые + существующие)

| P | задача | почему здесь |
|---|---|---|
| ~~P0~~ | ~~Чистый замер свежести ленты~~ | СДЕЛАНО 26.08: 95,5 % живьём (было 81 %) |
| ~~P0~~ | ~~**N4** спред врал до F5~~ | СДЕЛАНО 26.08 (`6fd82dd`): котировки везли метрики только небиржевым бумагам. Задеплоено, нужно наблюдение |
| ~~P0~~ | ~~**N2** новый выпуск не доезжает 12 дней~~ | СДЕЛАНО 26.08: формула из карточки MOEX + суточный TTL + список на /status. Осталась проверка на проде |
| **P0** | TZ-фикс проверить на живом контейнере (аудит 25.08, «Не закрыто») | фикс задеплоен, но не подтверждён; без `tzdata` он молча нерабочий |
| ~~P1~~ | ~~**N3** база цены в Аналитике~~ | СДЕЛАНО 26.08: флоатеры задеплоены, фиксы ждут деплоя (в дереве работа второй сессии) |
| ~~P1~~ | ~~**N1** цвета сделок в уведомлениях~~ | СДЕЛАНО 26.08 (`e9aad27`), задеплоено |
| **P1** | Курс для остальных валют номинала (`no_fx`, CHF) | сделка есть, звонка нет — тихая потеря сигнала |
| **P1** | Рублёвый объём валютных бумаг в `trade_tick` | подпирает N3 (VWAP замещаек) |
| **P2** | Ретеншен архива тиков / рост базы 2,8 ГБ | сторож звонит, падения не будет; решение «что теряем» — за пользователем |
| **P2** | Правило «шард молчит НЕнормально» | доставка тревог готова, осталось правило |
| **P2** | `pending_alerts`: отбор по `cur` пересмотреть | после фикса курса отбор стал непоследовательным |
| **P2** | Хвост спек фиксинга (ПионЛиз, 10 KEYRATE-бумаг) | 0,15–0,75 пп, точечно и дорого |
| **P2** | `bars.py:1016` face последнего бара, `metrics.py:122` breakeven мимо лесенки | из внешнего аудита, не проверены, дёшевы |
| **P3** | non-root контейнер, циклический импорт core↔services, 5 cashflow-builder'ов | техдолг без внешнего эффекта |


## 🔴 Стакан + алерты (новая крупная фича)

### Стакан выпуска (order book)
- [x] Кнопка открытия стакана **рядом с названием** вверху карточки облигации.
- [x] Панель стакана открывается **слева** от карточки (вторая выезжающая панель).
- [x] Данные: `GET /api/orderbook/{isin}`. Live-обновление — **поллинг 3с** (react-query refetchInterval). Alor WS — TODO (оптимизация нагрузки).
- [x] **Для КАЖДОГО уровня цены** (bid и ask) считать **SM и DM** — бэкенд готов: `services/bond_details.load_reprice_ctx` (тёплый ctx один раз) + `reprice_at_price(ctx, price)` (чистая, батчится по уровням). `/orderbook` считает SM/DM per-level с полными amorts/offers.
- [x] Колонки уровня: цена · объём (шт; ₽ в title) · SM · DM · YTM.
- [~] Работает для флоатеров (SM/DM/YTM). Для фиксов: кнопка есть, но per-level SM/DM = None (base не RUONIA/KEYRATE) → показывается только YTM. Проверить на живом фиксе; при желании g-спред per-level.

### Алерты по стакану — СДЕЛАНО (2026-07-27, задеплоено)
- [x] **Тип 1 — форма:** объём + цена/YTM/DM(g-спред) + сторона. `OrderbookAlerts.jsx` в панели стакана. op авто-выводится из стороны+метрики.
- [x] **Тип 2 — Ctrl/Cmd-клик** по уровню → префилл формы (сторона+цена уровня). Объём вводит юзер.
- [x] Мониторинг: `alerts_monitor` в main.py, каждые 12с в торговые часы, батч по (isin,kind), матч против Alor-стакана «на уровне/лучше» + накопленный объём шт/₽. Нотификация: `AlertsWatcher` (тост+бип WebAudio, поллинг 8с). WS-push/e-mail — позже.
- [x] Хранение: portfolio.db v3 таблица alerts, per-user. `services/alerts.py` CRUD + evaluate. `api/routes/alerts.py` GET/POST/DELETE.
- [x] Состояния: active/fired/cancelled + история (list_for_user).
- [x] Полировка: редактирование алерта (PATCH), подсветка сработавшего уровня в стакане (красный ⚠), плашка в StatusBar + попап + клик→карточка/стакан, красная строка бумаги. Задеплоено 2026-07-27.
- [x] **Alor WS реал-тайм стакан** — `services/alor_ws.py` персистентный WS, reprice+memo, broadcast через канал orderbook; фронт `connectOrderbookWs`, HTTP-поллинг фолбэком. Формат Alor подтверждён. Задеплоено 2026-07-27.
- [x] Telegram-push — СДЕЛАНО: сигналы дублируются в привязанный чат
  (`services/tg_notify.py`), задержка ~1–4 с; безадресные сделки звонят с живого
  потока Alor, а не с ISS-ленты. Свои эмодзи маркеров — `/custom`, отдельный
  канал на фильтр («Р5» → канал «Р5») — `/chats` + селектор в форме сигнала.
  Подробности и каветаты: STATUS.md, «Изменения на проде 2026-08-21, вечер».
- [x] Один принт доходит до КАЖДОГО канала (`1b358ce`, 25.08): дедуп блоков был
  по пользователю, широкий фильтр забирал сделку у узкого, и канал «Р5» не
  получил ни одного события за всё время. Ключ теперь `(пользователь, адресат)`.
- [ ] Хвосты: WS-push алертов (мало смысла — монитор 12с); e-mail-push (отложено).

**Заметки по реализации:**
- [x] SM/DM per-level: чистая `reprice_at_price(ctx, price)` вынесена из reprice (тёплый ctx → быстро для 10-50 уровней).
- Мониторинг: отдельный воркер в поллере, который для watch-алертов тянет стакан и матчит. Не долбить Alor на каждый алерт — батчить по isin.

---

## 🟡 Флоатеры — техдолг

### Расчёт (точность, низкий приоритет)
- [x] RUONIA daily-comp — ЗАКРЫТ 2026-08-21 бэктестом по факту выплат
  (`scripts/verify_fixing_specs.py`): индексные купоны (ВЭБ.РФ, Роснефть,
  ОФЗ 29026+) уже считаются капитализацией через `spec.compounded`, медиана
  ошибки по 99 RUONIA-бумагам 0.013 пп. Врал сам прибор — терял флаг в pspec
  (11 ложных WARN, коммит `f7a708c`).
- [ ] ПионЛизБР5 / ПионЛизБО6 (RUONIA): спека НЕ подобрана — 1.6–1.9 пп ошибки
  в ставке купона. `default(fwd~avg)` и `avg_window=1`, текста проспекта в базе
  нет. Лечится подбором (`scripts/fit_spec.py`) или ручной спекой.
- [ ] Хвост KEYRATE-расхождений (10 бумаг, 0.15–0.75 пп): ДОМ.РФ15об,
  ТрансФ1P05, КВС об.01, Ситимат01/Б2, ПушкПЗ1Р03, АЛЬФАT2CR6, ГТЛК 2P-01,
  РСХБ2Р31, МБЭС 2P-03. Старые выпуски со ступенями/экзотикой.
- [ ] `cut_at_offer` reset-детект мимо не-Cbonds бумаг → старый спред за reset.
- [ ] `yield_over_index` для амортизаторов apples-to-oranges (индекс роллится до дальнего погашения) — вторичка.
- [ ] Settle-модель: календарный T+1 vs бизнес — нужен бэктест (референс был НРД, off → отложено бессрочно).

### Чистка / рефактор
- [x] Мёртвый код — ПРОВЕРЕНО (список устарел): `get_cashflow_items`/`get_moex_coupon_fallback`/`searchBonds`/роут `/valuation` уже удалены; `calculate_floater_metrics` живой (`last_prices.py:301`); `solve_z_bps` живой (тест + `scripts/nrd_pipeline_probe.py`). Удалять нечего.
- [x] `/orderbook` не передавал amorts/offers → расходился с карточкой — ПОЧИНЕНО (reprice_at_price). Осталось `/valuation` роут (фронт не зовёт).
- [ ] Core↔services циклический импорт (держится на lazy-import) — landmine рефактора.
- [ ] 5 cashflow-builder'ов — консолидация (частично слито).

### Безопасность
- [x] login rate-limit по proxy-IP: был XFF, но брал ЛЕВЫЙ (спуфабельный) адрес → обходился ротацией. ПОЧИНЕНО: правый адрес (Caddy-added, edge). backoff 5→300с→…→1ч cap.
- [ ] Полный non-root контейнер (сейчас `cap_drop:ALL`+`no-new-privileges`; `data/` root bind-mount → нужен gosu-entrypoint).

### Живая лента и сигналы (после захода 25.08)
- [ ] Курс не для всех валют номинала: `services/fx` знает USD/CNY/EUR, а
      бумаги в CHF (сегодня 2 выпуска) дают `fx_ok=False` — тик пишется в
      архив, звонок не идёт. Лечится добором курса ЦБ по остальным валютам;
      счётчик пропусков — `no_fx` в `/api/status`.
- [ ] `trade_tick` хранит объём валютных бумаг В ВАЛЮТЕ НОМИНАЛА (пересчёт по
      курсу сделан только в стриме, для порога и звонка). Часовые бары и VWAP
      по замещайкам из-за этого не в рублях — проверить, где это вылезает на
      витрине, прежде чем чинить.
- [ ] `pending_alerts` отбирает `cur IS NULL OR cur='SUR'`, а живые тики Alor
      пишутся с `cur='SUR'` жёстко (`ingest_ticks`). Теперь, когда рублёвый
      объём валютных сделок считается верно, отбор стоит пересмотреть — иначе
      ISS-копия той же сделки с настоящим `cur` ведёт себя иначе, чем тик.
- [ ] Сторож шардов: счётчики есть, тревоги нет. Тихо молчащий шард ловится
      глазами по сводке лога (`БЕЗ ЕДИНОГО ТИКА: [id]`). Автотревогу не делал
      осознанно — хвост рынка неликвиден, ложняки; при желании привязывать не к
      тикам, а к сверке с ISS. С 25.08 доставка тревог готова и переиспользуема
      (`api/main._watch_alert` + `tg_notify.notify_admins`), так что осталось
      только правило «шард молчит НЕнормально» — оно и было настоящей задачей.

### Место на диске (после захода 25.08)
- [x] Тихий отказ обслуживания — ЗАКРЫТ: `disk_watchdog` звонит админам, когда
      свободного меньше `DISK_NEED_RATIO`×размер базы или свежей копии нет
      дольше `BACKUP_STALE_HOURS`. Пропущенный ночной VACUUM сообщается отдельно.
- [ ] САМ РОСТ базы не лечится сторожем. На 25.08: `portfolio.db` 2,8 ГБ +
      копии 1,6 ГБ, свободно 4,9 ГБ из 30 (83 %), рост десятки МБ в день.
      Решение — окно сырых тиков в `trades_archive.prune`: сколько дней держим
      поштучно и с какого объёма. Это выбор («что мы готовы потерять»), а не
      техработа, поэтому не делается само.

### Данные / инфра
- [x] Рейтинг-драйн: negative-кэш 404 (miss-маркер, _NEG_TTL 1д) + драйн 24/7 + батч /api/fixed + дедуп по реестру — СДЕЛАНО (коммиты perf(ratings)/fix(ratings)).
- [ ] Свежесть КС/RUONIA/cbr_forecast в UI (сейчас частично в `/meta` warnings).
- [ ] Хардкоды руками: `_CB_MEETINGS`, MOEX-праздники, `cbr_forecast.json` (обновлять после заседаний ЦБ).
- [ ] Суборд-исключение из B-бакета — нет флага в MOEX (блокировано данными).

### Аналитика (если шире)
- [x] Динамика спредов — `SpreadHistory.jsx` + `/api/history/{isin}/spread`: свечи 1d × reprice под текущий ctx → серия DM(флоатер)/g-спред(фикс) за 3м/6м/1г в карточке. Оценка (историч. цена × текущая модель). Задеплоено 2026-07-27.
- [x] Динамика спредов: candle-оценка (историч. цена × текущая модель) + интерактив (hover) + **точная история** (forward daily snapshot spread_daily, снапшоттер 19:00 МСК, эндпоинт мержит exact+est). Задеплоено 2026-07-27.
- [ ] Ещё аналитика: тепловая карта рейтинг×дюрация, скрин по carry/Y-IDX, экспорт CSV.

---

## 🟢 Фиксы — хвосты
- [x] Аналитика фиксов (scatter g-спред/дюрация, box-распределение по рейтингам/эмитентам, гистограмма срочности). Тоггл Список/Аналитика. `FixedAnalytics.jsx`. Задеплоено 2026-07-26.
- [x] Карточка фикса: reprice-калькулятор цены (как у флоатеров). `compute_fixed_row(price_override=)` + `GET /api/fixed/{isin}/reprice` + инпут в FixedCard. Задеплоено 2026-07-26.
- [x] Рейтинг: 2-й источник **smart-lab.ru** подключён (`enrich_smartlab.py`, фолбэк в `ratings.refresh` при промахе corpbonds). MOEX ISS рейтинг не отдаёт. Тест 8/8 мелких ВДО. Драйн перебирает ~784 непокрытых, покрытие растёт (смотреть СТАТУС).

---

## Выкинуто осознанно
Фонды (убрали), НРД-сравнения (удалили слой), z-перцентиль/Δz D-D (были на мёртвом НРД-z).
