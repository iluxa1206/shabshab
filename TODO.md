# TODO — shabshab (desk)

Ветка: `floaters-desk-deploy`. Прод: assetallocator.ru/desk.

---

## ⏭️ Следующий заход — начни отсюда

**Проверить чистый замер свежести ленты** (правки 25.08 вечером, детали в
STATUS.md → «Изменения на проде 2026-08-25, вечер»). До правок живьём ловилось
81% крупных биржевых сделок; замер сразу после деплоя (77%) грязный — окно
захватывало дореплойный час и рестарт. Взять полную сессию 26.08:

```bash
ssh root@161.104.17.23
cd /root/floaters && docker compose -f docker-compose.prod.yml exec -T floaters \
  python -c "from services import block_trades as bt; print(bt.live_capture(60))"
# ждём ratio ≥ 0.9; ниже 0.5 сторож напишет в телеграм сам
docker logs --since 30m floaters-prod-floaters-1 | grep "trades stream"
# ждём «шарды 13/13 живы» без хвоста «БЕЗ ЕДИНОГО ТИКА»
```
Если доля не выросла — искать третью причину потерь: сокеты живы и подписки
принимаются (проверено пробой 25.08, брокер отвечает `httpCode 200`), значит
теряется на нашей стороне. Смотреть `no_fx` и `skipped_small` в `/api/status`.

- [ ] Канал «Р5» должен наконец получать блоки ≥50 млн (фильтр id=9 — 0 событий
      за всё время до фикса `1b358ce`). Первое срабатывание = подтверждение.
- [ ] РПС (`market=ndm`) остаются +15 мин НАВСЕГДА — подписки у брокера нет.
      Если это мешает, единственный путь — платный источник, не наш код.

---

## 🆕 Заход 26.08 — новые задачи (с приоритетом)

Приоритет сквозной, вместе с уже лежащими в списке пунктами: см. таблицу
«Сводный приоритет» ниже.

### N1. Цвет сделки в окне уведомлений — `P1`
- [ ] Заливка фона строки с прозрачностью: **покупка — зелёная**, **продажа —
      красная**, **РПС — голубая**. Не текст, а фон всей строки (чтобы читалось
      боковым зрением, как в терминале).
- Где: `frontend-react/src/components/SignalsBell.jsx` (рендер строки события,
  уже зовёт `sideInfo(e)` из `src/signalFormat.js`).
- Данные есть: `block_trade.side` (`'buy'/'sell'`, `services/block_trades.py:164
  _side()` из `BUYSELL` безадресной ленты) и `market` (`'ndm'` = РПС).
- **Каветат:** у адресных сделок стороны нет в принципе — поэтому у РПС свой
  цвет, а не «серый без стороны». Проверить, что `side`/`market` доезжают до
  события уведомления (`signal_events`), а не только до ленты СДЕЛКИ.
- Проверить читаемость в обеих темах и на подсвеченной (сработавшей) строке —
  красный «сработал алерт» не должен путаться с красным «продажа».

### N2. Как и когда новый выпуск попадает в монитор — `P0` — СДЕЛАНО 26.08
- [x] Разобрано. Discovery отработала **в день размещения** (РЖД 1Р-54R:
      `discovery_seen.is_floater=1` и `instruments.first_seen` = 13.08 13:04).
      Бумага не показывалась потому, что **непрайсуема**: без `base`/`margin_bps`
      её выкидывает `universe_rows(only_priceable=True)`
      (`services/instruments_registry.py`). Ни одна из трёх гипотез не
      подтвердилась: negative-кэш не при чём, голодания очереди нет
      (`enrich_pending` ставит «никогда не пробованные» первыми), попытка
      обогащения была через 24 минуты после заведения — вердикт `nodata`.
      Причина: **corpbonds не знает свежих выпусков**, а TTL перепопытки был
      14 дней. Следующая автопопытка пришлась бы на 27.08.
- [x] **Формула купона теперь берётся у самой биржи.** MOEX ISS отдаёт
      `COUPON_BENCHMARK` + `COUPON_BENCHMARK_SPREAD` в карточке
      `/iss/securities/{isin}.json` — мы эти поля просто не читали. У РЖД 1Р-54R
      там `RUONIA + 1.90`, у ДОМ 2P10 — `RREFKEYR + 1.20`. Коды: `RREFKEYR` =
      ключевая ставка, `RUONIA`, `ZR_YLD_CRV` = G-кривая (→ `set_exotic`, вне
      линейной модели). Покрытие: свежие корпоративные — да, СФО/ВДО (ТБ-10,
      Филберт, ВИС) — нет, у них поле пустое.
- [x] **TTL перепопытки для свежих выпусков** — сутки вместо 14 дней
      (`_FRESH_ISSUE_DAYS`=45, `_FRESH_ISSUE_TTL_DAYS`=1), и свежие идут впереди
      хвоста очереди (иначе квоту 30/прогон съедает ротация старого бэклога).
      Только для исходов `nodata`/`not_found` — это вердикт про ИСТОЧНИК;
      `exotic`/`filled` от возраста выпуска не зависят.
- [x] **`issue_date` при дискавери** — из самого раннего купона графика. Листинг
      MOEX даты размещения не отдаёт вовсе, поэтому `mo.get("issue")` там всегда
      был None и «свежесть» было нечем мерить: из 521 непрайсуемой дата известна
      у 30. Плюс добор в справочнике MOEX (`_MAX_SECMASTER_PER_RUN`).
- [x] **Список свежих непрайсуемых на /status** — поимённо, с датой размещения и
      возрастом, ссылкой в Справочник. Счётчик очереди на вопрос «где моя
      бумага?» не отвечает.
- [x] **Проверено на проде** (деплой `2d3b15b`, синк 26.08 11:28: `enriched 140`).
      ДОМ 2P10 → `KEYRATE + 120` автоматически, даты размещения проставились.
      РЖД 1Р-54R на проде `manual_locked`, но его ручной ввод (`RUONIA + 190`)
      совпал с карточкой биржи ровно — источник сверен.
- [ ] **Осталось руками — 16 свежих без параметров** (список на /status).
      У ВСЕХ шестнадцати `COUPON_BENCHMARK` у биржи пуст (проверено запросом),
      т.е. автоматика их не закроет: ТБ-10_А1/А2, РКСОлм×3, ВИС Ф БП15,
      РусУдоб1Р1, Филберт01, ЭконЛиз2Р3, СТМ 1Р8/1Р9, НеваФин01, Сплит ПВ-3,
      СБСекр5АР1/АС и **РостелP28R (42 дня без базы, крупный корп)**.
      Секьюритизация/ВДО биржа формулой не описывает — только проспект.
- [ ] Мелочь на будущее: `count()['incomplete']` = 4, а `queue_stats()` = 349 —
      два разных счётчика с одним именем на одном экране админа. Не баг расчёта,
      но читателя путает.
- [ ] Отдельным вопросом: **smart-lab как источник формулы — мёртв.** Страница
      выпуска не содержит ни базы, ни спреда (проверено на трёх бумагах, HTTP 200,
      совпадений по «ключевая ставка»/RUONIA/спред — ноль). Он остаётся только
      источником рейтинга и типа купона.
- [ ] Инференс по одному купону (когда биржа молчит) — **не годится**: при плоской
      КС маржа под KEYRATE выходит «круглой» автоматически, и эвристика
      округления выбрала бы KEYRATE для РЖД 1Р-54R, у которого на самом деле
      RUONIA. `scripts/infer_base_margin.py` не зря требует 3 выплаченных купона.

### N3. Аналитика строится от средневзвешенных цен — `P1` — СДЕЛАНО 26.08
- [x] **Проверено: базы разъезжались.** У флоатеров scatter и box считались по
      средневзвесу, а линия «R-spread ДИНАМИКА» читалась из `spread_daily` — там
      спред по цене ЗАКРЫТИЯ (вечерний снапшот или honest-бэкфилл по close
      свечи), — и при этом её сегодняшнюю точку фронт достраивал опять по
      средневзвесу: две базы на одном графике, три на вкладке. Разница по бумаге
      4–10 бп, по медиане бакета до 18 бп (замер 17–26.08 на проде).
- [x] Динамика переведена на `bar_daily` (свёртка баров, средневзвес). Побочно
      ушли **строки за дни без сделок**: `spread_daily` заводит строку на каждый
      календарный день, включая выходные (523 строки за субботу 16.08), и линия
      шла по стейл-цене.
- [x] Для этого починена сама свёртка: **часы несут горизонт с версии движка 6,
      а `build_daily` его терял**. Без горизонта линию по средневзвесу строить
      нельзя — спред к оферте сложился бы со спредом к погашению (обвал на
      220 бп без движения цены). Рядом лежит спред ко второму горизонту,
      взвешенный тем же оборотом. Задеплоено `d45f322` + разовый прогон
      `scripts/backfill_daily_bars.py --days 400 --force` (1340 бумаг,
      292 176 дней): горизонт есть у 98,6 % строк с 2025-09.
- [x] **Вкладка ФИКСЫ** тоже переведена на средневзвес (`6e01d70`): `WAPRICE`
      борда шёл только фолбэком для `last`, теперь отдельным полем; g-спред по
      нему считается прямым пересчётом (700 бумаг 128 → 256 мс, heavy-поток).
      Свой тиковый средневзвес впереди биржевого. **НЕ ЗАДЕПЛОЕНО** — ждём,
      пока вторая сессия закончит работу над телеграм-дайджестом: `deploy.sh`
      синкает дерево целиком, а её `api/main.py` уже импортирует код, чей
      Pillow в прод-`requirements.txt` ещё не попал.
- [ ] Не проверено на живом: фиксы после деплоя — что `g_spread_wap_bps`
      приходит в строке и графики встали на него.

### N4. Спред врал до перезагрузки страницы — `P0`
- [ ] Воспроизвести и починить: 26.08 спред показывался неверно, пока страницу
      не обновили руками. То есть **клиентское состояние расходится с сервером** —
      либо кэш react-query не инвалидируется, либо WS-апдейт приносит частичные
      поля и затирает пересчитанные.
- Что проверить в порядке дешевизны:
  1. `staleTime`/`refetchInterval` витрины и карточки — не отдаётся ли
     дореплойный/довечерний снапшот из кэша query.
  2. WS-канал рынка: мерж апдейта в строку — не перетирает ли он `dm`/`y_idx`
     старыми значениями, когда апдейт пришёл без метрик.
  3. Смена **горизонта** (put/call vs погашение) и смена **базы цены** — не
     остаётся ли метрика от прошлого выбора (см. `pricing-horizon-rule`).
  4. Стейл-цена: спред мог быть посчитан от вчерашней цены, а после F5 — от
     сегодняшней (тогда баг не в фронте, а в свежести цены).
- Нужен точный сценарий воспроизведения (какая вкладка, какая бумага, что
  показывало до F5 и после) — без него легко починить не то.

### Сводный приоритет (новые + существующие)

| P | задача | почему здесь |
|---|---|---|
| **P0** | Чистый замер свежести ленты за сессию 26.08 (см. блок выше) | замер живёт ровно один день, потом окно уйдёт |
| **P0** | **N4** спред врал до F5 | показания прибора неверны у пользователя на экране — худший класс бага |
| ~~P0~~ | ~~**N2** новый выпуск не доезжает 12 дней~~ | СДЕЛАНО 26.08: формула из карточки MOEX + суточный TTL + список на /status. Осталась проверка на проде |
| **P0** | TZ-фикс проверить на живом контейнере (аудит 25.08, «Не закрыто») | фикс задеплоен, но не подтверждён; без `tzdata` он молча нерабочий |
| ~~P1~~ | ~~**N3** база цены в Аналитике~~ | СДЕЛАНО 26.08: флоатеры задеплоены, фиксы ждут деплоя (в дереве работа второй сессии) |
| ~~P1~~ | ~~**N1** цвета сделок в уведомлениях~~ | СДЕЛАНО 26.08 (`e9aad27`), задеплоено |
| **P1** | Курс для остальных валют номинала (`no_fx`, CHF) | сделка есть, звонка нет — тихая потеря сигнала |
| **P1** | Рублёвый объём валютных бумаг в `trade_tick` | подпирает N3 (VWAP замещаек) |
| **P2** | Ретеншен архива тиков / рост базы 2,8 ГБ | сторож звонит, падения не будет; решение «что теряем» — за пользователем |
| **P2** | Правило «шард молчит НЕнормально» | доставка тревог готова, осталось правило |
| **P2** | `pending_alerts`: отбор по `cur` пересмотреть | после фикса курса отбор стал непоследовательным |
| **P2** | Хвост спек фиксинга (ПионЛиз, 10 KEYRATE-бумаг) | 0,15–0,75 пп, точечно и дорого |
| **P2** | `bars.py:1016` face последнего бара, `metrics.py:122` breakeven мимо лесенки | из внешнего аудита, не проверены, дёшевы |
| **P3** | non-root контейнер, циклический импорт core↔services, 5 cashflow-builder'ов | техдолг без внешнего эффекта |


## 🔴 Стакан + алерты (новая крупная фича)

### Стакан выпуска (order book)
- [x] Кнопка открытия стакана **рядом с названием** вверху карточки облигации.
- [x] Панель стакана открывается **слева** от карточки (вторая выезжающая панель).
- [x] Данные: `GET /api/orderbook/{isin}`. Live-обновление — **поллинг 3с** (react-query refetchInterval). Alor WS — TODO (оптимизация нагрузки).
- [x] **Для КАЖДОГО уровня цены** (bid и ask) считать **SM и DM** — бэкенд готов: `services/bond_details.load_reprice_ctx` (тёплый ctx один раз) + `reprice_at_price(ctx, price)` (чистая, батчится по уровням). `/orderbook` считает SM/DM per-level с полными amorts/offers.
- [x] Колонки уровня: цена · объём (шт; ₽ в title) · SM · DM · YTM.
- [~] Работает для флоатеров (SM/DM/YTM). Для фиксов: кнопка есть, но per-level SM/DM = None (base не RUONIA/KEYRATE) → показывается только YTM. Проверить на живом фиксе; при желании g-спред per-level.

### Алерты по стакану — СДЕЛАНО (2026-07-27, задеплоено)
- [x] **Тип 1 — форма:** объём + цена/YTM/DM(g-спред) + сторона. `OrderbookAlerts.jsx` в панели стакана. op авто-выводится из стороны+метрики.
- [x] **Тип 2 — Ctrl/Cmd-клик** по уровню → префилл формы (сторона+цена уровня). Объём вводит юзер.
- [x] Мониторинг: `alerts_monitor` в main.py, каждые 12с в торговые часы, батч по (isin,kind), матч против Alor-стакана «на уровне/лучше» + накопленный объём шт/₽. Нотификация: `AlertsWatcher` (тост+бип WebAudio, поллинг 8с). WS-push/e-mail — позже.
- [x] Хранение: portfolio.db v3 таблица alerts, per-user. `services/alerts.py` CRUD + evaluate. `api/routes/alerts.py` GET/POST/DELETE.
- [x] Состояния: active/fired/cancelled + история (list_for_user).
- [x] Полировка: редактирование алерта (PATCH), подсветка сработавшего уровня в стакане (красный ⚠), плашка в StatusBar + попап + клик→карточка/стакан, красная строка бумаги. Задеплоено 2026-07-27.
- [x] **Alor WS реал-тайм стакан** — `services/alor_ws.py` персистентный WS, reprice+memo, broadcast через канал orderbook; фронт `connectOrderbookWs`, HTTP-поллинг фолбэком. Формат Alor подтверждён. Задеплоено 2026-07-27.
- [x] Telegram-push — СДЕЛАНО: сигналы дублируются в привязанный чат
  (`services/tg_notify.py`), задержка ~1–4 с; безадресные сделки звонят с живого
  потока Alor, а не с ISS-ленты. Свои эмодзи маркеров — `/custom`, отдельный
  канал на фильтр («Р5» → канал «Р5») — `/chats` + селектор в форме сигнала.
  Подробности и каветаты: STATUS.md, «Изменения на проде 2026-08-21, вечер».
- [x] Один принт доходит до КАЖДОГО канала (`1b358ce`, 25.08): дедуп блоков был
  по пользователю, широкий фильтр забирал сделку у узкого, и канал «Р5» не
  получил ни одного события за всё время. Ключ теперь `(пользователь, адресат)`.
- [ ] Хвосты: WS-push алертов (мало смысла — монитор 12с); e-mail-push (отложено).

**Заметки по реализации:**
- [x] SM/DM per-level: чистая `reprice_at_price(ctx, price)` вынесена из reprice (тёплый ctx → быстро для 10-50 уровней).
- Мониторинг: отдельный воркер в поллере, который для watch-алертов тянет стакан и матчит. Не долбить Alor на каждый алерт — батчить по isin.

---

## 🟡 Флоатеры — техдолг

### Расчёт (точность, низкий приоритет)
- [x] RUONIA daily-comp — ЗАКРЫТ 2026-08-21 бэктестом по факту выплат
  (`scripts/verify_fixing_specs.py`): индексные купоны (ВЭБ.РФ, Роснефть,
  ОФЗ 29026+) уже считаются капитализацией через `spec.compounded`, медиана
  ошибки по 99 RUONIA-бумагам 0.013 пп. Врал сам прибор — терял флаг в pspec
  (11 ложных WARN, коммит `f7a708c`).
- [ ] ПионЛизБР5 / ПионЛизБО6 (RUONIA): спека НЕ подобрана — 1.6–1.9 пп ошибки
  в ставке купона. `default(fwd~avg)` и `avg_window=1`, текста проспекта в базе
  нет. Лечится подбором (`scripts/fit_spec.py`) или ручной спекой.
- [ ] Хвост KEYRATE-расхождений (10 бумаг, 0.15–0.75 пп): ДОМ.РФ15об,
  ТрансФ1P05, КВС об.01, Ситимат01/Б2, ПушкПЗ1Р03, АЛЬФАT2CR6, ГТЛК 2P-01,
  РСХБ2Р31, МБЭС 2P-03. Старые выпуски со ступенями/экзотикой.
- [ ] `cut_at_offer` reset-детект мимо не-Cbonds бумаг → старый спред за reset.
- [ ] `yield_over_index` для амортизаторов apples-to-oranges (индекс роллится до дальнего погашения) — вторичка.
- [ ] Settle-модель: календарный T+1 vs бизнес — нужен бэктест (референс был НРД, off → отложено бессрочно).

### Чистка / рефактор
- [x] Мёртвый код — ПРОВЕРЕНО (список устарел): `get_cashflow_items`/`get_moex_coupon_fallback`/`searchBonds`/роут `/valuation` уже удалены; `calculate_floater_metrics` живой (`last_prices.py:301`); `solve_z_bps` живой (тест + `scripts/nrd_pipeline_probe.py`). Удалять нечего.
- [x] `/orderbook` не передавал amorts/offers → расходился с карточкой — ПОЧИНЕНО (reprice_at_price). Осталось `/valuation` роут (фронт не зовёт).
- [ ] Core↔services циклический импорт (держится на lazy-import) — landmine рефактора.
- [ ] 5 cashflow-builder'ов — консолидация (частично слито).

### Безопасность
- [x] login rate-limit по proxy-IP: был XFF, но брал ЛЕВЫЙ (спуфабельный) адрес → обходился ротацией. ПОЧИНЕНО: правый адрес (Caddy-added, edge). backoff 5→300с→…→1ч cap.
- [ ] Полный non-root контейнер (сейчас `cap_drop:ALL`+`no-new-privileges`; `data/` root bind-mount → нужен gosu-entrypoint).

### Живая лента и сигналы (после захода 25.08)
- [ ] Курс не для всех валют номинала: `services/fx` знает USD/CNY/EUR, а
      бумаги в CHF (сегодня 2 выпуска) дают `fx_ok=False` — тик пишется в
      архив, звонок не идёт. Лечится добором курса ЦБ по остальным валютам;
      счётчик пропусков — `no_fx` в `/api/status`.
- [ ] `trade_tick` хранит объём валютных бумаг В ВАЛЮТЕ НОМИНАЛА (пересчёт по
      курсу сделан только в стриме, для порога и звонка). Часовые бары и VWAP
      по замещайкам из-за этого не в рублях — проверить, где это вылезает на
      витрине, прежде чем чинить.
- [ ] `pending_alerts` отбирает `cur IS NULL OR cur='SUR'`, а живые тики Alor
      пишутся с `cur='SUR'` жёстко (`ingest_ticks`). Теперь, когда рублёвый
      объём валютных сделок считается верно, отбор стоит пересмотреть — иначе
      ISS-копия той же сделки с настоящим `cur` ведёт себя иначе, чем тик.
- [ ] Сторож шардов: счётчики есть, тревоги нет. Тихо молчащий шард ловится
      глазами по сводке лога (`БЕЗ ЕДИНОГО ТИКА: [id]`). Автотревогу не делал
      осознанно — хвост рынка неликвиден, ложняки; при желании привязывать не к
      тикам, а к сверке с ISS. С 25.08 доставка тревог готова и переиспользуема
      (`api/main._watch_alert` + `tg_notify.notify_admins`), так что осталось
      только правило «шард молчит НЕнормально» — оно и было настоящей задачей.

### Место на диске (после захода 25.08)
- [x] Тихий отказ обслуживания — ЗАКРЫТ: `disk_watchdog` звонит админам, когда
      свободного меньше `DISK_NEED_RATIO`×размер базы или свежей копии нет
      дольше `BACKUP_STALE_HOURS`. Пропущенный ночной VACUUM сообщается отдельно.
- [ ] САМ РОСТ базы не лечится сторожем. На 25.08: `portfolio.db` 2,8 ГБ +
      копии 1,6 ГБ, свободно 4,9 ГБ из 30 (83 %), рост десятки МБ в день.
      Решение — окно сырых тиков в `trades_archive.prune`: сколько дней держим
      поштучно и с какого объёма. Это выбор («что мы готовы потерять»), а не
      техработа, поэтому не делается само.

### Данные / инфра
- [x] Рейтинг-драйн: negative-кэш 404 (miss-маркер, _NEG_TTL 1д) + драйн 24/7 + батч /api/fixed + дедуп по реестру — СДЕЛАНО (коммиты perf(ratings)/fix(ratings)).
- [ ] Свежесть КС/RUONIA/cbr_forecast в UI (сейчас частично в `/meta` warnings).
- [ ] Хардкоды руками: `_CB_MEETINGS`, MOEX-праздники, `cbr_forecast.json` (обновлять после заседаний ЦБ).
- [ ] Суборд-исключение из B-бакета — нет флага в MOEX (блокировано данными).

### Аналитика (если шире)
- [x] Динамика спредов — `SpreadHistory.jsx` + `/api/history/{isin}/spread`: свечи 1d × reprice под текущий ctx → серия DM(флоатер)/g-спред(фикс) за 3м/6м/1г в карточке. Оценка (историч. цена × текущая модель). Задеплоено 2026-07-27.
- [x] Динамика спредов: candle-оценка (историч. цена × текущая модель) + интерактив (hover) + **точная история** (forward daily snapshot spread_daily, снапшоттер 19:00 МСК, эндпоинт мержит exact+est). Задеплоено 2026-07-27.
- [ ] Ещё аналитика: тепловая карта рейтинг×дюрация, скрин по carry/Y-IDX, экспорт CSV.

---

## 🟢 Фиксы — хвосты
- [x] Аналитика фиксов (scatter g-спред/дюрация, box-распределение по рейтингам/эмитентам, гистограмма срочности). Тоггл Список/Аналитика. `FixedAnalytics.jsx`. Задеплоено 2026-07-26.
- [x] Карточка фикса: reprice-калькулятор цены (как у флоатеров). `compute_fixed_row(price_override=)` + `GET /api/fixed/{isin}/reprice` + инпут в FixedCard. Задеплоено 2026-07-26.
- [x] Рейтинг: 2-й источник **smart-lab.ru** подключён (`enrich_smartlab.py`, фолбэк в `ratings.refresh` при промахе corpbonds). MOEX ISS рейтинг не отдаёт. Тест 8/8 мелких ВДО. Драйн перебирает ~784 непокрытых, покрытие растёт (смотреть СТАТУС).

---

## Выкинуто осознанно
Фонды (убрали), НРД-сравнения (удалили слой), z-перцентиль/Δz D-D (были на мёртвом НРД-z).
