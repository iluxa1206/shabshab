# План внедрения фиксов — 2026-08-26

Источник: аудит `docs/audit_2026-08-26.md` (8 агентов, ~100 находок) + 6 агентов-верификаторов,
проверивших радиус поражения каждого фикса на живом коде и данных.

**Главный вывод верификации: ни один патч из аудита нельзя применять дословно.**
Пять предложений оказались опасны (ломают тесты, дают регресс или OOM), три находки —
ложные, две недооценены, один настоящий корень найден только на верификации.

---

## Отменено (проверка не подтвердила)

| находка | вердикт |
|---|---|
| `tests/test_tg_digest.py` без Pillow — «дыра в CI» | **НЕПРАВДА.** `Pillow~=11.0` в `requirements.txt:19` как рантайм-зависимость прода. Отстал локальный venv. `pip install -r requirements.txt`. |
| `services/benchmarks.py` мёртв | подтверждено, но это уборка, не фикс |
| `instruments_registry.py:66` пропущенная запятая | подтверждено, **ущерб нулевой**: `ALTER` из `_MIGRATIONS` возвращает `cap_pct`, SQLite типизирован динамически. Косметика + тест-сканер схемы. |
| `scripts/backfill_horizon.py` «ретроспективно переписывает историю» | **НЕПРАВДА.** Скрипт случайно консистентен: «последний известный горизонт» — ровно то, что нужно `_one_horizon`. Чинить докстринг. |
| `payments_calendar` дублирует принципал | код-дефект реален, **достижимость 0**: прогон `build_payments_calendar()` — 21 035 событий, 0 деградированных. Все 598 бумаг универса имеют линейную базу. |

## Отменено (лечение хуже болезни)

| предложение | почему нельзя |
|---|---|
| `fixed_income`: достраивать `residual` до биржевого номинала | **регресс.** `put_date` у ABS — не оферта, а первый купон с `value=None`. Патч платит 577.64 через месяц → YTM 33–143 %. Сейчас `None`, после патча — уверенное враньё. Правильно: **отказ**, как во флоатерах. |
| `hz_key` по дате точки | в архив пишутся **оба** горизонта (`y_idx` + `y_idx_alt`), `_one_horizon` их восстанавливает — ущерб ≈0. А пер-дневный горизонт в режиме «Авто» (дефолт, `ChartPage.jsx:235`) даст **лестницу** на каждом пересечении порога выкупа у 107 бумаг + глобальный бамп версии. |
| стакан: TTL per-ISIN | **регресс.** Alor шлёт книгу по изменению (троттл 1700 мс), не heartbeat'ом. Неликвид молчит часами при живом сокете и выпал бы из фильтра по объёму. Отказ шардовый (`_SHARD_SIZE=150`) — метка должна быть шардовая. |
| `_realized`: `if obs <= dts[-1]: return True` | ломает `test_realized_internal_hole`, снимает защиту от дыры **источника** (регресс Росагрл1Р5). Праздник и сбой по длине неразличимы: NY-2026 = 14д, дыра источника = 13д. |
| `_last_obs_date`: `if w and int(w) > 1` | W=1 проваливается в ветку `average` → погасило бы определённые купоны у **152 бумаг**. Ломает 2 теста. |
| эхо-правило: «при `det is None` не гасить» | снимает эхо-защиту с **73 будущих** купонов (67 у ПионЛизБР5 — настоящий forward-fill). Ломает 2 теста. |
| калибратор: «отрыв от второго места» по лагам | на плоской КС соседние лаги дают **точно** равную ошибку (РОССИУМ3P2: `point/2..6 = 0.0833`) — порог снёс бы все point-бумаги. Отрыв допустим только МЕЖДУ РЕЖИМАМИ. |
| `_rate_avg`: добавить `_realized` | у калибратора нет `fwd_pct` — дни дыры выпали бы из среднего (n 92→84), сдвиг цели фита до −13 bps **переставляет argmin и меняет откалиброванный лаг**. Не нужен вовсе: после фикса `_realized` проекция совпадает с калибратором до 6 знаков. |
| мемо оферт по `day_iso` | 457 КБ × `_ASOF_MEMO_MAX=120` = **+55 МБ** при 768 MiB и истории OOM. Ключ — ближайший колл. |

---

## ВОЛНА 0 — сегодня, без пересчёта, деплоится сразу

Ничего не меняет в числах. Только гасит боевые отказы.

1. **`services/alor_ws.py:302`** — распаковать `val, _fx_ok = _tick_value(...)`.
   Масштаб больше заявленного: `try:` на `:161` накрывает весь `while True`, `except` на `:318`,
   поэтому TypeError рвёт **весь сеанс вместе с подписками на стаканы**, а `backoff` сбрасывается
   в 1 → шторм реконнектов раз в ~1 с. Стакан карточки живёт на HTTP-фолбэке.
   **Обязательно вторым движением:** сузить `try` до обработки одного сообщения.
   `_fx_ok` игнорировать — `trades_stream._on_trade:394` кладёт value безусловно.
2. **`services/cbr.py`** — три правки одним коммитом:
   - `_mem_fresh()` по truthiness вместо `is not None` (пустой `[]` пиннился на сутки);
   - троттлинг ретрая 15 мин по образцу `market_data.py:258` — иначе `_refresh()` из
     `_index_provider` (на каждую бумагу) и `ks_rate_at` (на каждый купон) = тысячи
     `requests.get(timeout=20)` под глобальным локом;
   - **`:256` `_save_cache` не должен затирать непустой кэш пустым фетчем.**
     ПОДТВЕРЖДЕНО ЖИВЬЁМ: локальный `ruonia_live` = 2 точки вместо 277.
     `ks` защищён порогом `len(ks) < 100`, у RUONIA порога нет. **Проверить прод.**
3. **`services/backdate.py:967`** — перенести снос стейл-строк из начала в `_flush`.
   Наивный перенос «в конец» СЛОМАЕТ пересчёт: `upsert_honest` делает
   `UPDATE … WHERE y_idx IS NULL`, строки с непустым `y_idx` не обновятся и не вставятся.
   Нужен новый `spread_history.drop_honest_dates(isin, dates)`, вызываемый ровно перед
   вставкой посчитанной порции. **Обязан лечь ДО волны 3** — это страховка «флак ISS
   не съест историю» во время самого пересчёта.
4. **`api/routes/signals.py:133`** — `Query(EVENTS_LIMIT, ge=1, le=500)` + пояс в
   `services/signals.py:249`. AST-скан всех роутов: это **единственный** такой дефект.
   Фронт зовёт 60 и 100 — не сломается.
5. **`services/instruments_registry.py:66`** — запятая + тест-сканер схемы
   (`PRAGMA table_info`, тип ≤2 слов). Скан 19 таблиц: больше нигде нет.
6. `scripts/backfill_horizon.py:16-18` — правка докстринга.

## ВОЛНА 1 — реестр и Справочник. Порядок ЖЁСТКИЙ

Разморозка 538 строк (46 % универса). Шаги нельзя переставлять.

1. **diff-импорт xlsx** (`api/routes/instruments.py:263`) — `set_manual` только по
   различающимся полям. Сравнивать ПОСЛЕ нормализации легаси (`point → average+W=1`),
   числа с допуском, `old is None` → всегда «различается».
   Намеренную очистку не теряем: импорт и сейчас не умеет очищать (`_coerce` → `None`),
   для этого есть `reset_manual`. Добавить `unchanged` в ответ и во фронт
   (`Catalog.jsx:216`) — иначе «обновлено 1» вместо «538» прочитается как поломка.
   **Сам по себе 538 строк НЕ лечит** — `manual_locked` это колонка, а не свойство операции.
2. **`face_value` вон из `_MANUAL_FIELDS`** (`:31`) + **одновременно** FACEUNIT-guard в
   `instruments_sync.py:78`. Проверено на данных: расхождений реестр↔MOEX 27, все в одну
   сторону; бумаг вне листинга 7 (не затрагиваются, `upsert` пропускает `None`);
   валютных среди locked — 0. **Бэкфилл не нужен**, первый дневной синк чинит сам.
3. `scripts/unfreeze_fixing_spec.py` (dry-run → `APPLY=1`).
4. `scripts/unlock_auto_imported.py` (dry-run, глазами список `ОСТАВЛЕНА`, → `APPLY=1`).
   **КОНФЛИКТ ПО ЗАМЫСЛУ с шагом 3:** unfreeze специально сохраняет lock («не даёт sync'у
   перезаписать обнулённые поля»), unlock его снимает. В обратном порядке разлоченные
   строки получат от синка обратно cbonds-спеку. Требует `bondsearch_*.xlsx` в корне.

## ВОЛНА 2 — пагинация ISS и потоки

Корень найден на верификации, его и чиним первым.

1. **`services/market_data.py:669` — КОРЕНЬ.** `fetch_bond_schedule_full` читает
   `amortizations` только со страницы `start == 0`. График амортизаций обрезан на 100
   строках у **22 бумаг**, раскрутка идёт по купонам, поэтому обрыв не ловит никакой `raise`.
   Именно это, а не «MOEX публикует один транш», порождает `face = 8.78`.
   Фикс: отдельный запрос `iss.only=amortizations` с пагинацией.
2. **`services/market_data.py:1342`** — `raise` вместо молчаливого `break` (семантика
   `:640-650`), `:1371` — лог вместо немого `pass`, `:1308` — ключ `_trading_day()`.
   Per-isin изоляция **уже есть** (`except` в `fetch_one`), доп. try не нужен.
   Бумаг с пагинацией: 46 из 605. Один ре-warm после деплоя (~600 запросов, семафор 5) —
   катить до 09:00 МСК или сразу после `daily_prewarm`.
3. **`services/payments_calendar.py:174`** — `gather(..., return_exceptions=True)`.
   Сегодня одна бумага с обрывом валит сборку всего календаря; без этого шаг 2
   будет выглядеть виновником.
4. **`services/fixed_income.py:82`** — guard **на отказ** (`return [], None, None` +
   `incomplete_schedule`), не на достройку. Порог 0.95 как во флоатерах: разброс
   бинарный (1.0000 либо 0.015), ложных срабатываний 0 из 1239.
5. **`coupons_per_year > 12`**: `max(1, 12 // max(1, cpy))` в `core/valuation.py:107`,
   `services/bonds.py:235` и `:289`. Валидация — **в `set_manual`**, а не в
   `InstrumentParams` (тот с `le=365` пропускает 26, и xlsx его минует).
   В БД уже есть `RU000A1093L2` ВЭБP-46 `cpy=26` — записано автоматически
   `instruments_sync.py:500` (`round(365/14)`). Зависание не случилось только потому,
   что `cpd=14` уводит в другую ветку. Но `next_coupon_after` при `step=12//26=0`
   **уже сейчас** возвращает дату погашения вместо ближайшего купона.
6. `services/cashflow.py:134` + `payments_calendar.py:142` — `residual` вместо полного
   `face`. Достижимость сегодня 0, но 22 бумаги «на полке».
7. `services/paths.py:28` — `tmp = f"{path}.tmp"` фиксирован, два писателя интерливятся
   и бьют кэш (наблюдалось живьём на `schedule_full_cache.json`). Уникализировать tmp.

## ВОЛНА 3 — калибровка купонов. Порядок ЖЁСТКИЙ

1. **`_realized` через производственный календарь** — не по длине дыры, а по числу
   рабочих дней в ней (≤2 = праздник, иначе сбой источника). Проверено на 45 легитимных
   дырах; оба существующих теста проходят без правок. Затронуто **307 бумаг / 628 периодов**.
   `core/forwards.py:101` НЕ трогать — у форвардной кривой прошлого нет по конструкции;
   максимум диагностический лог на вызов с `d < calc_date`, и то ПОСЛЕ фикса.
2. **`_rate_avg` — только тест-инвариант, кода нет.** После шага 1 проекция совпадает
   с калибратором до 6 знаков. Тест-сторож: `projected_ks_pct` с `fwd_pct=lambda d: 999`
   на целиком прошлом периоде обязан равняться `_rate_avg`.
3. **`_last_obs_date`** → `return hi if int(w) <= 1 else hi - timedelta(days=1)`.
   Меняет `det` у 152 бумаг — обязан лечь ДО шага 4.
4. **эхо-правило** → `kill = (det is False and future) or (det is not True and echo and (future or det is False))`.
   Спасает начавшийся купон у АРАГОН об и ТАЛК002P04, 73 будущих гашения не трогает.
   ОФЗ-ПК 29007–29010 не затронуты (у них `average/W=182`, ветка `det is False`).
5. **гейт калибратора** — `≥3 купонов` + `размах ≥1 пп`, отрыв **только между режимами**
   (×3). Затронутых бумаг 0 — чистый guardrail. Последним, чтобы не мешать замерам 1–4.

**Остаточный долг (отдельной задачей):** `_rate_avg` не знает `lag_unit` — всегда календарь,
а `projected_ks_pct` умеет `work`. Бумаг с рабочим лагом 126 из 608.

## ВОЛНА 4 — история. ОДИН точечный пересчёт

**Глобальный бамп версий НЕ делать.** Расходится ≤150 бумаг, а бамп гонит все 1318 —
ровно тот прогон, что ловил OOM 768 MiB.

1. **оферты на день точки**, мемо по ближайшему коллу (не по `day_iso`).
   `call_offers_asof` = 3,2 мкс, по CPU бесплатно. Расхождение `/reprice` vs график
   доказано: 15 из 48 бумаг на 180-дневном окне (RU000A103QN7 — 0,07 года против 5 лет).
2. **номинал дня** + **перестановка блока НКД выше блока номинала** в `fn`.
   Недетерминизм двух видов: цикл идёт `reversed(rows)` при стриминге и вперёд иначе
   (`:865`), плюс `fn` живёт в `_asof_memo` между запросами. 113 амортизируемых из 601.
   Записанные точки пересчитать нужно — правильных среди них нет.
3. `hz_key` **аргументом** из `ensure_honest_backfill` (шов уходит, числа не меняются)
   + `_one_horizon` в `/{isin}/spread` и колонки горизонта в ветке `bar_daily` у
   `/multi/spread` — вот это реальные дыры чтения.
4. Один прогон `scripts/recalc_bonds.py` по объединению «есть `call_dates`» (48) ∪
   «амортизируется» (113) ≈ **150 бумаг**, минуты.

## ВОЛНА 5 — доступ и уведомления

1. **каскад удаления аккаунта** — новый `services/user_purge.py`, одна транзакция,
   порядок: `signal_state` (по `filter_id`, пока фильтры живы) → `signal_events` →
   `signal_filters` → `tg_targets` → `trade_flag` → `tg_users` (**UPDATE**, не DELETE —
   чтобы чат мог подать заявку заново). Править и `api/routes/auth.py:305`, и
   `scripts/useradd.py` (подкоманда `remove`, не `--del`). ~55 строк.
2. **`tg_notify.py:129`** — проверять живость привязки перед использованием канала.
   **РЕШЕНО 2026-08-26: mute глушит ТОЛЬКО личку.** Значит `has_chats()` не подходит
   (он включает `muted`) — нужна отдельная `tg_users.email_exists_approved(email)`:
   `status='approved'` без учёта `muted`. Revoke и удаление аккаунта канал глушат, `/mute` — нет.
3. **стакан: метка по ШАРДАМ**, не по ISIN. `{shard_id: ts}` + `{shard_id: [isin]}`,
   TTL переиспользовать `_STALE_SEC=900`. Протухшие ISIN **изымать из словаря**,
   не класть пустую лестницу (`{"b":[],"a":[]}` прочиталось бы как «в стакане пусто»
   и дало ложный `money=0`). ~18 строк.
4. `bond_details`: НЕ резать таблицу потоков, а маркировать `beyond_horizon`
   (25 бумаг из 111 с офертой). Горизонт — предположение с 10-bps порогом, график —
   факт эмитента; при резке 110 строк мигали бы от одного тика цены.
   **А в waterfall PV паспорта (`bond_audit.py:495`) резать НУЖНО** — там `Σ PV`
   сверяется с `dirty` из `val_dict`, и у тех же 25 бумаг сейчас ложный разрыв.

## ВОЛНА 6 — РЕШЕНО 2026-08-26: ОСТАВЛЯЕМ КАК ЕСТЬ

**`core/forwards.py:287` — конвенция фикс-ноги KeyRate-IRS.** Это не баг, а методика.
**РЕШЕНИЕ ПОЛЬЗОВАТЕЛЯ: годовая нога — сознательный выбор, ничего не пересчитываем.**
Задача сводится к документации: привести в соответствие докстринг `SheetForwardCurve`
(`core/forwards.py:254-276`), `get_curve_plot`, `PROJECT_GUIDE.md:113` и комментарий
`services/ks_path.py:60-66`, а также пометить `_bootstrap_par_curve` (`months=3`) как
ДРУГУЮ конвенцию, а не как эталон для sheet-ветки. Плюс пометить
`services/implied_curve.py:_ks_implied_avg` как реплику файла, расходящуюся с прайсингом.

Репрайс свопов по каждой кривой:
```
ten   quoted  sheet@Q  sheet@A  boot@Q
1Y    14.040   13.373   14.044  14.040
10Y   15.100   14.175   14.944  15.100
```
Формула `4·((1+par)^0.25−1)` точно консистентна с ГОДОВОЙ фикс-ногой.
Противоречит `_bootstrap_par_curve:425` (`months=3`), докстрингу «конвенция СПФИ МБ»
и `PROJECT_GUIDE.md:113`. При квартальной ноге `avg = par` тождественно, пересчёт не нужен;
закрытой формулы «avg из par» при квартальных платежах и неплоской кривой НЕ СУЩЕСТВУЕТ.

`services/ks_path.py:60-66` уже документирует, что эта формула «на ~1.7–2.5пп ниже
bootstrap и НЕ арбитражна», и её выкинули из графика КС 20.07.2026. Через 9 дней она
вошла в прайсинг как `SheetForwardCurve`.

Цена правки: **Y-IDX +73…+91 bps у ВСЕХ KEYRATE-бумаг**, SM/DM ≈0, RUONIA 0.
Рвёт сопоставимость KEYRATE↔RUONIA во всей витрине/скринере/алертах, ступенька на линии.
Требует бампа обеих версий + миграции строк `src='snap'` и легаси `src IS NULL`
(их `drop_stale_honest` не инвалидирует — бьёт только по `src='honest'`).
Править синхронно с копией в `api/routes/curves.py:83`, иначе график и таблица разойдутся.

**`services/backdate.py:139` — а это баг, и он в волне 4.** `n = (t2 - a).days` +
guard `if n <= 0: return 0.0`. На типовом вызове +16 bps, но на пути
`core/valuation.py:386`: `t1 = eff−90д` → 0.18 % вместо 16.00 % (−1582 bps).
