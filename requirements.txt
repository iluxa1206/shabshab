fastapi==0.115.6
uvicorn[standard]>=0.30.0
httpx[socks]==0.27.2   # socks-экстра: TG_PROXY=socks5://... на сайдкар xray
python-dotenv==1.0.1
pydantic==2.9.2
Jinja2==3.1.4
bcrypt==5.0.0
PyJWT==2.13.0
python-multipart==0.0.20

aiohttp~=3.13.2
requests~=2.32.3
pandas
beautifulsoup4
openpyxl
# Картинки «разбора дня» в Telegram (services/charts_png): рисуем сами
# примитивами. matplotlib не берём — на проде уже ловили OOM на 768 МиБ,
# а его импорт стоит десятки мегабайт RSS ради тех же четырёх сюжетов.
Pillow~=11.0
pytest
# без него @pytest.mark.asyncio-тесты не запускаются вовсе (pytest валит их как
# «async def not natively supported») — 6 тестов has_call молча не выполнялись
pytest-asyncio~=1.4